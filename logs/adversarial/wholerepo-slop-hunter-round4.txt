VERDICT: DO-NOT-SHIP (15 findings)

The tree is genuinely leaner than it was in round 3 - the CSS has zero dead
selectors, the deleted Galata test left no orphan, and every one of the 14
taken fixes reproduces. But round 3's deletions did what round 2's did: the
mkdirp removal updated two lockfiles and left the one the build actually reads,
the favourites-ternary fix collapsed three arms to two and left one still
unreachable, the setwinsize deletion left a fake pty behind with nothing to
fake and a comment explaining the absence by a mechanism the same commit
deleted. Claims check out except where a fix's own citation rotted under it.

GATE - three uncached legs, all green:
  npx eslint . --no-cache              0 errors, 54 warnings (baseline)
  npx prettier "**/*{.ts,...}" --check All matched files use Prettier code style
  npx stylelint --no-cache style/**    exit 0
  pytest 150 passed | npx jest 82 passed | npx tsc --noEmit exit 0
  Galata: 20 passed (logs/galata-round3.log, 2026-08-10 08:33, post-fix) - not re-run

VERIFIED FROM ROUND 3 (all 14 taken fixes reproduce in the live tree):
  workflow MANDATORY list renumbered to 2 items, no research-source-extensions
  pointer; fix-batch-{2,3,4}-orders.md gone; uv.lock ignored at .gitignore:145;
  prefersReducedMotion has zero hits tree-wide; _effective_colour takes
  (session_id, default, stored) at routes.py:198 with 3 call sites matching;
  IResolvedLaunchMode has no `value`, IMigratedProvider no provider_id /
  favourites; DEF-31 cites panel.ts:845 and :1012 and both land; ui-tests/tmp/
  and **/tmp gone from the ignore files; caplog gone from test_migrate.py.
  Both declines accepted - I did not re-raise either, and the launch-mode
  precedence argument (a test guarding against an inlined reimplementation, not
  against today's delegating one) defeats my round-3 mutation table.

## Findings

- **[CRITICAL] `yarn.lock` still declares the `mkdirp` this change removed from `package.json`** - axis 6, the deletion left the authoritative lockfile behind. `yarn.lock:7626` sits inside the root workspace block opened at `:7597` (`"jupyterlab_ai_code_assistants_extension@workspace:."` -> `dependencies:`) and reads `mkdirp: ^1.0.3`; `package.json` devDependencies no longer carry it, and `package-lock.json` was updated in step. The build does not read package-lock.json: `Makefile:77` and `.github/workflows/build.yml:30` and `:131` all install via `jlpm`, which is yarn berry here (`yarn.lock` `__metadata: version: 6`), and `.yarnrc.yml` sets only `nodeLinker` and `enableScripts` - so `enableImmutableInstalls` keeps its default, which berry flips to true whenever `CI` is set, as GitHub Actions does. JOURNAL entry 18 records `yarn.lock` being restored from git after a build rewrote it, which is exactly how the drift survived the round-2 proof (`npm install --package-lock-only --offline` touches package-lock.json only). This is the project's own rule at `.claude/CLAUDE.md` - "a lockfile that drifts from the manifest breaks reproducible CI builds". Load-bearing check: cleared as a FACT, not as a delete - I did NOT run `jlpm install` (banned by the Makefile-only rule, and it would rewrite the lockfile). Fix: regenerate the yarn.lock workspace entry, or put `mkdirp` back in package.json until it can be. Do not ship a manifest and a lockfile that disagree.

- **[CRITICAL] `_FakePty` and its scaffolding in `test_routes.py` are dead on arrival - 18 lines added this round, none of it exercised** - axis 3/1. Delete-tested in a scratch copy of the tree at `/tmp/claude-1000/r4/tree` (baseline there: 148 passed, 2 skipped - `test_descriptor_parity.py` skips twice without `lib/providers`):
    * `_FakePty.__init__` body (`self.ptyproc = self` at `:367`, `self.pid = 1` at `:368`) - class body replaced with `pass`: 148 passed, 2 skipped, unchanged. Nothing ever probes a terminal the fake holds, so `ptyproc.pid` (read at `routes.py:825-830`) is never reached.
    * `_FakeTerminalManager.create`'s `self.terminals["term-1"] = _FakePty()` at `:357` - removed: unchanged.
    * `_FakeTerminalManager.get_terminal` at `:360-361` - zero callers; the route reads `terminal_manager.terminals.get(...)` at `routes.py:817`. Removed: unchanged.
    Whole block cut (18 lines, `self.terminals` annotation retyped to `dict[str, object]`): 148 passed, 2 skipped - identical to baseline. Load-bearing check: cleared - `grep -rn "_FakePty\|_FakeTerminalManager"` over the tree returns only `test_routes.py`; no conftest export, no fixture, no reflection. The `_FakePty` class is the corpse of the deleted `setwinsize` assertion: at HEAD the class does not exist at all. Honest caveat on `get_terminal` alone: its docstring at `:342-346` claims it keeps the fake from hiding DEF-32, and I measured that claim - with the whole block cut, restoring `terminal_manager.get_terminal(terminal_name)` in `routes.py` still reddens `test_probing_an_unknown_terminal_creates_nothing` (`assert 500 == 404`). It changes the failure MESSAGE, not the outcome. Keeping two lines for that is defensible; keeping the class and the registration is not.

- **[MAJOR] The launch-site comment explains the absent resize by a mechanism the same change deleted** - axis 4, newly added and false on arrival. `core/routes.py:746-747`: "The pty is sized by ``_INIT_WAITER`` inside the child, not from here: a resize issued at this point races the waiter's own baseline read." `_INIT_WAITER` at `:53-62` has no baseline read - `read R0 C0` and the `trap 'CHANGED=1' WINCH` were both deleted by this same change, which is what `test_routes.py:406` asserts (`assert "WINCH" not in script`). Today the reason is different and stronger: a server-side resize to anything other than 1x1 breaks the child's poll on its FIRST iteration, so the assistant execs with no client attached. The same stale sentence is repeated at `test_routes.py:396-397`. Cut: state the live reason once, or delete both sentences outright - the 20-line header at `:33-52` already carries the whole DEF-33 post-mortem, including "no browser ever renders 1x1". The orphan `#` at `:748` goes with it.

- **[MAJOR] `'Empty.'` at `panel.ts:1405` is unreachable - round 3's own fix collapsed the ternary from three arms to two and left one of them dead** - axis 1, same species the fix was aimed at. Facts: `_render` returns early at `:1296-1308` when `sessions.length === 0`; with an empty filter `_matchesFilter` returns true for every row (`:533-535`), so `filtered === sessions`; `recent` is `slice(0, this._recentLimit)` and `setRecentLimit` clamps to `Math.max(1, ...)` at `:255`, with `schema/plugin.json` declaring `recentLimit.minimum: 1`; `all` is the whole set; and the favourites section renders only under `favourites.length > 0 || this._sessions?.some(s => s.favourite)` at `:1327`, whose two disjuncts are the SAME condition when no filter is active. So every section that reaches `items.length === 0` reached it through a non-empty filter, and the ternary always takes `'No matches.'`. Concrete cut: `empty.textContent = 'No matches.';` plus the two-line comment at `:1403-1404`, which documents a distinction the code no longer draws. Delete-test: `tsc --noEmit` exit 0 and `npx jest` 82 are unaffected - `panel.ts` has no Jest coverage - and `grep` over `ui-tests/tests/` finds neither string. Load-bearing check: cleared - no i18n layer in the tree, no test selector, and `jp-AiAssistantsPanel-emptySection` is styled by class, not by content.

- **[MAJOR] 117 byte-identical `log:` lines are 24% of the acc-crit document - the same duplication round 3 removed from the file directly above them** - axis 5. `docs/acc-crit-jupyterlab-ai-code-assistants.md` is 407 lines with 161 `- log:` lines, of which 117 are the identical 150-character string `- log: 2026-08-07 closed - post-fix conformance review (wf_a4376a8b-03e) verdict with file:line evidence; make install + 125 pytest + 59 Jest + 16/16 Galata green at v0.1.7`. Round 3 removed the 124 `criterion added` repeats from these same criteria and replaced them with one blanket sentence at `:5`; the larger twin was left. NOT a flat delete, and I proved it: stripping all 117 leaves 111 criteria with no log line and `acc-crit.py check --strict` fails - `0 error(s), 111 warning(s) [FAIL]`, because `--strict` promotes the "no log: line under criterion" warning (`acc-crit.py:201-202`, `:213`). Load-bearing check: NOT cleared for deletion, so this is a shorten, not a cut. Proven-safe form, tested: replace each with `- log: 2026-08-07 closed - conformance review (v0.1.7)` and move the run id and the counts once beside the blanket sentence at `:5` -> `0 error(s), 0 warning(s)`, 57,697 -> 43,891 bytes (-13.8 KB). The counts in the repeated string (125 pytest / 59 Jest / 16/16 Galata) are legitimate history and belong in the one surviving copy.

- **[MAJOR] `.claude/plans/close-adversarial-defects-v1.0.0.md` is a spent plan whose own execution rotted its citations** - axis 5, and it is new to the tree this round. Every item it plans is closed: DEF-32 through DEF-36 are all `[x]` in `docs/defects.md`. Its findings table (`:15-22`) cites line ranges its own execution moved - `core/routes.py:811` (N-1) now lands mid-comment, `routes.py:748-757` and `:753-757` (N-2, N-5) is the dict-or-object `terminal_name` resolution that replaced the deleted resize block, `panel.ts:2100-2108` (N-3) is the two `addItem({type:'submenu'})` calls, not the branch command, which is at `:1978`, and `docs/defects.md:115` (S-3) is DEF-29's fix line, not DEF-31's. Three of eight still land (`panel.ts:309-315`, `:1978-1983`, `terminals.ts:379-389`). It also carries pre-flight instructions for work already done - `:42` "Verify before committing to it: confirm `stty rows 1 cols 1` succeeds on the child's own tty". JUDGEMENT, stated as such: round 3 kept `.claude/workflows/implement-ai-code-assistants.js` because the global rules make an idempotent re-runnable SCRIPT the cross-restart resume recipe; a prose plan for finished work has no such function, and the durable record is `docs/defects.md`, which already carries every fix and both declines verbatim. Load-bearing check: cleared - untracked, and `grep` over the tree finds no reference to it from any file or script. Cut it, or re-pin the cite table - what it must not do is sit in the repo as the third artefact in this session to rot its own file:line evidence.

- **[MINOR] DEF-33's only evidence is a file `.gitignore` guarantees will never be in the repo** - axis 8, dead source. `docs/defects.md:36` cites `logs/waiter-race-sweep.log`; `git check-ignore -v logs/waiter-race-sweep.log` answers `.gitignore:4:*.log`. `git ls-files logs/` returns `README.md` and the `adversarial/*.txt` set and nothing else. Same class one line up in `logs/README.md:3`, which documents `make-*.log` - a file class the repo has never contained and cannot. Cut: the defect line already states the measurement in full ("BAD 10/10 at every delay from 3ms to 20ms ... BAD 0/10 at every delay, 0/20 at no injected delay"), so the path buys nothing and can go; `logs/README.md` should describe what is actually tracked there.

- **[MINOR] The `wholerepo-*-round3.txt` series does not exist, and `docs/defects.md` cites it five times** - axis 8. `logs/adversarial/` holds `wholerepo-{architect,bug-hunter,slop-hunter,ux-designer}-round1.txt` and `-round2.txt` only; the first campaign has its round3 files (`bug-hunter-round3.txt`, `ux-designer-round3.txt`) and the colour campaign is complete to round7. `grep -c "whole-repo round 3" docs/defects.md` = 5 - every one of DEF-32 through DEF-36 points its "reported" line into that hole, and `logs/README.md:4` documents the convention as "round results (`*-roundN.txt`), one series per adversary". Either the round-3 reports land in the series or the register should not cite them by round.

- **[MINOR] The `_tree_assistant` rationale is written twice, near-verbatim, both copies added this round** - axis 4. `core/routes.py:135-139` and `tests/test_routes.py:550-553` both read "a claude terminal that spawned ``codex`` as a tool must answer 'not yours' to the codex panel, or that panel reuses a terminal it does not own". Keep one - the test docstring is the natural home, since it is the reason the test exists; the function docstring can name the invariant ("shallowest wins; every enabled provider is asked") without re-arguing it.

- **[MINOR] The `force`-vs-resolved glyph rationale is written three times across three files** - axis 5. `src/core/modes.ts:56-62` (the `resolvedLaunchModeEntry` docstring), `src/core/panel.ts:1732-1737` (`_resolvedVariant`), and `src/__tests__/launch-mode.spec.ts:60-64` all argue that deciding the warning from the menu's `force` argument marks the safer-looking of two identical launches and leaves the clicked one unmarked. Keep it at `modes.ts`, where the mechanism lives; the other two need one line each pointing at it. Three copies of one argument is three places to drift.

- **[MINOR] `REFRESH_SPIN_FLOOR_MS` was inserted between `BRANCH_WATCH_INTERVAL_MS` and the comment that describes it** - axis 4. `panel.ts:77-78` reads "After a fork is requested, watch for its lazily-written store entry at this faster cadence (bounded) so a new branch surfaces in seconds" and now sits directly above `REFRESH_SPIN_FLOOR_MS = 500` at `:82`, which is neither a fork watch nor a cadence; `BRANCH_WATCH_INTERVAL_MS = 2_000` follows at `:83` with no comment of its own. Cut: move the new constant above `:77` or below `:84`.

- **[MINOR] `refresh()` restates its own constant's docblock and hardcodes its value in prose** - axis 4. `panel.ts:225-227` repeats `REFRESH_SPIN_FLOOR_MS`'s doc at `:79-81` almost word for word ("`_fetch` is filesystem-fast, so without a floor the spinner shows for a single frame and reads as 'nothing happened'") and then says "Hold it ~500 ms" beside the named constant that holds the 500. Cut those three lines. `:228-231` - why the floor is held under reduced motion - says something the constant's doc cannot and stays.

- **[MINOR] `ISession.colour`'s documentation has been outgrown by the server it describes** - axis 5. `src/core/types.ts:129-131` says the field carries the colour "for `colourSource: 'native'` providers" (and `:250-251` says the same of the probe response), but `SessionsHandler.get` now runs `_effective_colour` over every row of every provider (`routes.py:332-337`), so codex, kimi and gemini rows carry a value too. The only consumer is `terminals.ts:364-369`, which passes it to `effectiveColour(...)` as the `native` argument, read only when `source === 'native'` (`colour.ts:87-88`) - so for three of the four providers the value crosses the wire and is discarded. FACT, not a delete demand: either correct both doc comments or re-gate the server-side resolution. Leaving the two disagreeing is how the next reader gets it wrong.

- **[MINOR] (taste) `store.py:313` is the only line over 100 characters in the Python tree** - axis 4. 120 characters: "Unsupported by default: a store that cannot fork inherits this and the route answers 400 ``fork_unsupported``. A". `awk 'length>100'` over `core/*.py` and `providers/*.py` returns exactly this one hit; the rest of the file wraps at ~79. No Python formatter is configured (`pyproject.toml` has no ruff/black/line-length key, and `make test` runs `jlpm test` plus pytest only), so nothing will catch it. Rewrap.

- **[MINOR] `terminals.ts` keeps a changelog in a comment** - axis 4. `:381-385`: "five call sites had four different rejection disciplines between them, and the one that awaited it skipped its own refresh when a repaint threw" describes the shape of code that no longer exists; git owns that history. The sentences around it - why the pass swallows, and why a silent swallow is indistinguishable from a panel with no colours - are the why-comment and stay.

## What earns its place

- **The CSS**. All 52 `jp-AiAssistants*` selectors in `style/base.css` have a referent in `src/` or `ui-tests/tests/` - zero dead rules in a 900-line stylesheet for a panel that has been rewritten four times. The contrast comments added this round (`:182-184`, `:333-337`, `:399-402`) each carry a measured ratio, which is a why-comment no code can state.
- **`test_probing_an_unknown_terminal_creates_nothing`**. It kills its mutant: restoring `terminal_manager.get_terminal(terminal_name)` in `routes.py` reddens it with `assert 500 == 404`, and it does so even after the fake scaffolding I want cut. The 16-line comment above the lookup at `routes.py:802-816` earns its length - it explains why the attribute is read with no `getattr` default, which is the non-obvious half.
- **`colour_store._read`'s asymmetry docstring** (`:41-50`). It explains why a legacy file counts as holding nothing hand-set - that calling an inherited tint hand-set destroys a branch's colour with no undo, while the converse costs one re-pick. That is a one-way-door decision the code cannot say itself.
- **The `.gitignore` block for `tmp/` and `graphify-out/`** (`:137-143`). It states that both patterns are unanchored and therefore `ui-tests/tmp/` needs no line of its own - a comment that pre-empts exactly the re-addition round 3 removed. And the Galata deletion left no orphan: `AVAILABLE` in `ui-tests/tests/shared.ts:12` still has ten live uses in `panels.spec.ts`.
