I'll start by reading the adversary file and my round-1 report.

`[2026-08-09 Sun 16:40]`

VERDICT: DO-NOT-SHIP (7 new findings) - five of the six actionable round-1 findings are cleared, but the release affordance is keyed to the row's *current* conversation only, so the one-way door is still open for every branched conversation, and the Galata spec that certifies the release cannot see the colour it claims to restore.

## (a) Round-1 findings

- **1 [CRITICAL] override cannot be released** - **NOT-CLEARED (narrowed).** A release now exists (`src/core/panel.ts:1726-1735`, `_resetColour` at `src/core/panel.ts:920-927`) and it works for the conversation the row currently represents. It does not exist for any other conversation of that project - see NEW-1 - and the gesture that sets the colour still has no working release on its own surface - see NEW-2.
- **2 [MAJOR] failed write destroys the colour** - **CLEARED.** `ColourStore.set` returns a boolean and rolls the cache back (`src/core/colour.ts:173-201`); `reconcileColours` returns before `_applyColour` when `userSet && !held` (`src/core/terminals.ts:324-337`). A rejected POST now leaves both the tab and the companion's localStorage entry untouched, and the next pass retries.
- **3 [MAJOR] write-back attributed to a guessed conversation** - **CLEARED.** `observedId = info.session_id ?? null` and the write is gated on it (`src/core/terminals.ts:321,325-327`); the cwd fallback survives for painting only (`src/core/terminals.ts:303-306,339-353`).
- **4 [MAJOR] canonical docstrings contradict the code** - **CLEARED.** `jupyterlab_ai_code_assistants_extension/core/colour_store.py:3-8`, `.../core/routes.py:191-195`, `:446-449`, `:511-516` all now describe the ladder as shipped.
- **5 [MINOR] server-side branch inheritance skips native** - **CLEARED**, and by the exact fix proposed: `parent_default = None` for a native provider, one unconditional `inherit_colour` (`.../core/routes.py:549-557`).
- **6 [MINOR] the two new frontend rules untested at every tier** - **NOT-CLEARED.** `src/__tests__/` is untouched by the diff (`git diff --stat` lists no file there); there is still no test on `_inheritColour`'s native branch (`src/core/panel.ts:941-946`) nor on the reconcile guard (`src/core/terminals.ts:335-337`). The new Galata spec covers the release path only, and covers it against a null default (NEW-3).
- **7, 8, 10** - declined by the caller, not re-raised.
- **9 [MINOR] ui-test leaves the store dirty** - **CLEARED.** `test.afterEach` clears the seeded conversation (`ui-tests/tests/colour-override.spec.ts:80-85`).

## (b) New defects

- **[CRITICAL] The release is keyed to the row's pinned conversation, so a coloured branch tab is still a one-way door.** `isVisible: () => !!this._colours.get(active()?.session_id)` and `execute` both read only the row's own id (`src/core/panel.ts:1728-1733`). A row's `session_id` is the project's pinned-or-latest transcript (`jupyterlab_ai_code_assistants_extension/providers/claude.py:722-726`, chosen at `:562-609`), while `open-branch` launches a terminal for a *different* conversation without touching the pin (`src/core/panel.ts:1770-1781`). Colour that terminal's tab and the write lands under the branch's `info.session_id` (`src/core/terminals.ts:321,326`); the row then shows no "Reset Tab Colour", and the seeded fixture itself gives every project three conversations (`ui-tests/jupyter_server_test_config.py:111-116`). The only escape is to switch the project's current conversation to that branch, reset, and switch back - four steps that mutate which conversation the project resumes, which is not a release gesture any user will find. Fix: make visibility and reset cover the whole project - `isVisible` true when any conversation of the row (`_lastBranches` plus `session_id`) holds an entry, and reset the coloured one(s); or resolve the target from the focused terminal rather than the row.
- **[MAJOR] The release lives on a different widget from the gesture, and the gesture's own release still silently reverts.** The colour is set by right-clicking the *terminal tab* (companion menu, `node_modules/jupyterlab_colourful_tab_extension/lib/index.js:300-320`); the release is in the *sidebar row* menu. The item the user will reach for first is the companion's `Clear` (`.../index.js:322-340`), which strips the class and the localStorage entry, looks like it worked, and is then repainted from the store on the next reconcile (`src/core/terminals.ts:345-353`) up to 30 s later - a silent revert with no explanation, which is the exact symptom round 1 named. Nothing in `src/` contributes to `app.contextMenu` (grep: only `this._contextMenu`). Fix: register the same `reset-colour` command against the terminal-tab selector so it sits beside the colours in the menu that set them; the stated reason for not using Clear (this extension stops reading the companion's storage) does not block the alternative either - the release signal can be the tab's *class list*, which this extension can read, not the companion's storage.
- **[MAJOR] The spec certifies "back to the assistant's colour" with a null-vs-null assertion.** The seeded transcripts contain only `{"cwd": ...}` (`ui-tests/jupyter_server_test_config.py:113`), and Claude's default colour comes from an `agent-color` record (`.../providers/claude.py:187-190`, mapped at `:104-111`). So `assistants` is `null` in both tests: `expect(after.colour).not.toEqual(assistants)` (`ui-tests/tests/colour-override.spec.ts:98`) is `'rose' !== null`, and `expect((await seededSession(page)).colour).toEqual(assistants)` (`:143`) is `null === null` - a restatement of the store-empty poll two lines above, not evidence the assistant's colour returns. The acc-crit "Colour release affordance" is closed on that (`docs/acc-crit-jupyterlab-ai-code-assistants.md`, log line "restores the assistant's colour"). Fix: append `{"type": "agent-color", "agentColor": "blue"}` to the newest seeded jsonl so the row's default is `sky`, and both assertions become real.
- **[MINOR] The item's precondition is invisible on the surface that hosts it.** `_renderRow` paints an indicator, name, branch badge, star and time (`src/core/panel.ts:1297-1379`) - no colour anywhere. The row never shows that this conversation carries an override, so "Reset Tab Colour" appears and disappears with no perceivable cause, and during the up-to-30 s before capture the colour is on screen while the item is absent. Fix: render the effective colour as a small swatch on the row (the panel already has it via `ColourStore.get`); the item then reads as "reset *that*".
- **[MINOR] The item is offered while tab colouring is switched off.** `isVisible` consults the store only (`src/core/panel.ts:1728`), but the `colouredTabs` setting (`schema/plugin.json:23`, applied at `src/index.ts:173`) makes `reconcileColours` return immediately (`src/core/terminals.ts:296-300`). With tinting off the user is offered a reset for a colour nothing displays, and clicking it changes nothing visible. Fix: gate visibility on the coloured-tabs flag as well.
- **[MINOR] The reset failure is the only one-shot failure in the panel with no cause and no log.** `src/core/panel.ts:922` calls `_setInlineError('Could not reset the tab colour - try again.')` where every sibling failure uses `_showActionError` (`:562`, `:601`, `:833`), which appends the server message and writes to the console (`:513-517`). The banner is also cleared by the next successful poll (`:535`), so a user who looks away loses it entirely. Fix: have `set()` surface the error (or reuse `_showActionError` with the caught error) so the banner names what failed.
- **[MINOR] The visibility assertion races the colour load.** `isVisible` reads the in-memory cache, which `_fetch` populates in a fire-and-forget `load()` *after* `_render()` (`src/core/panel.ts:539`), while `openBranchyMenu` right-clicks as soon as the row is visible (`ui-tests/tests/colour-override.spec.ts:73-74`). A Lumino menu fixes its items at open time, so if the open wins the race the `toBeVisible()` at `:113-117` retries against a node that was never created and fails after the full timeout. Fix: wrap open-and-assert in `expect.poll` so the menu is reopened, rather than asserting on one open.

## Pinned questions, answered

- **Discoverable?** No - see NEW-2 and NEW-4. The set gesture and the release gesture are on different widgets, the surface hosting the release never shows the colour, and the affordance the user reaches for first appears to work and then reverts.
- **Right to hide it?** Hiding is correct in principle (conditional affordance beats a permanently disabled item), and it costs nothing *provided* it is absent only when it truly does not apply. It is not: NEW-1 hides it while an override exists, and NEW-5 shows it while nothing can apply. Fix those two and keep the hiding.
- **Placement?** Acceptable, not ideal. It ends the flat first group after two clipboard reads; a state-mutating appearance reset would read better in its own group after the separator. The group is already mixed (`toggle-favourite` sits with the copies), so this is taste, not a defect.
- **Stale state after reset?** None found. `_resetColour` reconciles and refetches before returning (`src/core/panel.ts:925-926`); the paint goes through `api.setColour` (`src/core/terminals.ts:382-387`), whose class change is picked up by the companion's mutation observer into `applyToolbarColour` (`.../index.js:341-375`), and terminals carry no `jp-toolbar` to strand. The manual reconcile does not double-arm the timer (`startColourLoop` returns early while a handle is pending, `src/core/terminals.ts:268-276`). On failure the cache rollback keeps both the item and the colour, which is the right recovery.

## What works

- The failed-write path is now correct end to end: optimistic cache, rollback on rejection, and a paint that is skipped rather than destructive (`src/core/colour.ts:173-201` with `src/core/terminals.ts:324-337`). The `held` flag reads exactly as "the store is holding what the tab shows".
- Keying the write on `info.session_id` while keeping the cwd fallback for painting is the precise split round 1 asked for, and the comment at `src/core/terminals.ts:313-317` says why in three lines.
- Both tiers now resolve the ladder identically, including branch inheritance (`.../core/routes.py:549-557` against `src/core/panel.ts:941-946`), with no provider named in core.
- The spec drives the menu item by pointer and asserts the hidden state as well as the visible one (`ui-tests/tests/colour-override.spec.ts:101-118`) - the right shape, undermined only by the null default it measures against.

VERDICT: DO-NOT-SHIP (7 new findings)