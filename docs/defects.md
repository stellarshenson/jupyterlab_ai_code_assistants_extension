# Defects - jupyterlab_ai_code_assistants_extension

`[ ]` open or ruled not fixed, `[x]` fixed. Dated notes under each track how it evolved.

## Server core

- [x] `DEF-2` **Kimi pins silently dropped by pin validation** - `core/state._is_pin` rejected any id containing `_`, so every Kimi pin (`session_<uuid>`) was silently dropped: a switch or a fork wrote nothing and the row fell back to recency; `jupyterlab_ai_code_assistants_extension/core/state.py`
  - 2026-08-07 found by the unit suite (`test_kimi_pin_outlives_recency`); charset widened to alphanumeric plus `-` and `_`, which cannot widen the traversal surface - `/`, `.` and NUL stay out, and every store re-validates the id before a path join
- [x] `DEF-5` **provider_disabled gating never fires - settings read as nested dict vs flat dotted keys** - HIGH; server reads settings[providers][id][enabled] as nested dict but schema/plugin.json and src/index.ts use flat dotted keys providers.<id>.enabled, so .get(providers) is always None and the gate always answers enabled; fix read flat dotted keys server-side; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-07 reported: found by Galata agent; Galata cannot assert it (settings mocked in memory), acc-crit pytest route-gating affected
  - 2026-08-07 fixed: core/routes.py reads the flat dotted key via the new routes.enabled_setting_key(provider_id) ('providers.<id>.enabled'), the shape schema/plugin.json declares and src/index.ts writes; an absent key still reads as enabled. Covered by test_routes.py test_the_gate_reads_the_key_the_schema_declares (reads schema/plugin.json rather than restating it) and test_a_flat_disable_key_gates_the_route; the disable fixture now builds flat keys
- [x] `DEF-11` **New-session pin clear never fires for an assistant that mints its own id** - MINOR; the launch route cleared a project's conversation pin only when the body carried `new_session_id`, which kimi and codex never send (`mintsNewSessionId: false`), so a new conversation stayed shadowed by the conversation the user had switched to and clicking the row resumed the old one; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-07 reported: reported by the bug-hunter adversary (round 1, MINOR)
  - 2026-08-07 fixed: the launch route clears the pin on ANY launch that opens no existing conversation (`session_id is None and fork_from is None`), which covers a caller-minted id and a CLI-minted one alike; a fork still writes its own pin. Covered by test_a_new_session_launch_clears_a_pin_without_minting_an_id and test_resuming_a_conversation_leaves_the_pin_alone in test_routes.py
- [x] `DEF-12` **Colour entries dropped for conversations whose deletion failed** - MINOR; the delete route dropped the stored colours of every REQUESTED id rather than of the ids actually removed, and the panel mirrored it - so a multi-select delete in which one conversation was refused cost that survivor its tab tint on the next reconcile; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-07 reported: reported by the bug-hunter adversary (round 1, MINOR); the architect's wire-contract finding (never-emitted IRemoveResponse.removed / IDisposalReport.failed_count / IForkResponse.forked_from) reconciles here - the removed-ids response is the one real shape
  - 2026-08-07 fixed: `SessionStore.delete_branches` now answers the ids it ACTUALLY disposed of instead of a count - all four stores build the list per item - and the route drops exactly those colours and echoes them as `removed_ids`. The panel's two mirrors (_cleanupParallel, _deleteBranches) forget the response's ids rather than the request's. The never-emitted wire fields went with it: IRemoveResponse.removed and IDisposalReport.failed_count are gone (IRemoveResponse is now an alias) and IForkResponse.forked_from too, so removed_ids is the one real shape
- [x] `DEF-14` **Trash failure silently escalates to a permanent delete** - MINOR; `core/store.dispose_path` fell back to `shutil.rmtree`/`unlink` on ANY send2trash failure, so on a filesystem with no usable trash (WSL2 /mnt/c, NFS, /tmp) a dialog that promised "moved to trash" destroyed the data permanently with no warning; `jupyterlab_ai_code_assistants_extension/core/store.py`
  - 2026-08-07 reported: reported by the bug-hunter adversary (round 1, MINOR); runtime-verified - send2trash raises TrashPermissionError under /tmp in this environment while succeeding under $HOME
  - 2026-08-07 fixed: `dispose_path` raises when a requested trash move fails instead of falling through to rmtree/unlink, so the item counts as NOT disposed of - it stays out of removed_ids, keeps its colour, and the panel reports the shortfall. send2trash is now declared in pyproject [project] dependencies rather than ridden in transitively through jupyter_server
- [x] `DEF-18` **Store-computed colour discarded and recomputed by a per-row glob** - MAJOR; the claude store emitted `color` from the transcript tail it had already read, the sessions route popped it and recomputed the same value through `default_colour`, which globs `*/<sid>.jsonl` across the whole projects root - once per row, per poll - and the American spelling survived inside the store after DEF-3 fixed it on the wire; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-07 reported: reported by the architect adversary (round 1, MAJOR)
  - 2026-08-07 fixed: the claude store emits `colour` (British) straight from the tail scan it already performs, and `_effective_colour` takes the row's own value as the native default instead of re-deriving it - the listing is O(N) with no glob per row. ClaudeStore.default_colour survives for ONE caller, the terminal probe, which resolves a conversation no row may represent: kept deliberately, once per probed terminal rather than once per row
- [x] `DEF-19` **Dead server surface: parallel-scope disposal, native-flag fork, zero-implementor seams** - MAJOR; no client ever sent a `scope` of `parallel` yet the route branch, `SessionStore.cleanup` and four implementations served it; `SERVER_MINTED_FORKS` carried `native-flag` although the frontend mints that id itself, so `ClaudeStore.fork` and the branch route's colour write for it were unreachable; and `rowBadges`, `menuItems`/`IMenuContribution`/`SHARED_ICONS`, `derivedColour`, `launchExtras`, `SessionStore.on_launched`, `Capabilities.has_remote_control` and the `routes.py` shim had no implementor or importer at all; `jupyterlab_ai_code_assistants_extension/core/store.py`
  - 2026-08-07 reported: reported by the architect adversary (round 1, MAJOR); every deletion grep-verified against the whole tree before removal
  - 2026-08-07 fixed: deleted after grep-verifying each has no user - the `scope` branch, `SessionStore.cleanup` and its four overrides plus their tests; `native-flag` from SERVER_MINTED_FORKS, ClaudeStore.fork and the registry comment that contradicted the client; `rowBadges`, `menuItems` with IMenuContribution, SHARED_ICONS and the shieldIcon that existed only for it, `derivedColour` (and its threading through colour.ts/terminals.ts), `launchExtras` (and TerminalManager's now-unused hooks option), `SessionStore.on_launched`, `Capabilities.has_remote_control` and the `routes.py` shim, whose docstring claimed an import path **init**.py never used
- [x] `DEF-23` **Whole-project delete drops the colours of conversations that survived it** - MINOR; the remove route read every id BEFORE the delete and dropped all their colours whenever remove() answered True, but codex disposes thread by thread through its CLI and answers True on PARTIAL success - so a refused thread lost its user-set tint server-side and client-side while still being there on the next poll; cause `SessionStore.remove` reported a bool, which cannot express a shortfall; fix `remove()` answers the ids it ACTUALLY disposed of and the route drops exactly those; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-08 reported: reported by the bug-hunter adversary (round 2, MINOR) - a fix-introduced regression: DEF-12 closed the same class for the per-branch delete shape and left it live in the whole-project shape
  - 2026-08-08 fixed: `SessionStore.remove` now answers `list[str] | None` - the ids actually disposed of, None on failure - so the route hands exactly those to `drop_colours` and echoes them as `removed_ids` instead of a pre-read list. Codex returns what `_dispose` already computed thread by thread; claude, kimi and gemini are one atomic directory disposal and answer all their known ids on success, none on failure, with `removed_count: 1` unchanged for every store. Covered by test_colour_store.py test_a_refused_thread_keeps_its_colour_when_the_project_goes, which drives the DELETE route with the codex CLI refusing one thread and asserts the survivor keeps its tint and stays out of removed_ids

- [x] `DEF-32` **Terminal probe route CREATES the terminal it fails to find, breaking `GET /api/terminals` for the whole server** - CRITICAL; `TerminalProbeHandler.get` calls `terminal_manager.get_terminal(name)`, which in both terminado's `NamedTermManager` and `jupyter_server_terminals.TerminalManager` is get-OR-CREATE, so a probe of a name the manager has never seen spawns a bare `/bin/bash` and registers it; `last_activity` is monkey-patched only inside `TerminalManager.create`, so the ghost has none and `TerminalManager.list()` raises `AttributeError` for EVERY terminal in the list, which is what `TerminalRootHandler.get` calls and what `@jupyterlab/services` polls every 10s - the Running sidebar's Terminals section then 500s for every client of the server until restart, and the orphan shell survives it; reached whenever a terminal widget outlives its pty (JupyterLab's `closeOnExit` off) or on the enumerate-to-probe race the frontend already anticipates; `terminal is None` at the line below is dead code that never fires; upstream's own websocket handler guards this exact call and its REST handler uses `get()`; `jupyterlab_ai_code_assistants_extension/core/routes.py`, reached from `src/core/terminals.ts`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 3, CRITICAL); runtime-proven - `tm.get_terminal("ghost-terminal-42")` returned a live `PtyWithClients` with a real pid and `tm.list()` then raised
  - 2026-08-10 fixed: the route reads `terminal_manager.terminals` - the registry terminado itself writes every created terminal into - so a miss is a miss and the existing 404 is live for the first time. Confirmed at the library source rather than only empirically: `NamedTermManager.get_terminal` is docstring'd "Get or create a terminal by name" and assigns `self.terminals[term_name]` on a miss. Read off the attribute with no `getattr` default on purpose - a `{}` fallback would turn a library shape change into every probe quietly answering 404, taking tab colours and session identification down silently. The frontend needs no change: `_probe` already maps a failed request to null, which is the same "not ours" it drew from the old `running: false`. Covered by test_probing_an_unknown_terminal_creates_nothing, which asserts 404 AND an unchanged registry; `_FakeTerminalManager.get_terminal` was made get-or-create so the fake reproduces the defect instead of hiding it. Mutation-checked - restoring `get_terminal` reddens the test with DID NOT RAISE
- [x] `DEF-33` **Launch resize races the init waiter and can exec the assistant into a 1x1 pty** - MAJOR; the launch route marks the new pty 1x1 with `ptyproc.setwinsize` so the init waiter can tell a real client attach from terminado's 24x80 default, but the waiter captures its own baseline and installs a SIGWINCH trap in a separate process, so the two orderings give opposite outcomes - if the child reads `24 80` before the server's resize lands, the trap fires on the server's OWN resize, the loop breaks on its first iteration and the assistant execs at 1 row by 1 column with no client attached at all; measured at 1-in-5 with no injected delay; the failure is exactly what the waiter exists to prevent; cause the sentinel is differential (has the size CHANGED) rather than absolute, and is written by a process other than the one reading it; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 3, MAJOR); a fix-introduced regression - the resize was added in round 2 to close the 5s launch stall, which it does in 19/20 trials
  - 2026-08-10 fixed: the sentinel moved into the child and became absolute. `_INIT_WAITER` now opens with `stty rows 1 cols 1` and polls until the size is no longer 1x1; the SIGWINCH trap and the captured baseline are gone, and so is the server-side `setwinsize` - marking and polling happen in one process in that order, so there is no interleaving left to lose rather than a narrower window. Measured over a delay sweep - both waiter forms driven against a real `ptyprocess` pty, a simulated client attaching at 1.5s on a timer, and the server's resize injected at a varying delay after `create` (the numbers below are the record; the harness was scratch, and `logs/` is `*.log`-ignored so no file in the tree holds them): the old design is BAD 10/10 at every delay from 3ms to 20ms between `create` and the resize - not a rare race but deterministic once the resize lands after the child's baseline read - while the new one is BAD 0/10 at every delay, 0/20 at no injected delay, execs 0.4s after a client attaches, and still falls back at 5.5s when no client ever connects. Deleting the resize block closed DEF-34 with it. Covered by test_a_launch_sizes_the_pty_from_inside_the_child, which asserts the child's own mark is in the launch script and that no WINCH trap remains; mutation-checked
- [x] `DEF-34` **`model["name"]` subscript sits above the dict-or-object defence, in an `except` that omits TypeError** - MINOR; the resize guard subscripts the model dict three lines above the block that states "some versions return an object with a `.name` attribute" and handles both shapes; on such a version the subscript raises `TypeError`, which the `(KeyError, AttributeError, OSError)` tuple does not catch, so the handler 500s AFTER `terminal_manager.create` has spawned the pty - orphaning a terminal, which is what the pre-flight checks above it exist to avoid, and contradicting the guard's own "never a failed launch" comment; does not fire on jupyter_server_terminals 0.5.4, which returns a dict; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 3, MINOR)
  - 2026-08-10 fixed: closed by the DEF-33 fix rather than separately - the whole resize block went with the race it caused, taking the subscript with it. The dict-or-object resolution of `terminal_name` below it is untouched and is now the only reader of the model

- [x] `DEF-50` **Kimi never recognises its own terminal - the CLI renames its process and the store matched the old name** - CRITICAL; `KimiStore` set `comm_name = "kimi"` and inherited exact comm equality, but the launched binary renames itself as it boots: sampling `/proc/<pid>/comm` every 0.5ms from exec gives `python` at 0.2ms, `kimi` at 5.4ms, `MainThread` at 10.6ms and `kimi-code` at 674.5ms, where it stays - so identity was true for a few milliseconds of a terminal's life and false for the rest of it; end to end under a pty launched exactly as the route does, `_tree_assistant` answered None for Kimi while identifying claude, codex and gemini from the same harness; blast radius is every Kimi terminal answering `running:false` / `session_id:null` / `colour:null`, `findForSession` never matching so every row click spawns a DUPLICATE terminal, and the colour loop returning early so Kimi tabs are never tinted; `jupyterlab_ai_code_assistants_extension/providers/kimi.py`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 5, CRITICAL), runtime-measured against the installed kimi 0.34.0; survived both skeptics. The same defect class as DEF-24's Gemini comm, in the one provider that had not been re-examined for it
  - 2026-08-10 fixed: `comm_name` is the settled `kimi-code` and `owns_pid` matches the candidate set `_KIMI_COMMS = {"kimi", "kimi-code"}`, so a probe during startup and a probe at steady state both identify it. No argv discriminator is needed here, unlike gemini: every spelling is the assistant naming itself rather than a shared interpreter

- [x] `DEF-63` **Server hardcodes JupyterLab's default user-settings path instead of the configured one it already holds** - MINOR; `Path(jupyter_config_dir()) / "lab" / "user-settings"` is the DEFAULT, while `user_settings_dir` is a configurable trait (`--LabApp.user_settings_dir`, `JUPYTERLAB_SETTINGS_DIR`) - so on a server that moved it the provider toggles are written somewhere the gate never looks and every provider reads as enabled whatever the user turned off; the identical hardcode sat in `core/migrate.py`, where it decides where the RETIRED extensions' settings are read from; `jupyterlab_ai_code_assistants_extension/core/routes.py`, `core/migrate.py`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 5, MINOR); survived both skeptics
  - 2026-08-10 fixed: `_user_settings_dir()` prefers the running server's own LabApp (`web_app.settings['lab'].user_settings_dir`, read per request so an extension loaded later is still seen) and keeps the default as the fallback, so the bare `jupyter server` the tests run against behaves exactly as before. Proven against a real server initialised with `--LabApp.user_settings_dir=<tmp>`. `migrate.py` carried the same hardcode and was closed with it, but by parameter rather than by import - `routes` already imports `migrate`, so the reverse would be circular; the route knows the running app and passes the folder down
- [x] `DEF-66` **`colour_store.py` is the one core module with a failure path and no logger** - MINOR; `_save` swallowed `OSError` and returned False, so a full disk or a permissions change lost hand-set tab colours silently; `jupyterlab_ai_code_assistants_extension/core/colour_store.py`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 4, MINOR)
  - 2026-08-10 fixed: `_log = logging.getLogger(__name__)` in the same shape as `core/migrate.py` and `core/registry.py`, and the swallowed `OSError` logs a warning naming the provider, the file and the errno. Mutation-checked by restoring the bare `except OSError: return False`, which reddens the new assertion
  - 2026-08-10 premise corrected: the TITLE is false and the fix was closed on it. `colour_store.py` was NOT the one such module - `core/state.py` has two swallowed `OSError`s (`:136`, `:153`) and no logger at all. Found by the architect adversary in round 7, after the "a defect already in the ledger is not a finding" rule was withdrawn mid-run; under that rule this correction was unreportable. Carried forward as `DEF-90`
- [x] `DEF-67` **`default_colour`'s docstring advertises a `colour_source` precedence the core stopped implementing** - MINOR; the docstring described a rule where a `native` provider's own colour beat the extension's store, which `routes._effective_colour` has not done since DEF-18 - it is `stored.get(sid) or default` for every provider alike; `jupyterlab_ai_code_assistants_extension/core/store.py`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 4, MINOR)
  - 2026-08-10 fixed: the docstring was corrected to the code rather than the code to the docstring, because two existing tests pin the actual behaviour deliberately - `test_a_hand_set_colour_beats_the_assistants_own` and `test_user_set_colour_beats_the_derived_hash`

- [x] `DEF-82` **`MigrateHandler.post` is the one route that does its work on the IOLoop** - MINOR; every other handler in `routes.py` hands its filesystem work to `run_in_executor` (seven call sites), while `MigrateHandler.post` calls `migrate.migrate` synchronously and blocks the event loop for its duration; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-10 reported: found by the request-ceiling impact agent while enumerating every route whose work scales with N, NOT by a round-6 adversary - this entry first credited the architect, and a methodologist audit of the run disproved it: no `logs/adversarial/wholerepo-*round6.txt` mentions `MigrateHandler`, `IOLoop` or `run_in_executor`. Confirmed as an inconsistency rather than a hazard - the work is O(4 providers) of small JSON reads with no subprocess and no network, so it is not a timeout risk and has no observable symptom
  - 2026-08-10 deferred: logged for consistency's sake alone. Nothing measurable changes if it is fixed, and the change would touch the one route whose whole job is a one-shot migration of the retired extensions' state - the surface with the least test coverage and the least reason to be edited in a wave whose approved scope is two lines
  - 2026-08-10 reason corrected: "the ONE route that does its work on the IOLoop" is false. The seven `run_in_executor` sites are real but sit in five async bodies, while SIX synchronous handlers touch the filesystem on the IOLoop - `StatusHandler.get:323`, `FavouriteHandler.post:506`, `ColoursHandler.get:555`/`post:562`/`delete:591`, `TerminalHandler.get:851`. Found by the architect adversary in round 7. The deferral still stands; the sentence justifying it did not, and this entry had its provenance corrected earlier the same day - two false statements in one entry, both written by the agent that logged it
  - 2026-08-10 fixed: all six converted to `async def` with the fs work in `run_in_executor`, the file's own idiom - `StatusHandler.get` (roster hoisted into a local), `FavouriteHandler.post`, the three `ColoursHandler` verbs (the shared `_finish_store` made async, smaller than wrapping `load_store` at three sites), `TerminalHandler.get` (probe hoisted); TerminalHandler's 'Deliberately synchronous' docstring paragraph corrected, the change itself having made it false. The entry's own TITLE route closed too: `MigrateHandler.post` converted in the same idiom immediately after the wave (`routes.py:962`), so IOLoop discipline is uniform across all thirteen handler methods. Residuals recorded, not restructured: `_ProviderHandler.resolve` still reads settings and stats PATH on the loop for all eleven provider routes - changing it restructures the seven already-async methods too; and two coverage gaps (StatusHandler's `enabled` value, FavouriteHandler's response body survive mutation) are proven PRE-EXISTING against the pre-wave file. pytest 169 on the settled tree
  - 2026-08-10 corrected (round 8): 'uniform across all thirteen handler methods' was true of the signatures, not the work, and the residual list was incomplete - `state.write_pin`/`clear_pin` and `colour_store.inherit_colour` still ran on the loop inside async bodies. Round 8 then proved that asymmetry was one side of a lost-update race (`DEF-99`), and the remedy INVERTED the story: the four mutating executor wraps (toggle_favourite, set_colour, drop_colours, migrate) were reverted, so the file tells one story - READS in the executor, store WRITES loop-owned, the loop being the lock. The comment at each write site says so
- [x] `DEF-99` **The `DEF-82` executor conversion de-serialised the state and colour writers - a lost favourite or pin, reproduced 200/200** - MAJOR; moving `toggle_favourite`, `set_colour`, `drop_colours` and `migrate.migrate` onto the default thread pool while `write_pin`/`clear_pin`/`inherit_colour` stayed on the IOLoop made writer-vs-writer interleaving possible for the first time: both stores are an unlocked load-modify-save of one JSON file, and `write_json_atomic` prevents torn files, not lost updates. Pre-wave every writer ran on the single loop thread, serialised by construction. Two threads toggling different projects lose one favourite in 200 of 200 trials; the user-visible shape is a star or current-conversation pin vanishing silently on the next poll; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-10 reported: found independently by BOTH round-8 adversaries over the close-19 wave - architect by mechanism, bug-hunter with the 200/200 reproduction - as the wave's only new correctness hazard, introduced by a conversion whose own defect was consistency-only with no observable symptom
  - 2026-08-10 fixed by REMOVAL: the four mutating wraps reverted to loop-thread calls, each carrying a comment naming the rule - store WRITES are loop-serialised, only READS use the executor. Chosen over a lock or a single-worker executor because it deletes code instead of adding the campaign's next mechanism: the loop was already the lock both stores were designed against
  - 2026-08-10 corrected (round 9): two sentences above are FALSE and both round-9 adversaries proved it - `ClaudeStore.switch` has written a pin from the executor since before this wave (`claude.py:872` under the `routes.py:504` wrap, present at `ad1402a`), so 'pre-wave every writer ran on the single loop thread' and 'possible for the first time' overstate: the wave WIDENED a pre-existing executor-writer surface rather than creating it. The residual writer is closed by `DEF-101`, after which the loop-owns-writes story is true for the first time

- [x] `DEF-126` **`pid_alive` raises `OverflowError` on an out-of-range pid, 500ing the sessions listing for every project** - MAJOR, found by the round-1 adversarial review (architect and bug-hunter independently, against a change that was reverted); `jupyterlab_ai_code_assistants_extension/core/store.py`
  - 2026-08-26 reported: `os.kill(pid, 0)` answers an int outside C `pid_t` with `OverflowError`, which is NOT a subclass of `OSError` - so it passed straight through `pid_alive`'s own `except OSError` and out of every caller. All three call sites are in `providers/claude.py` (`_session_state_by_cwd` at the listing poll, `bg_agents`), and each reads its pid out of a record the `claude` CLI wrote, so the value is another program's to choose. Reproduced: `pid_alive(2**63)` raises, and one such record makes `_session_state_by_cwd` raise out of the executor, 500ing the sessions poll for EVERY project on every 30 s tick with nothing naming the file. Exactly the shape of the `updatedAt` ISO-string fault that `test_a_malformed_number_costs_a_row_a_field_not_the_whole_listing` already pins, one field over. Latent rather than firing: measured across the 3030 records in the live store, 0 carry a non-int pid and 0 an out-of-range one, so the trigger is a future or foreign CLI writing one - which is precisely how the `updatedAt` case arrived
  - 2026-08-26 fixed: `except (OSError, OverflowError): return False` - a pid no process can hold reads dead rather than raising. One line at the origin, chosen over guarding the individual call sites because all three want that answer and patching them one at a time leaves the cause live. Covered by `test_an_out_of_range_pid_costs_a_row_a_field_not_the_whole_listing`, which asserts the listing still returns its row AND that the unit answers False; severing the fix reddens exactly that test and nothing else (verified by mutation against a hash-checked snapshot). pytest 171
  - 2026-08-26 note: independent of the change the review was aimed at. That change (an auto-sweep of stale `kind: "bg"` session records) was reverted after its causal premise was experimentally refuted - see the journal - and this defect predates it, shipping in 1.0.30 and every earlier version

## Providers

- [x] `DEF-1` **Codex declares a fork it cannot serve** - the Codex descriptor declares `fork_strategy: "native"` but `CodexStore` does not override `fork`, so `POST providers/codex/branch` answers 400 `fork_failed`; the CLI mints the fork id itself (`codex fork <parent>`) and the core has no launch path carrying `fork_from`, so branching Codex is unreachable server-side; `jupyterlab_ai_code_assistants_extension/providers/codex.py`
  - 2026-08-07 recorded from `test_capability_flags_match_the_store[codex]`, which xfails against this entry rather than asserting the broken state; the fix is a core change (a fork-from launch payload plus the frontend's discovery watcher), not a provider one
  - 2026-08-07 fixed: core launch path now carries fork_from: LaunchHandler validates it (mutually exclusive with the other three ids, 400 invalid_fork_from) and passes it to store.launch_argv, whose contract gained the kwarg; CodexStore already built [codex, fork, <parent>] from it. The Python fork_strategy vocabulary was aligned with the frontend descriptor's - native-flag / native-command / server-copy / none - and the branch route now serves only registry.SERVER_MINTED_FORKS, so codex answers 400 fork_unsupported instead of 400 fork_failed. Frontend discovery watcher was already ported (panel._branchByNativeCommand plus _watchForNewBranch, 2s x 90). xfail in test_capability_flags_match_the_store removed and replaced by a real assertion per strategy, plus test_every_store_accepts_the_whole_launch_contract and test_routes.py test_a_native_command_fork_launches_with_fork_from
- [x] `DEF-8` **gemini resume uses unsupported --resume <uuid> form** - HIGH; gemini --resume accepts latest or a positional index, not a uuid, so a Gemini resume can open the wrong conversation; fix resume via --session-file <chat path> with the store mapping session id to its chat JSON, per acc-crit Gemini Resume criterion; `jupyterlab_ai_code_assistants_extension/providers/gemini.py`
  - 2026-08-07 reported: found by pre-fix conformance review; research doc records --session-file as the id-stable form
  - 2026-08-07 fixed: GeminiStore.launch_argv resumes with --session-file <chat file path>: the new chat_file(session_id) maps a conversation id onto its file across the registry's projects, and a session whose file is gone degrades to a bare launch rather than opening whichever conversation an index would have landed on. parse_session_id reads the id back out of the file, since the filename holds only its first eight characters and the reuse ladder matches on the whole one. Module docstring corrected - it claimed --resume <uuid> matched stored ids, which the CLI's own help contradicts (latest or a positional index only). Covered by test_gemini_launch_argv_resumes_by_chat_file_never_by_resume and test_gemini_parse_session_id_reads_the_chat_file
- [x] `DEF-10` **Gemini claims every Node process as a running conversation** - HIGH; gemini's comm_name is `node-MainThread`, which /proc reports for EVERY node process, and `_tree_assistant` matched on comm alone - so a terminal running `npm run dev` in a gemini-registered project answered `running: true`, the colour loop tinted it, and the tab colour the user set on it was written into gemini's colour store under a cwd-guessed session id; `jupyterlab_ai_code_assistants_extension/providers/gemini.py`
  - 2026-08-07 reported: reported by the bug-hunter adversary (round 1, MAJOR); runtime-verified - a plain `node -e` and `gemini --help` report the identical comm on this machine
  - 2026-08-07 fixed: identity is now a per-store `owns_pid(pid)` on the SessionStore contract - the default is the comm match every native binary is exact under, and GeminiStore overrides it to ALSO require `gemini` in /proc/<pid>/cmdline. `_tree_assistant` asks each enabled store rather than matching a comm map, so the probe, the 30s colour loop and the tab-colour write-back all stop claiming unrelated node processes. Covered by test_gemini_claims_only_a_node_process_that_is_actually_gemini (a vite/npm argv is refused, the gemini bundle path is claimed, an unreadable cmdline is refused) and test_a_native_binary_store_claims_a_pid_by_comm_alone
- [x] `DEF-15` **Claude listing stat race 500s the whole sessions poll** - MINOR; the claude store sorted and stamped transcripts with an unguarded `Path.stat()`, so a transcript deleted between the glob and the stat (a second browser tab, or Claude's own housekeeping) raised FileNotFoundError out of the executor and the sessions poll answered 500; kimi and gemini already guarded the same read; `jupyterlab_ai_code_assistants_extension/providers/claude.py`
  - 2026-08-07 reported: reported by the bug-hunter adversary (round 1, MINOR)
  - 2026-08-07 fixed: a module-level `_mtime()` returns 0.0 on OSError and every per-file stat in the claude store goes through it (the two recency sorts, the row mtime, the branch mtime), matching the tolerant pattern kimi and gemini already used - a transcript deleted mid-poll now sorts last instead of 500ing the listing
- [x] `DEF-20` **Four stores, three disposal disciplines, and duplicated helpers** - MINOR; kimi and claude called `dispose_path` unwrapped, so one unwritable file raised a 500 mid-loop leaving a partial deletion with no colour cleanup and no index prune, while gemini counted per item and codex logged each refusal; claude/kimi/gemini had no logger at all; and the flag-value parser (x3), `_git_branch` (x2), `_now_iso_z` (x2) and `_process_comm` (x2) were copied between modules; `jupyterlab_ai_code_assistants_extension/providers/kimi.py`
  - 2026-08-07 reported: reported by the architect adversary (round 1, MAJOR + MINOR helper duplication)
  - 2026-08-07 fixed: every store disposes per item inside try/except OSError, logs the refusal through a module `logging.getLogger(__name__)` (added to claude, kimi and gemini) and leaves the failed id out of the answer; kimi's project-wide remove is wrapped too. The duplicated helpers moved into core/store.py with their signatures intact - `flag_value`, `cmdline_args`, `git_branch`, `now_iso_z`, `process_comm`, `process_cmdline` - and claude, kimi, gemini and codex import them
- [x] `DEF-21` **Gemini reads an unconfined path advertised on /proc, plus dead store knobs** - MINOR; `GeminiStore.parse_session_id` JSON-parsed whatever `--session-file` path a process advertised without confining it under the store's own tmp root, `GeminiStore.__init__(root)` was a knob with zero callers, kimi's mode token was a bare literal at two sites and codex baked `codex_home()` into its LegacySource at import time; `jupyterlab_ai_code_assistants_extension/providers/gemini.py`
  - 2026-08-07 reported: reported by the architect adversary (round 1, MINOR)
  - 2026-08-07 fixed: `_confined_chat_file` resolves a /proc-advertised --session-file path and refuses anything outside the store's own tmp root before read_chat_meta touches it; GeminiStore's dead `__init__(root)` knob is gone (HOME is the one override, as the CLI does it) while kimi's test-used twin stays; kimi's `yoloMode` is a named YOLO_MODE constant at both sites plus its legacy settings map; codex's LegacySource.state_file is the home-relative literal `~/.codex/...`, expanded at use like kimi's and claude's
- [x] `DEF-24` **Gemini claims any node process with gemini anywhere in its argv** - MINOR; `GeminiStore.owns_pid` confirmed the argv with a bare substring test (`b"gemini" in cmdline`), so a dev server started in a project folder named `my-gemini-app` was claimed as a running conversation - runtime-proven, `node /tmp/my-gemini-app/server.js` answered owns_pid True - reviving the mis-tint and cwd-guessed colour write-back DEF-10 narrowed; cause the substring matched the project path, not the CLI; fix match an argv ELEMENT whose basename is `gemini` or `gemini.js`, the CLI binary and the bundle behind it; `jupyterlab_ai_code_assistants_extension/providers/gemini.py`
  - 2026-08-08 reported: reported by the bug-hunter adversary (round 2, MINOR); the residual of DEF-10, whose argv confirmation was too loose
  - 2026-08-08 fixed: `GeminiStore.owns_pid` splits the cmdline into arguments and claims the pid only when one argument's basename is `gemini` or `gemini.js`, so a project path carrying the word no longer matches. Existing test extended - the installed bundle path still claims, an unreadable cmdline and a vite argv still do not, and `node /home/lab/projects/my-gemini-app/server.js` is now refused

- [x] `DEF-64` **Gemini Resume silently starts a NEW conversation when the chats directory is gone** - MINOR; `GeminiStore.launch_argv` degraded to a bare `gemini` invocation when a resume's chat file could not be resolved, so the user asked to reopen a conversation and got a fresh one that looked like it; the launch pre-flight cannot catch it because the argv is well-formed; `jupyterlab_ai_code_assistants_extension/providers/gemini.py`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 5, MINOR); survived both skeptics
  - 2026-08-10 fixed: a new `SessionNotFound` on the `launch_argv` contract in `core/store.py`, raised by `GeminiStore.launch_argv` when the chat file cannot be resolved, so the route answers the 404 the panel already renders. Deliberately an exception rather than an empty argv: a falsy argv already means `launch_unsupported`, and overloading it would make two different refusals indistinguishable at the route. Covered by `tests/test_launch_missing_session.py`, which drives the live `jupyter_server` with the chats directory removed and asserts HTTP 404 `session_not_found` and that no terminal was created
- [x] `DEF-65` **`codex.py` documents `_THREAD_COLUMNS` by naming a function that does not exist** - MINOR; the comment read "Columns `_list_threads` needs" and no `_list_threads` exists in the tree; the real consumer is `_threads_from_db`; `jupyterlab_ai_code_assistants_extension/providers/codex.py`
  - 2026-08-10 reported: reported by the slop-hunter adversary (whole-repo round 5, MINOR)
  - 2026-08-10 fixed: the comment names `_threads_from_db`. Verified by grep - `_list_threads` appears nowhere in `jupyterlab_ai_code_assistants_extension/` or `src/`

- [x] `DEF-127` **`claude -c` refuses a compacted conversation, so the panel's primary is unreachable from the terminal** - MAJOR, reported by the user against `jupyterlab_export_markdown_extension`; `jupyterlab_ai_code_assistants_extension/providers/claude.py`
  - 2026-08-26 reported: `claude -c` walks a conversation to its chain root and refuses one rooted at a `system`/`compact_boundary` record - the shape every conversation takes once it has been compacted - then falls back to the next-newest transcript. A project whose only conversation is compacted therefore answers "No conversation found to continue" against a transcript that is intact, unheld and resumable by id, which is why the panel opens it while the user's terminal cannot. Measured on CLI 2.1.246 in both directions on isolated clones with no live process and no index history: removing the boundary from a refused transcript makes it ACCEPTED, splicing one into an accepted transcript makes it REFUSED, and a known-good transcript cloned by the same method is ACCEPTED (the control that rules out the method). Three further facts settled the design - `-c` orders candidates by mtime, it DOES fall back past an unusable one rather than stopping at the newest, and Claude's own `--fork-session` does not help because the fork inherits the boundary root and is refused too. 9 of 112 local projects were in this state, including `kolomolo/sales/proposals`, whose `-c` failures the `my-claude` skill had attributed to held branches
  - 2026-08-26 fixed: `ensure_continuable` gives such a transcript a root `-c` accepts by ADDING a synthetic `isMeta` user turn and re-pointing the boundary at it, so the boundary's `compactMetadata` and `logicalParentUuid` survive - no record is removed. `make_continuable` wraps it with the id gate, repairs every encoded directory holding that id (a renamed project leaves the same conversation under two), and is total by construction because its callers are a switch and a launch, neither of which may fail over a transcript's shape. Wired at both, per the user's choice: `switch` repairs BEFORE its existing mtime touch, so being newest and being selectable land together; `launch_argv` repairs whatever `session_id` it is opening. Rewrites are gated twice - skipped while any live pid records itself as serving that conversation, and aborted if size or mtime moved while the temp file was being written - because transcripts are append-only and a rewrite racing an append drops the appended turns. Idempotent, declines any transcript it cannot parse whole, and preserves mode 0600. Covered by seven tests whose seven mutants each redden exactly one and nothing else, including both call sites. End-to-end against the real CLI on a clone of the reporting user's own transcript: REFUSED before, opens after, second call a no-op. pytest 178
  - 2026-08-26 note: this writes into Claude Code's own transcript, which is the class of action `DEF-126`'s review condemned. It is taken deliberately and on the user's instruction, and it differs on the three points that made the earlier attempt wrong - the mechanism is measured rather than inferred, the change is additive rather than destructive, and the goal is verified end to end. The upstream defect is Claude Code's; if a later release makes `-c` accept a boundary root, this becomes a permanent no-op rather than a hazard

## Frontend / server contract

- [x] `DEF-3` **frontend and server disagree on three route shapes** - the panel cannot talk to this server as built: `ColourStore` calls GET/POST/DELETE `providers/<id>/colours` while the server registers POST `providers/<id>/colour` only; `ILaunchRequest` sends `modes` as a record while `LaunchHandler` reads a single `mode`; `IStatusResponse.delete_to_trash` and `ITerminalProbeResponse.running` are never sent by `StatusHandler` / `TerminalCwdHandler`; `src/core/colour.ts`
  - 2026-08-07 found while writing the unit tiers, which test each side against its own contract; the crossing is integration territory and belongs to the Galata tier plus a core fix, so no unit test asserts either shape today
  - 2026-08-07 fixed: server now serves the shapes the panel calls. Colour store is GET/POST/DELETE providers/<id>/colours (ColoursHandler), each verb answering the whole store so the panel's cache reconciles from any of them. Launch payload settled on the acc-crit's singular mode token - panel._launchMode() sends one token ('<id>' for a boolean mode, '<id>=<value>' for an enum), replacing the modes record on ILaunchRequest, TerminalManager.openSession and every launch site. StatusHandler emits delete_to_trash. Two further crossings found and fixed with them: the terminal probe is now the provider-scoped providers/<id>/terminal/<name> the panel actually calls (was global terminal-cwd/<name>) and emits running, and the wire spelling is British 'colour' throughout (rows and the probe sent 'color' while ISession/ITerminalProbeResponse read 'colour'). Covered by test_routes.py test_the_colour_store_serves_all_three_verbs, test_colours_are_per_provider, test_a_native_colour_provider_refuses_a_write, test_status_reports_the_move_to_trash_preference
- [x] `DEF-9` **launch body never carries encoded_path so fork pin and preflight are unreachable** - HIGH; core/routes.py:696-702 pins a native-flag fork (and clears the pin for a new session) only when the launch body carries encoded_path, and the session_not_found preflight at :650-656 has the same guard - but ILaunchRequest has no such field and no call site sends it, so a Claude fork never becomes the row's current conversation and the 404 handling at panel.ts:714-718 is dead; fix add encoded_path to ILaunchRequest and send it from every launch site that has the session in hand; `src/core/types.ts`
  - 2026-08-07 reported: found by the feature-parity audit (Claude MISSING row); only forkStrategy native-flag affected - codex pins via switch, kimi via branch
  - 2026-08-07 fixed: encoded_path added to ILaunchRequest and sent from openSession fresh-launch, _branchByNativeFlag and _newSession (existing-row lookup); strict-null guard on _sessions; make install + 127 pytest + 59 Jest green at v0.1.9
- [x] `DEF-16` **Client colour inheritance calls a route the server categorically refuses** - MINOR; a native-flag fork called `ColourStore.inherit`, which POSTs `providers/<id>/colours` - a route that always answers 400 `colour_owned_by_assistant` for a `native` provider - so every claude fork made one guaranteed-failing request and showed the parent's tint until the next reconcile dropped it; `src/core/panel.ts`
  - 2026-08-07 reported: reported by the bug-hunter adversary (round 1, MINOR); the same unreachable write existed server-side in BranchHandler's inherit_colour
  - 2026-08-07 fixed: the panel's three fork sites go through `_inheritColour`, which returns early for `colourSource: 'native'` - no request, no flicker. The server-side twin is gated too: BranchHandler skips inherit_colour for a native provider, so the only writer that could bypass ColoursHandler's refusal no longer does
- [x] `DEF-17` **Core asks the branches route for decorations under the wrong parameter name** - MAJOR; the panel sent `include_bg=1` for a provider declaring `hasBgAgents` while the server reads only `include_extras`; it worked solely because claude's `branchQuery` hook independently sent the right name, so any future bg-agents provider relying on the descriptor alone would silently get no markers; `src/core/panel.ts`
  - 2026-08-07 reported: reported by the architect adversary (round 1, MAJOR) and confirmed by the bug-hunter's wire tracing
  - 2026-08-07 fixed: the panel sends `include_extras=1`, the name the server actually reads. With that, claude's `branchQuery` hook had nothing left to contribute, so the hook and the `branchQuery` seam on IProviderHooks were deleted - the descriptor's hasBgAgents flag alone now drives the decoration

- [x] `DEF-78` **Three DELETE call sites scale with N and no client timeout constant can cover them** - MINOR; `DEF-72` bound every request at a fixed 60s, but `_removeProject` (`src/core/panel.ts:708`), `_cleanupParallel` (`:782`) and `_deleteBranches` (`:1252`) each issue one DELETE whose server work grows with the project's conversation count. Only Codex shells out per conversation - `CodexStore._dispose`, one subprocess per thread at `_CLI_TIMEOUT_S = 30` (`providers/codex.py:79`) - while claude, gemini and kimi do pure filesystem work; at the measured ~0.7s per CLI invocation a breach of the 60s ceiling needs 86 conversations in one project, and at ~70ms it needs 858; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 6) as a MAJOR naming two call sites, and rescoped by its impact agent - there are three, and the severity does not hold. A breach costs a spurious toast, not data: the tornado executor thread runs to completion regardless of the client aborting, `colour_store.drop_colours` still executes, and a retry is idempotent. Logged now because `DEF-72`'s closing note acknowledged this residual in prose and never gave it an id
  - 2026-08-10 deferred: a constant cannot bound an N-scaling route, so the fix is a different shape entirely - a per-route bound, or a delete that reports progress instead of blocking on one request - and neither is worth building against a symptom whose worst outcome is a toast on a project with 86 Codex conversations
  - 2026-08-10 closed by ruling (Star Colonel's direction, close-19 campaign): accepted residual. A breach needs 86 Codex conversations in one project at the measured ~0.7s per CLI invocation (858 at the fast path) and costs a spurious toast, never data - the executor thread completes, `drop_colours` still runs, retry is idempotent. No constant can bound N-scaling work, and a progress-reporting delete is machinery priced against a toast

## Packaging

- [x] `DEF-4` **extension docks no panels without jupyterlab-colourful-tab-extension installed** - HIGH; IColourfulTabs is an optional token in src/index.ts yet plugin load fails outright when the extension is absent; cause package.json declares sharedPackages colourful-tab {bundled:false, singleton:true} so the federated module must come from an installed extension; fix decide bundle vs hard runtime dependency (base extension auto-installed it as a pip dependency); `package.json`
  - 2026-08-07 reported: found by Galata first run 15/16 red; test venv installs it as workaround, documented in ui-tests/README.md
  - 2026-08-07 decision (Star Colonel): hard pip dependency on jupyterlab_colourful_tab_extension, same approach as the base extension - not bundling
  - 2026-08-07 fixed: pyproject.toml [project] dependencies now carries jupyterlab_colourful_tab_extension>=1.0.19 with the federation rationale, mirroring the base extension; package.json keeps ^1.0.19 and the non-bundled singleton sharedPackages entry unchanged. make install pulls jupyterlab-colourful-tab-extension-1.1.16 as a dependency
- [x] `DEF-22` **Template leftovers, unguarded barrel drift and a duplicate chalk copy** - MINOR; the scaffold's `1+1` spec and the `@types/react`, `@types/react-addons-linked-state-mixin` and `yjs` devDependencies survived with no React or Yjs in `src/`; `generate-schema.mjs` globbed `lib/providers` without checking the barrel, so a module dropped from `providers/index.ts` still got a settings toggle; and node_modules held two chalk copies (2.4.2 and 4.1.2), the documented duplicate-package landmine in the `@jupyterlab/builder` isolated-build path; `package.json`
  - 2026-08-07 reported: reported by the architect adversary (round 1, MINOR) plus the jupyterlab-extension release checklist
  - 2026-08-07 fixed: the scaffold spec and the @types/react, @types/react-addons-linked-state-mixin and yjs devDependencies are gone (the lib0 and webpack resolutions stay - the build is green with them); generate-schema.mjs now asserts its module count equals the barrel's export count and throws on drift; package.json pins chalk 4.1.2 in resolutions and gains an npm overrides block ({webpack, chalk}), leaving exactly one chalk copy in node_modules and an isolated `python -m build` at exit 0

## Retirement and migration

- [x] `DEF-6` **migration endpoint exists but nothing ever calls it** - HIGH; POST migrate is implemented and unit-tested idempotent, but no code in src/ invokes it, so a user upgrading from the standalone extensions silently loses favourites and settings; fix call migrate once from activation before first status render; `src/index.ts`
  - 2026-08-07 reported: found by pre-fix conformance review
  - 2026-08-07 fixed: src/index.ts calls POST migrate exactly once at activation, after the settings registry has loaded and before the first reconcile docks a panel, and applies the mapped keys the server hands back through settings.set (the server has no writable handle on the registry). Idempotent server-side, so the per-activation call migrates nothing the second time; any failure is a console.warn and activation continues
- [x] `DEF-7` **no duplicate-panel conflict detection** - HIGH; a still-installed standalone extension (claude/codex/kimi) yields two panels and two command sets per assistant; fix app.hasPlugin('<standalone>:plugin') check at activation suppresses this extension's panel for that assistant and shows a one-time uninstall notice, per acc-crit conflict criteria; `src/index.ts`
  - 2026-08-07 reported: found by pre-fix conformance review
  - 2026-08-07 fixed: the retired standalone's plugin id is descriptor data (IProviderDescriptor.legacyPluginId, set on claude/codex/kimi, absent on gemini), so no core file names an assistant. At activation src/index.ts checks app.hasPlugin('<legacyPluginId>:plugin') and adds the hit to a suppressed set: reconcile skips those ids entirely, so the provider is never started - no panel, no polling, no registered command - and a single Notification.warning names the package(s) to uninstall, once per page load rather than on every refresh
- [x] `DEF-13` **Migration marker commits before the settings half can land** - MINOR; `POST migrate` marks a provider migrated in the same call that returns its mapped settings keys, but those keys are applied client-side through the settings registry - so a first activation whose `settingRegistry.load` failed migrated the favourites, dropped the settings, and the marker blocked every retry; `src/index.ts`
  - 2026-08-07 reported: reported by the bug-hunter adversary (round 1, MINOR)
  - 2026-08-07 fixed: src/index.ts returns from migrate() when the settings registry is unavailable (settings null), so the server-side marker can never commit before the settings half has somewhere to land; the next activation retries the whole migration

## Panel and settings UX

- [x] `DEF-25` **Gemini approval-mode settings copy states the inverse of the implemented precedence** - MINOR; the descriptor's Approval mode description said the ladder applies "when no boolean launch mode overrides it", the rule `resolveLaunchMode` had just replaced - a non-default enum now beats the YOLO switch - so a user who set `approvalMode: plan`, read the copy and turned YOLO on launched read-only against what Settings told them; cause the precedence fix left its own documentation behind; fix reword the description to the implemented rule (schema/plugin.json is generated from it); `src/providers/gemini.ts`
  - 2026-08-08 reported: reported by the ux-designer adversary (round 2, MINOR), clause shape independently endorsed by the architect; a fix-introduced regression in a touched file
  - 2026-08-08 fixed: reworded to the implemented rule: passed when set to a value other than the default, a non-default value takes precedence over the YOLO switch, and only the forced YOLO menu action overrides it. Edited on the TS descriptor only - schema/plugin.json is generated by scripts/generate-schema.mjs in the build chain and carries the new wording at v0.1.14
- [x] `DEF-26` **Popup switch rows are focusable but semantically anonymous** - MINOR; the branch-popup rows took `tabIndex` and Enter-to-switch but no `role`, so a screen reader announced a generic div with no role and no action, and Space did nothing where the panel rows accept it; cause the keyboard fix upgraded the panel rows and stopped short of the popup's; fix `role="button"` plus the panel's Space branch (scroll suppressed), Enter unchanged; `src/core/popup.ts`
  - 2026-08-08 reported: reported by the ux-designer adversary (round 2, MINOR); the popup residue of the round-1 keyboard finding closed on the panel
  - 2026-08-08 fixed: popup branch rows carry `role="button"` and the panel's Space branch (preventDefault so the list does not scroll); Enter behaviour unchanged. Galata 16/16 green
- [x] `DEF-27` **Popup row keydown cancels its own children's keyboard activation** - CRITICAL; the row-level Enter/Space handler had no target guard, and unlike a panel row the branch row hosts three focusable children - the selection checkbox and the Open/Copy buttons; a keyboard user pressing Space on the checkbox had the native toggle cancelled by the row's preventDefault and got activate() instead, so with nothing yet selected the dialog disposed and the session switched - the keystroke did the opposite of what it named; Enter/Space on Open and Copy were swallowed the same way, their stopPropagation being on click, which never fires once the keydown default is cancelled; cause the panel's key branch was transplanted onto a row with interactive children; fix the switch affordance moved off the row onto the label span, leaving the row a plain container; `src/core/popup.ts`
  - 2026-08-08 reported: reported by the ux-designer adversary (round 3, CRITICAL); a regression introduced by the DEF-26 fix
  - 2026-08-08 fixed: one structural change - `role="button"`, `tabIndex` and the keydown moved from the row onto the label span (src/core/popup.ts); the row keeps its click-to-switch and is otherwise a plain container, so the checkbox and the Open/Copy buttons get their native Enter/Space back. Verified at v0.1.15: Jest 61/61, pytest 132/132, Galata 16/16, eslint 0 errors
- [x] `DEF-28` **Popup keydown route bypasses the delete busy lock** - MAJOR; the panel's key branch opens with `if (removing) return;` and the popup copy dropped the equivalent `deleting` guard; the busy lock is `jp-mod-busy` pointer-events CSS, which the popup's own checkbox comment notes does not block keyboard, so during the async delete an Enter/Space on a row either mutated the in-flight `selected` set - exactly what the lock exists to prevent - or, with an empty selection, disposed the dialog mid-delete; cause the transplanted key branch lost the guard line; fix `if (deleting) return;` as the first line of the new label keydown, mirroring the panel; `src/core/popup.ts`
  - 2026-08-08 reported: reported by the ux-designer adversary (round 3, MAJOR); a regression introduced by the DEF-26 fix
  - 2026-08-08 fixed: the label keydown opens with `if (deleting) return;` mirroring the panel's `if (removing) return;` (panel.ts:1371), with the comment naming why the pointer-events scrim is not enough. Verified at v0.1.15
- [x] `DEF-29` **Nested interactive controls inside the popup row's role=button** - MAJOR; ARIA gives `button` presentational children, so `role="button"` on a row containing a checkbox and two `<button>`s is the axe `nested-interactive` violation - screen readers announce one composite button whose name concatenates every descendant and need not expose the inner controls as operable; the panel row was exempt because it holds spans only, so the pattern was safe at its source and wrong at its destination; fix host `role="button"`, `tabIndex` and the keydown on the label span instead, which contains only text and a badge; `src/core/popup.ts`
  - 2026-08-08 reported: reported by the ux-designer adversary (round 3, MAJOR); a regression introduced by the DEF-26 fix
  - 2026-08-08 fixed: the same structural move - the row carries no role or tabIndex, and the label span that now hosts them contains only the text span and the provider/fork badge, so no interactive descendant sits inside a role="button". Verified at v0.1.15

- [x] `DEF-35` **Flattened branch item names no verb and carries no branch icon** - MAJOR; when every launch variant suppresses itself (`_visibleVariantCount() === 0`) the Branch Session submenu is replaced by its inner default item at top level, where that item's label reads `Default (Bypass Approvals)` or bare `Normal` and its icon is the variant glyph - the word Branch and `branchIcon` both lived on the submenu title that was just removed; on Codex, whose `namingStrategy` is `none`, `_branchSession` shows no name dialog, so one click on an item that never says branch forks the conversation and launches a terminal, immediately above the separator guarding Clean Up Parallel Sessions and Remove; renders worse on a Claude row holding a live background agent, where `_resolvedVariant()` is null-guarded but `_buildsSameLaunch` is not, so the item reads bare `Normal` with no icon; `src/core/panel.ts`
  - 2026-08-10 reported: reported independently by the architect, ux-designer and bug-hunter adversaries (whole-repo round 3, MAJOR); mode resolution runtime-verified against the compiled `lib/core/modes.js` with the Codex descriptor
  - 2026-08-10 fixed: the command's label and icon are already lazy, so both now branch on the same predicate that decides the flattening - at top level it reads `Branch Session` plus the variant suffix and wears `branchIcon`, inside the submenu it keeps `Normal` / `Default (...)` under a title that already names the verb. The suffix carries the mode in words, which is what the single icon slot cannot also do. The bare-`Normal` rendering goes with it: whatever `_resolvedVariant()` answers on a background-agent row, the flattened item now names the verb. The guard asymmetry between `_resolvedVariant()` and `_buildsSameLaunch` is left alone - the visible symptom is the finding, and widening the guard is a change nothing asked for. Galata 20/20
- [x] `DEF-36` **The `+` button's direct-launch path never names the mode it launches with** - MINOR; with a mode in force the variant items suppress themselves, so the button's click handler calls `_newSession()` straight away instead of dropping a menu, and `_newSession` sends the resolved mode - one click starts an approval-free session; the tooltip built by `nameNewButton` is `New <label> session in <folder>` and never appends `_variantSuffix()`, so it is the one surface that omits the mode, where the dropdown item it replaced said `New <label> Session (Skip Permissions)` with the warning glyph and the row tooltip adds a `Launch mode:` line; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer and bug-hunter adversaries (whole-repo round 3, MINOR)
  - 2026-08-10 fixed: `nameNewButton` appends `_variantSuffix()`, and so does the title set at build time - a screen reader reads the accessible name without hovering, so the static one had to carry it too. `claude-regression.spec.ts:33` matches this button by EXACT title and still passes, because the suffix is empty with no mode in force; a run with one in force would need that selector loosened to a prefix match. Galata 20/20

- [x] `DEF-37` **One right-click on a background-agent row strips the launch mode's name off the whole panel** - MAJOR; `_resolvedVariant()` tested `hasBgAgents && _activeSession?.bg_id` and answered null while that row was active, which is correct for exactly one caller - `resume`, where an attach genuinely never receives the flag - and wrong for the four that do not attach: the row tooltip, the `+` button title, `new-session` and `branch-session`; `_activeSession` is set on ANY right-click and deliberately never cleared, so after right-clicking a row holding a live worker the `+` title drops back to naming no mode while a click on it still POSTs `mode: dangerouslySkipPermissions`, and the branch item renders a bare `Branch Session` with a neutral glyph while forking approval-free; the DEF-35 note priced this asymmetry as invisible, which was wrong; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 4, MAJOR) after I asserted in its seed that the helper was row-independent; disproved by execution against a seeded live background agent, with the POST bodies captured
  - 2026-08-10 fixed: `_resolvedVariant()` is now settings-only and a new `_resumeVariant()` carries the attach test, read by the `resume` command's label and icon alone. The suppression is stated where it is true rather than where it is convenient
- [x] `DEF-38` **The `+` button's title is written before settings arrive and never rewritten** - MINOR; `_buildShell` runs in the constructor while `_modes` is `{}`, and `applySharedSettings` calls `setModes` afterwards, so the title a user reads without ever hovering or focusing the button says no mode is in force however the setting is saved; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 4, MINOR); the residue of DEF-36's fix, which corrected the hover and focus paths only
  - 2026-08-10 fixed: the naming closure is held on the panel and re-run at the end of `setModes`. Menu labels are lazy and re-read the mode on every open; a title is a written string and has to be rewritten
- [x] `DEF-39` **The branch item traded its danger glyph for a branch glyph** - MAJOR; DEF-35's fix put `branchIcon` on the flattened item unconditionally, so with a mode in force `Branch Session (Skip Permissions)` rendered with a glyph identical in weight and colour to Show in File Browser while `Resume (Skip Permissions)` beside it in the same menu wore the caution triangle - the item had carried the triangle before that fix; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 4, MAJOR), rendered; a fix-introduced regression
  - 2026-08-10 fixed: the mode's glyph wins and `branchIcon` is the fallback for when no mode is in force. The suffix names the mode in words for assistive technology; the icon slot goes back to marking danger, which is its job everywhere else in the menu
- [x] `DEF-40` **The caution triangle is the smallest glyph in the menu and the same grey as the neutral ones** - MINOR; authored 13x13 in `jp-icon3` against 16x16 siblings, measured rgb(97,97,97) light and rgb(189,189,189) dark - identical to the folder, star and trash glyphs - so the one icon whose whole job is to say "this differs" was the least conspicuous; `src/core/icons.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary in round 3 and re-raised in round 4 with a rendered sample in both themes after I declined it for want of evidence
  - 2026-08-10 fixed: 16x16 and `jp-icon-warn0`, so it matches its siblings in size and departs from them in colour
  - 2026-08-11 glyph changed by user direction (v1.0.18): the triangle became the reference implementation's shield path - the shield is the established marker for an action touching the permission system (the UAC pattern). `warningIcon` renamed `shieldIcon` (`:shield`), all seven `panel.ts` call sites including the DEF-45 cleanup marker, sizing and colour of this fix kept (16x16, `jp-icon-warn0`), so the reference's 13x13 grey is deliberately NOT copied. Jest 121 green, Galata 27/27 at v1.0.18 including this entry's computed-fill test, shield rendered in the context menu on the 8931 test server
  - 2026-08-11 colour half reversed by user direction (v1.0.20): the shield paints in the standard `jp-icon3` grey like its siblings - the user ruled the shield SHAPE alone marks the difference. The size half stands (16x16). The unit colour test now asserts `jp-icon3` parity and the Galata probe identifies the glyph by its path data instead of `jp-icon-warn0`, its fill-differs assertion deleted with the orange
- [x] `DEF-41` **The branch count and the background-agent chip are clipped away on long project names** - MAJOR; both markers are appended inside `.jp-AiAssistantsPanel-name`, which is `overflow:hidden; text-overflow:ellipsis`, so at sidebar width a long name hides them entirely - measured with the badge box at x=1083 against a clip edge at x=913 - while a short-named row beside it shows both; the lost chip is the one saying a click JOINS a running worker rather than starting one; the popup already solved this and `base.css` records the fix; `src/core/panel.ts`, `style/base.css`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 4, MAJOR), rendered and measured; deferred - a row-layout change (name cell as a flex line, ellipsised text span, markers as non-shrinking siblings) is wider than this campaign's remit and wants its own rendered verification
  - 2026-08-10 fixed: the row name became the flex line the popup's branch rows already use - the text ellipsises inside its own `.jp-AiAssistantsPanel-nameText` span and both badges are `flex: 0 0 auto` siblings on the line, so the ellipsis can no longer eat them. Covered twice, both mutation-checked: a DOM test asserting the full name lands in `.nameText` with both badges as direct children of `.name`, and a stylesheet test asserting the ellipsis moved off `.jp-AiAssistantsPanel-name`. The pixel geometry in the report was NOT re-measured - jsdom performs no layout - so the structural claim is proven and the visual one is owed a Playwright screenshot
  - 2026-08-10 rendered proof (v1.0.7): the pixel claim is now settled - covered permanently by `ui-tests/tests/panel-regressions.spec.ts`, green in a full Galata run of 26/26 against a venv rebuilt from this tree. The suite asserts that the badge's box sits inside the row's box AND that the name genuinely overflows, so the assertion cannot pass by the fixture happening to fit.
- [x] `DEF-42` **The extension's submenus draw no submenu affordance** - MAJOR; `_menu()` builds a plain `@lumino/widgets` `Menu`, whose default renderer emits an empty `div.lm-Menu-itemSubmenuIcon`, so all three submenus render no caret where JupyterLab's own menus show one; round 3 sharpened the cost - `Branch Session` (hover, two choices) and `Branch Session (Skip Permissions)` (acts immediately) now differ only by a parenthetical; fix is `MenuSvg` from `@jupyterlab/ui-components`, which may also retire the icon-alignment patch in `base.css`; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 4, MAJOR), measured against JupyterLab's File menu in the same session; deferred - swapping the menu class touches every menu this extension draws and is not a regression from this campaign
  - 2026-08-10 fixed: `_menu()` builds a `MenuSvg` from `@jupyterlab/ui-components`, which fills the submenu marker div JupyterLab's own menus fill. "Nothing else changed" was verified rather than assumed - the same items rendered through a stock Lumino `Menu` produce an identical list of `type|label|has-icon` triples, and a source read of `MenuSvg` puts the remaining deltas at the `LabIconStyle` class on the icon cell (which `base.css` already forces), the `jp-ThemedContainer` class, and a check glyph for toggled items, of which this panel registers none. The optional half was declined: the `base.css` icon-alignment patch is now redundant rather than wrong, and removing a rule whose effect is invisible in jsdom needs the screenshot this wave could not take
  - 2026-08-10 rendered proof (v1.0.7): covered permanently by `ui-tests/tests/panel-regressions.spec.ts`, green in a full Galata run of 26/26 against a venv rebuilt from this tree. The suite asserts that the submenu marker holds an `svg` with a non-zero box - the marker cell exists in a plain Lumino menu too, empty, so asserting the cell alone would have been vacuous.
- [x] `DEF-43` **Ok on an empty branch name is a silent no-op** - MINOR; the handler returns without a word and Ok is never disabled, so a user who takes the greyed parent name for a default - the standard "press Ok for the default" idiom - gets a closed dialog, no POST, no notification and no inline error; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 4, MINOR), executed; deferred to the user - the fix is a choice between disabling Ok while blank and making the placeholder a real default
  - 2026-08-10 fixed: chose REPORT over disabling Ok. Ok on a blank or whitespace-only name raises `Notification.warning('Enter a name for the new session - nothing was branched.')` and launches nothing; cancel still returns silently. Reason recorded in the code: a notification is how this panel refuses everywhere else, and `InputDialog.getText` exposes no hook to disable Ok while the field is empty, so the alternative means replacing the dialog with a hand-built body widget for one refusal path. Three tests, mutation-checked - blank warns and never launches, `feature` launches trimmed, cancel says nothing at all
- [x] `DEF-44` **Recent and All render the identical list below the recent limit** - MINOR; with fewer projects than `recentLimit` the two sections show the same rows under different headings, which is the default state for anyone with under ten projects; Favourites already sets the conditional-section precedent; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 4, MINOR), rendered; deferred to the user - hiding a section the user can see the header of is a behaviour choice, not a defect fix
  - 2026-08-10 fixed: Recent renders only while it is genuinely shorter than All (`sessions.length > this._recentLimit`); below the limit the panel shows Favorites and All alone. Measured against the UNFILTERED count, following the Favourites precedent, so typing in the filter cannot make a section vanish underneath the user. Three tests, mutation-checked - three sessions at limit 10 give `['all']`, the same three at limit 2 give `['recent','all']`, and a filter narrowing to one row at limit 2 leaves both sections standing
- [x] `DEF-45` **Only one of the two destructive context-menu items is marked** - MINOR; `Clean Up Parallel Sessions (N)` registers no icon while `Remove from ...` carries the trash glyph, so the pair the separator was added to protect reads as one dangerous item and one ordinary one; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 4, MINOR), rendered; deferred to the user
  - 2026-08-10 fixed: the `cleanup-parallel` command carries `icon: warningIcon` - the glyph already imported in this file and already worn by the launch variants - so both members of the post-separator destructive group are marked alike. Asserted against the live `CommandRegistry` rather than the source, and mutation-checked.
  - 2026-08-10 rendered proof (v1.0.7): `panel-regressions.spec.ts` asserts both destructive entries draw a glyph with a non-zero box in a real browser. Green in a 26/26 Galata run. It also has a permanent home now in `src/__tests__/panel.spec.ts`, not only in the lane's scratch suite
  - 2026-08-11 glyph changed by user direction (v1.0.20): `cleanup-parallel` no longer shares the unsafe-launch marker - it wears the new `cleanupIcon` (Material `delete_sweep`, the sweep variant of the trash), same family as `Remove from ...`'s plain trash but visibly the bulk one. The shield stays on the permission-skipping entries only. Jest 121 green, Galata 27/27 at v1.0.20, rendered in the context menu on the 8931 test server

- [x] `DEF-46` **The row tooltip promises a launch mode the server discards on a background-agent row** - MINOR; `_buildRowTooltip` reads `_resolvedVariant()`, which since DEF-37 no longer consults `bg_id` at all, and appends `Launch mode: <label>` under a comment claiming "the row click launches with this"; for a Claude row held by a live background agent that is false - a row click resolves `attach_id` and `ClaudeStore.launch_argv` returns `[claude, attach, <id>]` BEFORE the skip-permissions append, because attach takes no options - so one row carries `Background agent: ... (click attaches to it)` and `Launch mode: Skip Permissions` one line apart while the context menu's first item, which goes through `_resumeVariant()`, correctly names neither; the exact mirror of DEF-37, whose fix removed the `bg_id` test from every caller including the one that does describe an attach; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 4, MINOR), runtime-proven against a real panel with the tooltip text captured; a fix-introduced regression from DEF-37, landed mid-review
  - 2026-08-10 fixed: `_buildRowTooltip` asks `_resumeVariant(s)` instead of the settings-only `_resolvedVariant()`, and `_resumeVariant` gained an optional target (defaulting to the active row) so any surface describing someone else's resume can ask about THAT conversation rather than the selected one. Three tests, mutation-checked: a dormant row's tooltip names the mode, a `bg_id` row names the agent and no mode at all, and a third pins DEF-37 against regression by holding the `+` label at "New Testbed Session (Skip Permissions)" while a background-agent row is active
- [x] `DEF-47` **The init waiter's fallback ships the 1x1 sentinel into the assistant** - MINOR; when no client ever attaches the loop exhausts its 50 iterations and execs with the pty still at the `stty rows 1 cols 1` the child set, so the assistant renders into one column until the user resizes something - measured `falls back at 5.50s, size=('1','1')`; the same end state is reached by a ~2ms inversion where the client's resize lands BEFORE the child reaches its `stty`, the child stamps 1x1 over the browser's size and nothing resizes again - measured bad 10/10 at 0ms, 9/10 at 1ms, 1/10 at 2ms, 0/10 from 3ms upward; the sentinel is the right mechanism and the wrong thing to fall back to; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-10 reported: reported independently by the architect and bug-hunter adversaries (whole-repo round 4, MINOR), both measured; the no-client case is certain rather than racy, and one clause on the timeout path closes both
  - 2026-08-10 fixed: `_INIT_WAITER` captures the pty size BEFORE marking it 1x1 and restores that captured baseline on the timeout path, so an exec that never saw a client resize lands in the window terminado created rather than in one column. Both directions close together, which is why they were fixed as one item: the no-client case now falls back to a usable size, and the ~2ms inversion - client resize landing before the child's `stty`, after which nothing resizes again - ends at the same restored baseline instead of 1x1. Proven by driving a real pty exactly as `LaunchHandler` does (`ptyprocess.PtyProcessUnicode.spawn(_wrap_with_init(argv))`, which sizes 24x80 like terminado) with an inner `stty size`, with and without a simulated attach. The server-side `setwinsize` stays deleted - DEF-33 records why marking must happen in the child. Covered by `tests/test_init_waiter.py`
- [x] `DEF-48` **The Galata gate races the panels it asserts on** - MINOR; `waitForApplication` waits only for the splash to detach and `.jp-LabShell` to be visible, while the extension docks its panels after two server round trips (`probeStatus` then `migrate`), and Galata's `openTab` does a single `count()` with no retry and no auto-waiting assertion - so a loaded runner can report a red suite naming a spec unrelated to the change under test and be green on re-run; fix is an `expect(locator).toHaveCount(1)` inside a shared `openPanel` helper that every spec uses instead of the bare `page.sidebar.openTab`; `ui-tests/tests/shared.ts`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 4, MINOR); the mechanism is code-traced and stands, but the supporting observation (17/20, 19/20, 20/20 on one tree) is CONFOUNDED - the main session was running a second Galata suite on the same port at the time, and `rm -rf .scratch` against a literal path means concurrent runs delete each other's fixtures regardless of port. Re-measure on a quiet machine before sizing it
  - 2026-08-10 fixed: the `waitForApplication` override in `ui-tests/tests/shared.ts` - which already existed for a related reason - now waits, after splash and shell, for a sidebar tab to attach for every provider the server's own status reports, and the bare `page.sidebar.openTab` call sites go through a retrying helper instead. The confounded flake measurement was NOT used as evidence for the fix; the mechanism is code-traced against Galata's own source, and the suite has not been re-run because no lane in the wave was permitted to
- [x] `DEF-49` **Concurrent Galata runs destroy each other regardless of port** - MINOR; `webServer.command` opens with `rm -rf .scratch` and `jupyter_server_test_config.py` resolves `SCRATCH` to a literal path, so `JLAB_TEST_PORT` isolates the port and nothing else - two suites on different ports still wipe each other's fake HOME, seeded stores and stub binaries mid-run; cost the round-4 architect two of its three runs and the main session one whole round; `ui-tests/playwright.config.js`, `ui-tests/jupyter_server_test_config.py`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 4) and then reproduced first-hand by the main session, which collided with the bug-hunter agent on port 8899; concurrent runs are not a stated requirement, so a sentence in `ui-tests/README.md` is a defensible fix - deriving the scratch root from the port is the thorough one
  - 2026-08-10 fixed: took the thorough one. Every directory a run owns is keyed by its port - `.scratch/<port>` for the sweep, the fake HOME, the seeded stores and the stub binaries, `test-results/<port>` for the output dir, and the same for the Playwright report - so two suites cannot alias whatever order they start in. The nesting sits inside paths `.gitignore` already covers, so no ignore rule changed. Same pass found and fixed the live-config leak the suite denied having: `jupyter_server_test_config.py` did set HOME, but Jupyter had already built its config search path by then, so the variable arrived too late to move it

- [x] `DEF-51` **Both irreversible confirmation dialogs hand the keyboard to the destructive button** - CRITICAL; `_removeProject` and `_cleanupParallel` call `showDialog` with `[cancelButton, warnButton('Remove')]` and no `defaultButton`, and JupyterLab normalises the missing field to `buttons.length - 1`, sets `_primary` from it and focuses it on attach - so the destructive button is focused the moment the dialog appears and a single Enter drops an entire project history; `resolve()` with no index falls back to the same button; JupyterLab's own file browser sets `defaultButton: 0` on its delete with the comment that Delete and Enter sit next to each other on many keyboards; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 5, CRITICAL), proven in jsdom against the real `Dialog` - the panel's own button pattern focused `Remove`, and a control with `defaultButton: 0` focused `Cancel`; survived both skeptics
  - 2026-08-10 fixed: `defaultButton: 0` on both dialogs, with the reason recorded beside each. `src/__tests__/panel.spec.ts` now pins it permanently, and each assertion is paired with a NON-VACUITY control that focuses the destructive button and presses the same Enter - so a test that stopped exercising the keyboard would fail rather than pass silently

- [x] `DEF-53` **A failed launch erases its own error message before it can be read** - MAJOR; `_openSession`'s catch calls `_showError`, and its `finally` calls `_fetch()`, whose success path clears the inline banner in the same tick - so a refused launch flashes an error and wipes it; the copy was wrong for the case as well, reading "Could not reach the server - retrying" for a launch that reached the server and was refused; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 5, MAJOR); survived both skeptics
  - 2026-08-10 fixed: a new `_notifyLaunchError(copy, err)` logs to the console and raises `Notification.error`, matching the 404 branch three lines above it in the same catch, and the two catch sites whose `finally` refetches now route through it. Every other `_showError` call site was checked before touching the helper: `_branchByNativeCommand` and `_branchByServerCopy` `return` inside their catch before any refetch, so their banner survives to the next poll and they were deliberately left alone. Proven by driving the real panel with `openSession` and `launch` rejecting, and reading what `Notification.error` received
- [x] `DEF-54` **The one-click approval-bypassing launch is the only launch surface with no warning glyph** - MAJOR; with an unsafe mode in force `_visibleVariantCount()` is 0 and the `+` button launches directly on click, carrying only `addIcon`, while every menu entry for that same action carries `warningIcon`; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 5, MAJOR); survived both skeptics
  - 2026-08-10 fixed: the button's glyph is painted by a held `paintNewIcon()` closure re-run from `setModes`, so settings retitle AND repaint it - the DEF-38 lesson applied to the icon rather than only the title. Verified by reading the rendered svg's `data-icon`: `...:add` with no mode in force, `...:warning` after `setModes({skip: true})`, exactly one svg either way
  - 2026-08-10 rendered proof (v1.0.7): the browser confirms the repaint, and settles the DEF-40 half against a live measurement rather than an authored class name - the warning glyph's computed `fill` differs from a neutral sibling read in the same page, and its box is not smaller than its siblings'. Green in a 26/26 Galata run.
- [x] `DEF-55` **The cleanup progress modal hides its only exit** - MAJOR; `_cleanupParallel` hid `.jp-Dialog-footer` for the duration of the request and restored it in `finally`, so a hung request left a modal with no keyboard or mouse way out; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 5, MAJOR, contested - one of the two skeptics refuted it)
  - 2026-08-10 fixed: judged REAL after checking the premise rather than accepting it - `src/core/request.ts` has no `AbortController`, no signal and no timeout, and neither does `ServerConnection.makeRequest` beneath it, so a hung request hangs indefinitely. The footer is no longer hidden; the lookup and the `finally` that restored it are gone with it. Closing the dialog does not cancel the request, which the comment now records - a modal with no exit is worse than one dismissed early
  - 2026-08-10 rendered proof (v1.0.7): the Galata test HOLDS the DELETE open so the dialog is caught mid-flight, which is the only state the defect existed in, and asserts Close is visible and enabled there. It also corrected a wrong assumption of mine: the dialog does not dismiss itself when the request lands - it shows the outcome and waits - so the test now clicks Close and asserts it hides, which is the stronger claim. A way out is only a way out if pressing it gets you out.
- [x] `DEF-56` **The filter toggle communicates its pressed state to sighted users only** - MINOR; `filterBtn` carried `jp-mod-active` and no `aria-pressed`; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 5, MINOR)
  - 2026-08-10 fixed: built with `aria-pressed="false"` (the bar starts hidden) and written by `_toggleFilterBar` beside the existing class toggle. Verified across two toggles that the attribute and the class never disagree
- [x] `DEF-57` **Every row is a tab stop, and one project can be three of them** - MINOR; the same project renders in Favourites, Recent and All, each row `tabIndex = 0`, so tabbing through the panel hits it three times; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 5, MINOR)
  - 2026-08-10 fixed: roving tabindex per section. `_renderSection` gives one stop per section - the remembered row from a new `_roving` map, falling back to the first row when it is gone - and the arrows move within the section. The poll's focus restoration by `dataset.encodedPath` still works, which was the condition for attempting it at all. Verified with three sessions, one starred, at recentLimit 2 so all three sections render: exactly three `tabindex=0` rows panel-wide, one per section
  - 2026-08-10 rendered proof (v1.0.7): asserted in a real browser as stops == populated sections, counting only sections that actually render rows so a collapsed section cannot fail a correct state. Green in a 26/26 Galata run.
  - 2026-08-10 fixed: `defaultButton: 0` on both. Grepped every dialog in `src/core`: these two are the only ones carrying a `warnButton`, and the popup's delete is an armed two-step rather than a dialog default

- [x] `DEF-76` **`_showError`'s retry promise is false at three of its five call sites** - MINOR; `_showError` prefixes every message with "Could not reach the server - retrying." (`src/core/panel.ts:607`), which is TRUE on the two poll/refresh paths that own the retry (`:240`, `:2559`) and false everywhere else - the cleanup catch at `:806`, which has already written `Cleanup failed: <msg>` into its own modal, and the two JupyterLab command rejections at `:2056` and `:2078` (`terminal:create-new`, `filebrowser:go-to-path`) which never touch the server at all; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 6, MINOR) and corrected by its impact agent - the finding counted the poll and refresh callers among the offenders, where the copy is the one honest thing on the path; the residual is three sites, not five
  - 2026-08-10 deferred: left out to keep this wave's diff at two lines in one function pair, and because neither remaining site is a copy change. At `:806` routing to `_showActionError` would print the modal's own sentence a second time in the inline bar, so the smaller correct move is to drop the banner entirely - a behaviour change. The two command rejections need copy naming the command that failed, which is a third string. Resolve together with `DEF-81` as one deliberate pass over `_showError`, `_showActionError` and `_notifyLaunchError`
  - 2026-08-10 fixed: the one deliberate pass this entry and `DEF-81` both named. The cleanup catch in `_cleanupParallel` dropped its `_showError` call entirely - the modal already writes `Cleanup failed: <msg>`, so the banner duplicated it (accepted side effect: that path's `console.error` went with it, `_showError` having done both). The two JupyterLab command rejections route through `_showActionError` with copy naming the failure - 'Could not open a terminal for this session - try again.' (`panel.ts:2080`) and 'Could not open the project folder - try again.' (`:2107`). Final inventory: exactly two `_showError` call sites remain, both retry-owning - manual Refresh (`:245`) and the poll tick (`:2602`). No mutation check possible - `panel.ts` is not Jest-importable, so no test can redden on a banner string; verification is the grep inventory, `jlpm build:lib` exit 0, eslint 0 errors, and the copy rendering in the Galata 27/27 run at v1.0.11
  - 2026-08-10 correction and re-land (round 8): the two command-site routings were NOT in the tree when the note above was written - both round-8 adversaries proved the strings absent and four `_showError` call sites live. The lane's own verification was true when it ran; a parallel lane's mutation-check restore of `panel.ts` (see `DEF-100`) overwrote the two late-file hunks afterwards. Re-landed byte-equal to the description; the inventory now holds - grep answers two `_showError` sites (`:243`, `:2602`). The 'copy rendering in the Galata 27/27 run' sub-claim is WITHDRAWN: no browser test asserts these strings and that run predates the re-land; verification is the grep inventory, `jlpm build:lib`, eslint, and the Galata suite re-run over the re-landed file
  - 2026-08-10 line drift (round 9, cosmetic): the poll-site `_showError` call moved from `:2602` to `:2612` when the `DEF-74` hunk landed above it. The two-site count stands

## Colour store

- [x] `DEF-30` **Colour store hand-rolls write ordering instead of serialising its own requests** - MINOR; `ColourStore` carries a monotonic write counter, a per-id record of the latest write and a reload counter (`_writeSeq`, `_writeStart`, `_current`, `_readSeq`/`_readAbsorbed`) so that answers arriving out of order cannot undo each other; every one of those questions becomes unconstructible if the store simply chains its own `load`/`set`/`forget` through one promise, since only one request would ever be on the wire; cause the ordering rules were added defect by defect across three adversarial rounds rather than designed; fix a `_run(op)` chain field, then delete the counters and the interleaving tests that describe states which can no longer occur; `src/core/colour.ts`
  - 2026-08-09 reported: proposed by the architect adversary (round 7, MINOR judgement) after its round-7 MAJOR removed the optimistic cache write that half of the machinery existed to police
  - 2026-08-09 deferred: not a correctness risk as it stands - the reproduced defects are closed and covered by Jest. Deferred with a caveat the proposal does not price: `requestProvider` sets no timeout, so chaining writes behind a hung GET would silently stop capturing hand-set colours for the rest of the session. A serialisation chain needs a bounded request first, which is a change to the shared request helper rather than to this store
  - 2026-08-10 fixed: the prerequisite was met first (`DEF-72`), then the chain landed. `_run(op)` is one field and five lines - `const result = this._chain.then(op); this._chain = result.then(settled, settled); return result;` - and `load`, `set` and `forget` all enter through it, so only one of this store's requests is ever on the wire. Deleted with it: `_writeSeq`, `_writeStart`, `_readSeq`, `_readAbsorbed`, the `_current(ids, seq)` predicate and the whole `_absorb` reconciliation. Counted rather than estimated - 22 references to those six names before, 0 after. Eight mutations, each type-checked first so a red is behavioural, each reverted from a scratch copy and re-verified identical; the routing mutations (issue both requests at once, bypass the queue per method) redden 3 to 4 tests each. Five interleaving tests were deleted and five written in their place, each deletion carrying a proof that its state can no longer be constructed rather than an assertion that it is fine - the suite is 34 tests before and after
  - 2026-08-10 note: `_pending` and `isPending` deliberately SURVIVE, and now count from the CALL of a write rather than from the moment its POST is sent. The serialisation created that window - a capture can sit unsent in the queue - and a release affordance offered inside it would be undone by the capture landing afterwards. Mutation-checked: moving `_openWrite` inside the queued op reddens three tests
- [x] `DEF-31` **A fork started inside the capture window inherits the parent's OLD colour** - MINOR; `_branchSession` reads the parent's tint at click time (`panel.ts:845` -> `TerminalManager.colourFor`, and the native branch of `_inheritColour` at `:1012`, called from `:897` and `:1082`), and since the cache holds only server-confirmed values, a colour the user set on the parent's tab moments earlier is not there yet; the branch is then born with the previous tint or the id hash, stored as INHERITED, so it is never offered for release and no later pass corrects it; window is one round trip, permanent when it fires; `src/core/panel.ts`, `src/core/terminals.ts`
  - 2026-08-09 reported: reported by the bug-hunter adversary (round 8, MINOR); a consequence of removing the optimistic cache write, which had been masking the window
  - 2026-08-09 deferred: graphify blast radius over `ColourStore` names 17 consumer sites across three files; 15 satisfy the confirmed-value contract and these 2 are the whole remainder. Closing it means either awaiting a pending write inside the fork path - which contradicts the deliberate snapshot-before-fork - or a new settlement API on the store. Both are more machinery than the defect, and adding machinery to this store is what the last four rounds did
  - 2026-08-10 fixed: with the store serialised, the second option arrived for free and nothing was built. `_inheritColour` gates on `isPending(parentId)`, calls the existing public `load()` - which IS the chain step, because a queued request cannot be sent until the capture ahead of it has been answered - and then RE-READS the parent, with the click-time value demoted from the value to the fallback. Mechanisms added: none; four statements over two methods that already existed. The re-read is unconditional rather than inside the pending branch, which closes a second hole the waiting alone would have left: the parent's colour is read before the name dialog opens, and a capture in flight at click time very often lands while that dialog is up, leaving nothing pending by the time the fork runs. Six mutations, each reverted from a scratch copy; dropping the wait reddens both settlement tests, dropping the gate reddens both no-reload tests, and dropping the fallback does not even compile. The blast radius was re-derived rather than taken from the old note - 17 consumer sites, confirmed
  - 2026-08-10 caveat, stated because it is the honest limit: everything above is jsdom against a mocked `requestProvider`. It proves the panel does not write the fork's colour until the parent's capture has been answered, and that it writes the answered value. It does NOT observe a real JupyterLab doing it, so the step from "the client issues these requests in this order" to "the user sees the fork wearing the new tint" is an argument rather than a measurement

- [x] `DEF-52` **The two FNV-1a implementations disagree outside the Basic Multilingual Plane** - MAJOR; `fnv1aColour` walked UTF-16 code units (`charCodeAt`) and `derived_colour` walked code points (`ord`), so an id containing an astral-plane character hashed to a different colour depending on which runtime resolved it - measured, `"𝕏id"` tinted rose in the browser and peach on the server; the Python docstring asserted the opposite in as many words, claiming "the same hash, over the same id string ... so a conversation tints identically whichever side resolved it"; `src/core/colour.ts`, `jupyterlab_ai_code_assistants_extension/providers/kimi.py`
  - 2026-08-10 reported: a round-4 architect SUSPICION (the two implementations exist with nothing binding them) turned into a measured fact by the differential guard built for it in the round-5 fix wave, which failed on its first run - 1 of 508 ids
  - 2026-08-10 fixed: both hash the id's UTF-8 BYTES, which is what FNV-1a is defined over. Bytes rather than either character reading on purpose: `charCodeAt` and `ord` are each the natural idiom in their own language, so fixing one to imitate the other would drift back the next time somebody rewrote that loop - bytes are the reading both languages spell naturally. Every real conversation id is ASCII (UUIDs, hex, `session_<uuid>`), where all three readings coincide, so no existing tab changes colour. `test_the_derived_colour_agrees_across_runtimes` went red to green over the change

- [x] `DEF-77` **A tab tint outlives the provider that was disabled under it** - MINOR; disabling a provider leaves the tint standing on every terminal tab that already carried one. The gate is an ORDERING, not a race: JupyterLab persists the setting to disk before it emits `changed`, so by the time `panel.dispose()` reaches `clearColours()` (`src/core/panel.ts:217`) every `probe()` is already answering 404 `provider_disabled` (`core/routes.py:277`) and returning null, and the null guard in the reconcile loop (`src/core/terminals.ts:449`) returns before `_applyColour(widget, null)` can clear anything; `src/core/terminals.ts`, `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 6, MINOR) citing `terminals.ts:432` and an unwired `clearColours()`; both are wrong and the impact agent said so - `clearColours` IS wired, from the `colourfulTabs` setter (`terminals.ts:262`) and from panel disposal (`panel.ts:217`), and `_applyColour` is never reached to be blamed. The mechanism above is the corrected one
  - 2026-08-10 deferred: bounded and self-clearing - the tint lives only on the widget's `title.className`, so it dies on reload or when the tab closes, and no stored colour, session or file is affected. The only honest fix is a client-side `Set` of the widget references this panel has tinted, so `clearColours` can reach them without asking a server that is now refusing - new retention and new state in a method with zero test coverage, which is exactly the shape that made 11 of this ledger's entries fix-induced
  - 2026-08-10 closed by ruling (Star Colonel's direction, close-19 campaign): wontfix. Bounded and self-clearing - the tint lives only on `title.className`, dies on reload or tab close, and no stored colour, session or file is affected. The only fix is a client-side `Set` of tinted widget references in a method with zero test coverage - new retention, new state, the exact shape this ledger's fix-induced entries share. The defect costs a transient cosmetic; the fix prices as the next entry

## Guards, tests and tooling

- [x] `DEF-58` **The core-neutrality guard's assistant list is a hardcoded regex** - MAJOR; the architecture's one enforced invariant is that core code never names an assistant, and the guard enforced it by grepping `src/core/` for the four current names - so a fifth provider gets a green suite over a core file that names it, silently, which is the exact moment the guard is most needed; `src/__tests__/core-neutrality.spec.ts`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 5, MAJOR); survived both skeptics
  - 2026-08-10 fixed: the scan pattern is built at run time from the registry - id, CLI binary, legacy plugin id and the non-generic words of each descriptor's label, plus a hand-kept vendor-alias list for spellings no descriptor carries. Proven end to end on a scratch copy: a fictional fifth provider `nimbus` added to `src/providers/index.ts` plus an `isNimbus()` appended to a core file, and the guard caught it
- [x] `DEF-59` **The one binding between the two descriptor copies discards enum values** - MINOR; `test_descriptor_parity.py` compared field presence but flattened `launchModes` in a way that dropped the enum VALUES - the exact drift class its own docstring names; `jupyterlab_ai_code_assistants_extension/tests/test_descriptor_parity.py`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 5, MINOR)
  - 2026-08-10 fixed: the Node dump flattens `launchModes` into the token vocabulary Python stores - boolean modes as `id`, enum modes as `id=value` per value - and the comparison is over the whole token list. Mutation-checked against a scratch build with `auto_edit` renamed to `autoEdit` in the compiled Gemini descriptor, which the new comparison fails and the old one passed
- [x] `DEF-60` **Three cross-runtime guards read build artifacts that `make test` never refreshes** - MINOR; every assertion in the parity file compares the Python registry against COMPILED TypeScript under `lib/`, and `make test` runs `jlpm test` then `pytest` with no build step - so a stale `lib/` turns the guard into a green light for drift it never looked at; `jupyterlab_ai_code_assistants_extension/tests/test_descriptor_parity.py`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 5, MINOR); survived both skeptics
  - 2026-08-10 fixed: inside the test rather than in the Makefile, which is a copy of a canonical shared file and out of scope to fork. `_require_fresh_lib()` compares the newest `.ts` under `src/` (excluding `src/__tests__`, which tsc never compiles) against the newest `.js` under `lib/` and fails naming both files and the build command. It fired immediately against the repo as found, which is how the staleness was confirmed rather than argued
- [x] `DEF-61` **The terminal probe route's success path has zero tests and the fake built for it is dead code** - MAJOR; DEF-32 added a test for the 404 path only, leaving `_FakePty` with no exercising caller and the whole resolve-a-conversation-from-a-terminal path unasserted; `jupyterlab_ai_code_assistants_extension/tests/test_routes.py`
  - 2026-08-10 reported: reported by the slop-hunter adversary (whole-repo round 5, MAJOR)
  - 2026-08-10 fixed: `test_probing_a_known_terminal_answers_its_conversation_and_colour` registers a terminal directly in `manager.terminals`, stubs the store's pid identification and POSTs a hand-set colour, then asserts the probe answers all three. Two mutations checked - forcing `_tree_assistant` to `(None, None)`, and dropping the colour - each reddening only its own field
- [x] `DEF-62` **Six defects closed in `panel.ts` and `icons.ts` and no runnable suite can redden any of them** - MAJOR; `src/__tests__/` held colour, core-neutrality, labels, launch-mode and registry specs and nothing for the panel, so every panel fix this campaign made was verified by rendering it once and never again; `src/__tests__/panel.spec.ts`
  - 2026-08-10 reported: reported by the slop-hunter adversary (whole-repo round 5, MAJOR). Correct, and the most uncomfortable finding of the round - DEF-51 had already proven the panel is testable in jsdom against the real `Dialog`, so the tests were absent rather than impossible
  - 2026-08-10 fixed: `src/__tests__/panel.spec.ts`, 16 tests with 14 mutation checks, covering DEF-35, DEF-36, DEF-38, DEF-39, DEF-40, DEF-45, DEF-46 and DEF-51. It runs under the COMMITTED jest config: the ESM chain that blocked it (`@jupyterlab/apputils` reaching `@jupyter/react-components` and `@jupyter/web-components`) is cut by two `jest.mock` calls in the spec itself, so `jest.config.js` needed no change. The DEF-51 assertions each carry a non-vacuity control that focuses the destructive button and presses the same Enter
- [x] `DEF-68` **eslint config contradicts the codebase's own conventions and its sibling ignore files** - MINOR; `no-unused-vars` was configured `{ args: 'none' }`, covering neither caught errors nor variables, while the code uses a leading underscore for deliberately-unused bindings - 10 of the 54 standing warnings were that mismatch; separately the config had no ignore for `tmp/`, which `.gitignore` and `.prettierignore` both gained this session; `eslint.config.mjs`
  - 2026-08-10 reported: reported by the slop-hunter adversary (whole-repo round 5, MINOR, two findings). Its stronger claim - "lint gate is red now" - did NOT reproduce: `npx eslint . --no-cache` measured 0 errors both before and after the graph was present, so the ignore was added for consistency with its siblings and not as a repair
  - 2026-08-10 fixed: the rule reads `{ args: 'after-used', argsIgnorePattern: '^_', varsIgnorePattern: '^_', caughtErrorsIgnorePattern: '^_' }`, and `tmp` / `graphify-out` joined the ignores. Warnings fell 54 → 43. The one remaining unused binding was a genuine one and was fixed rather than silenced: `request.ts` used `catch (error)` and never read it, now a bindingless `catch`
- [x] `DEF-69` **`logs/README.md` documents two log families out of ten, one of which cannot exist** - MINOR; the directory is the campaign's own record and its index described almost none of it; `logs/README.md`
  - 2026-08-10 reported: reported by the slop-hunter adversary (whole-repo round 5, MINOR)
  - 2026-08-10 fixed: the list covers every family actually on disk, with the run kinds each carries
- [x] `DEF-70` **`ForkStrategy` carries a `'none'` member with no producer and no consumer** - MINOR; nothing emits it, nothing reads it, and its justification is contradicted by the field being required; `src/core/types.ts`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 4, MINOR)
  - 2026-08-10 fixed: deleted after a grep across BOTH runtimes, and replaced by a note on why there is no TypeScript member - Python's `Capabilities` defaults the strategy instead
- [x] `DEF-71` **`src/index.ts` restates the settings defaults as literals and leaves the clamp bounds unnamed** - MINOR; `'name'`, `10`, and the `Math.max(1, Math.min(100, ...))` bounds each existed twice or three times across `index.ts`, `panel.ts` and `schema/plugin.json` with nothing binding them; `src/index.ts`, `src/core/panel.ts`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 4, MINOR); the fix crosses two files, so the fix wave's lane owning `index.ts` correctly declined it rather than reaching into `panel.ts`
  - 2026-08-10 fixed: `panel.ts` exports `DEFAULT_RECENT_LIMIT`, `MIN_RECENT_LIMIT`, `MAX_RECENT_LIMIT` and `DEFAULT_PRESENTATION_MODE`; `index.ts` imports them and the clamp names its bounds. A third copy of the same numbers survives in `scripts/generate-schema.mjs`, which generates the schema that is the source of truth for them - left alone deliberately, and recorded here so it is not rediscovered as new

- [x] `DEF-72` **Every request the frontend makes is unbounded** - MAJOR; `requestAPI` set no timeout, and `ServerConnection.makeRequest` beneath it sets none either, so a request that never answers keeps its promise pending for as long as the browser holds the socket; it is what made `DEF-55`'s cleanup modal a genuine trap rather than an inconvenience, and it is the prerequisite `DEF-30`'s deferral note named; `src/core/request.ts`
  - 2026-08-10 reported: named twice from opposite directions - by `DEF-30`'s 2026-08-09 deferral note, and independently by the round-5 fix wave's Lane A2 while judging the cleanup-modal finding, which verified there is no `AbortController`, no signal and no timeout anywhere on the path
  - 2026-08-10 fixed: `REQUEST_TIMEOUT_MS = 60_000` with a hand-rolled `AbortController` plus timer, armed ONLY when the caller passes no `signal` of its own, and the controller's signal spread into a COPY of `init` so the caller's object is never mutated. A timeout surfaces as a new `RequestTimeoutError` with an `isRequestTimeout` predicate mirroring the file's existing `isResponseStatus`, and is asserted NOT to be a `ServerConnection.NetworkError` - a caller that cannot tell "the server is slow" from "the server is gone" cannot report either honestly. `clearTimeout` in a `finally` around both the fetch and the body read, because fetch resolves on headers and a stalled body is a second way to hang. Seven tests, six mutations, each reddening only its intended target
  - 2026-08-10 note on the number, which is not free to pick: 60s sits above the server's own `_CLI_TIMEOUT_S = 30` (`providers/codex.py`), re-verified rather than assumed, because a Codex fork legitimately keeps the server busy for thirty seconds and a client bound at or below that would break branching silently on exactly the slow path it was tuned for. Two routes are N-bounded and NO constant can cover them - `CodexStore._dispose` is one CLI invocation per conversation, and the sessions listing calls `git_branch` once per project root - which was reported rather than papered over by raising the constant. Also measured rather than assumed: `AbortSignal.timeout` is UNDEFINED in this jsdom (node 26, jest jsdom environment), so hand-rolling was mandatory, not stylistic
- [x] `DEF-73` **The serialised colour store has a deadlock invariant that nothing enforces** - MINOR; `_run` queues each request behind the last, so an op that itself called `load`, `set` or `forget` would await a chain waiting on it - a silent permanent hang, not an error. It cannot be caught by a runtime guard: at the moment the inner call is made, "an op is executing" is equally true of a legitimate call from the panel, and the store has no way to tell the two apart. `inherit` is un-queued for exactly this reason, and a future edit that queued it would deadlock with nothing to say so; `src/core/colour.ts`
  - 2026-08-10 reported: found by the chain's own gate agent, which was asked to judge whether the file got simpler and answered that the ordering mechanism did while the file did not - "a new global invariant appears that nothing enforces". A fix creating the next round's hazard is the pattern this whole campaign has been closing, so it was closed in the same pass rather than logged for later
  - 2026-08-10 fixed: enforced the way this repository already enforces its other unrunnable invariant (`core-neutrality.spec.ts`) - by reading the source. A new `ColourStore queue re-entrancy` block extracts each queued method's body by brace matching and asserts it contains no call to another, plus a non-vacuity test pinning that the bodies are big enough to be worth scanning and that the scan DOES find the shape when it is legitimately present (`inherit` calls `set`, from outside the queue). Mutation-checked by injecting `await this.load()` into `set`: the guard reddens on `set` alone, 1 failed / 2 passed, and the same mutation hangs six runtime tests to jest's 10s timeout - which is the deadlock itself, observed
- [x] `DEF-74` **The server-copy fork path can still hand a branch the parent's old colour** - MINOR; `DEF-31` closed the client-side reads, but `_branchByServerCopy` (kimi, gemini) is not a `ColourStore` consumer at all - the SERVER copies the colour during the fork, so a capture still queued on the client is not in the store the server reads; the same symptom as `DEF-31` through a different mechanism, and the fix is a different one too: make the branch POST wait for the client's colour queue to drain so the server's read is fresh; `src/core/panel.ts`
  - 2026-08-10 reported: found and MEASURED by the `DEF-31` stage, which left it open deliberately and said so rather than widening its own scope. It is outside `DEF-31` as written - the defect text names the client-side reads, and the graphify blast radius named those two as the whole remainder precisely because `_branchByServerCopy` is not a store consumer
  - 2026-08-10 deferred: needs its own mechanism and its own rendered verification against a real Kimi or Gemini fork, neither of which has a stub in the Galata fixtures today. Logged rather than folded into a closing change on the object with this codebase's worst regression history
  - 2026-08-10 fixed: `_branchByServerCopy` gates on `isPending(parent)` and awaits the store's public `load()` before the branch POST - `DEF-31`'s exact pattern, twelve lines, no new machinery, no change to `ColourStore`. The awaited load is a queued request on the `DEF-30` chain, so it cannot be sent until the capture ahead of it is answered, which is what makes the server's colour read fresh; outside the capture window the gate is false and a fork costs nothing extra, inside it the wait is bounded by `REQUEST_TIMEOUT_MS`. Honest coverage, in `DEF-31`'s own terms: no automated test exercises this gate - the `DEF-31` jsdom harness lives in `panel.spec.ts:388+` and never reaches `_branchByServerCopy`, `colour.spec.ts` constructs no panel. `colour.spec.ts` 38/38 and `panel.spec.ts` 21/21 green over the change; the rendered-tint claim remains an argument, not a measurement - the Galata fixtures still hold no Kimi/Gemini fork stub
- [x] `DEF-75` **The lib-freshness guard can report a staleness no rebuild clears** - MINOR; `_require_fresh_lib` compares the newest `src/*.ts` mtime against the newest `lib/*.js`, but `tsconfig.json` sets `incremental: true`, so a compile whose sources are semantically unchanged emits nothing and refreshes neither the outputs nor `tsconfig.tsbuildinfo` - a file touched without changing content leaves all four parity tests red and no ordinary rebuild can clear them; `jupyterlab_ai_code_assistants_extension/tests/test_descriptor_parity.py`
  - 2026-08-10 reported: hit first-hand minutes after `DEF-60` added the guard, by restoring `src/core/colour.ts` from a scratch copy after a mutation check - which is a thing this campaign does constantly, so the false positive is not exotic
  - 2026-08-10 fixed: the marker is now `max(newest lib/*.js, tsconfig.tsbuildinfo)`, and the failure message names the remedy for the residual case - delete the buildinfo to force a full emit. Deliberately NOT a general fix, and the docstring says so: mtime cannot tell "edited and not rebuilt" from "touched and identical", and the conservative direction is the correct one, because the failure the guard exists to prevent is a silent green. My first attempt claimed the buildinfo is refreshed on every compile; that was measured to be FALSE and the comment was corrected rather than left flattering. Proven both ways - `touch` alone still reds, `rm tsconfig.tsbuildinfo && npx tsc --sourceMap` clears it with no content change

- [x] `DEF-79` **The lib-freshness guard is disarmed by the project's own typecheck command** - MINOR; `DEF-75` widened the freshness marker to `max(newest lib/*.js, tsconfig.tsbuildinfo)` (`BUILD_INFO`, `tests/test_descriptor_parity.py:41`), and `npx tsc --noEmit` - the typecheck this project runs in every verification list - writes `tsconfig.tsbuildinfo` while emitting no `.js` at all, so running it makes a provably stale `lib/` read as fresh. Measured rather than reported: buildinfo at 14:28:57, one comment appended to `src/core/types.ts`, `npx tsc --noEmit`, buildinfo at 14:57:57 and all four parity tests green over a `lib/` that never saw the edit. It is a silent green, the exact failure the guard exists to prevent, and the third link in a chain - `DEF-60` added the guard, its false positive became `DEF-75`, and `DEF-75`'s fix became this; `jupyterlab_ai_code_assistants_extension/tests/test_descriptor_parity.py`
  - 2026-08-10 reported: found by the round-6 impact agent scoping the `DEF-75` fix, and measured on the spot rather than argued from the tsc documentation
  - 2026-08-10 deferred in code, CAUSE fixed instead: scope is the LOCAL loop only - `.github/workflows/build.yml` runs `pip install .[test]`, which builds `lib/`, before pytest, so CI cannot reach this state. The candidate code fix is to read `program.affectedFilesPendingEmit`, a PRIVATE TypeScript structure whose rename would degrade to a silent green - the same trade that produced this entry, taken a second time. So the command was changed instead of the guard: the verification step in `.claude/plans/fix-wave-v1.0.0.md`, `close-v1.0.0-final.md` and `close-adversarial-defects-v1.0.0.md` now names `jlpm build:lib` (`tsc --sourceMap`), which emits and genuinely refreshes `lib/`, in place of `npx tsc --noEmit`
  - 2026-08-10 closed via cause: the disarming command is gone from every verification list - the three plan files instruct `jlpm build:lib`, the close-19 campaign specs banned `npx tsc --noEmit` outright, and CI builds `lib/` before pytest so the state is unreachable there. The in-code fix would read `affectedFilesPendingEmit`, a private tsc structure whose rename degrades to a silent green - the same trade that produced this entry, declined a second time
- [x] `DEF-80` **The core-neutrality guard's comment stripper truncates at a `//` inside a string** - MINOR; `stripComments` cuts each line at the first `//` with no string-literal awareness (`src/__tests__/core-neutrality.spec.ts:88`), so nine lines are truncated before the scan ever sees them - all nine in `src/core/icons.ts` (15, 24, 33, 42, 60, 69, 80, 91, 104), every one the `http://www.w3.org/2000/svg` namespace inside an SVG template literal. None of them hides a violation: a grep of `icons.ts` for any assistant name returns nothing at all; `src/__tests__/core-neutrality.spec.ts`
  - 2026-08-10 reported: reported by the slop-hunter adversary (whole-repo round 6, MINOR) as a hole in the architecture's one enforced invariant, and narrowed by its impact agent to a hole with nothing behind it - the truncated region is nine namespace URLs
  - 2026-08-10 deferred: closing it means a ~30-line character scanner tracking quote and template-literal state, inside a guard that would then have nothing new to catch. Recorded here with the adjacent bug that IS real and is not this one: the block-comment strip on the line above (`:87`) deletes the newlines it removes, so a genuine violation in `panel.ts` would be reported about 141 lines off its true position - measured at 2637 lines before stripping and 2496 after. That one costs a reader real time on the day the guard finally fires, and it is a one-line change, but it is a change to a guard nobody is currently reading and it stays out of a two-line wave
  - 2026-08-10 fixed, the real half only: the block-comment strip substitutes `'\n'.repeat(newlines in the match)`, preserving line geometry - a violation planted at line 2686 of `panel.ts` is reported at 2686 where the old logic said 2541, the 145-line skew this entry measured. One regression test added inside the existing 'comment-stripping' describe block (20 tests, was 19); mutation-checked with a byte-identical restore. The `//`-inside-string truncation stays UNBUILT per this entry's own ruling - the nine truncated `icons.ts` lines re-verified as namespace URLs with nothing behind them
- [x] `DEF-81` **`isRequestTimeout` has no production consumer** - MINOR; `src/core/request.ts:57` exports a predicate that nothing outside `src/__tests__/request.spec.ts` calls. Only the PREDICATE is unused - `RequestTimeoutError` is constructed and thrown at `request.ts:120` and its message reaches the user through `_showError`, so the timeout surface `DEF-72` added is live; `src/core/request.ts`
  - 2026-08-10 reported: reported by two round-6 adversaries as dead code covering the whole timeout surface, which is false and was corrected by the impact agent - the error type, the throw and the user-visible message all have callers; the predicate alone does not
  - 2026-08-10 deferred: both available moves are worse than leaving it. Deleting it removes the only ready-made hook for the distinction `DEF-72`'s own note argues is necessary - a caller that cannot tell "the server is slow" from "the server is gone" cannot report either honestly. Wiring it into `_showError` adds a second string to the most-executed error path in the panel while `DEF-76` has that same copy open. Resolve as one deliberate pass over `_showError`, `_showActionError` and `_notifyLaunchError` together, which is the same pass `DEF-76` names
  - 2026-08-10 fixed: in the same pass, `isRequestTimeout` gained its production consumer - `_showError` (`panel.ts:616`) branches on it, answering a timeout with 'The server is not answering - retrying.' (reached but slow) while the non-timeout copy keeps 'Could not reach the server - retrying.' (gone). The predicate itself stays pinned by the pre-existing `request.spec.ts` assertions (:93, :142, :184); the branch copy has no reddening test, `panel.ts` being outside Jest's reach - stated rather than papered over. `_showActionError` and `_notifyLaunchError` copy untouched, diff-confirmed against the pre-edit baseline
  - 2026-08-10 residual, logged not fixed (round 8 architect, MINOR): on the timeout path the banner renders the friendly copy PLUS the `RequestTimeoutError` message - two statements of the same fact. Same stacking pattern as the non-timeout copy, so it stays; fold when `_showError` is next touched

- [x] `DEF-83` **The `DEF-73` re-entrancy guard hand-freezes a method list it could derive** - MINOR; the guard scans `colour.ts` for `this.load(`, `this.set(` and `this.forget(` from a literal array, which is the `DEF-58` failure repeated one wave later - a fourth queued method would pass unscanned. Not a silent hole in practice: injecting `await this.inherit('rose', 'x')` into `load`'s op leaves the guard green but reddens 7 of the 38 runtime colour tests by timeout, which is the deadlock itself; `src/__tests__/colour.spec.ts`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 6, MINOR) and the escape route measured by the bug-hunter adversary in the same round
  - 2026-08-10 deferred: the derivation would read the queue's membership out of the source it is scanning, which is circular, and the runtime suite already fails on the case the guard would miss. One of nine round-6 survivors that the triage wave left with no id; given one here after a methodologist audit of the run named the omission
  - 2026-08-10 closed by ruling: covered by the runtime suite. Deriving the scanned method list from the source being scanned is circular, and the measured escape - a fourth queued method calling into the chain - already reddens six runtime colour tests to jest's timeout, which is the deadlock itself, observed. The guard is a fast-failure convenience over a suite that catches the real thing
- [x] `DEF-84` **`REQUEST_TIMEOUT_MS` is bound to `codex.py`'s `_CLI_TIMEOUT_S` by comment alone** - MINOR; the 60s client ceiling is chosen as "a full CLI timeout above" the server's 30s cap and the reasoning lives only in prose, so lowering `_CLI_TIMEOUT_S` leaves the justification silently false; the jest guard restates the Python number as a TypeScript literal rather than reading it; `src/core/request.ts`, `jupyterlab_ai_code_assistants_extension/providers/codex.py`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 6, MINOR)
  - 2026-08-10 deferred: binding them needs a third cross-runtime guard, and the guard chain (`DEF-60` to `DEF-75` to `DEF-79`) is what this campaign stopped adding to
  - 2026-08-10 fixed: `tests/test_request_ceiling.py` binds the pair - regexes `REQUEST_TIMEOUT_MS` out of `request.ts` source (numeric separators handled), imports `_CLI_TIMEOUT_S` from `providers.codex` as a module import rather than regexing Python it can import, and asserts `>= 2 * _CLI_TIMEOUT_S * 1000`, with the docstring naming this entry and why the ratio is two. The not-found path calls `pytest.fail`, never skip - the vacuous-guard shape this ledger keeps burying. Both mutations verified: `30_000` reddens the ratio test naming both constants; RENAMING the constant reds both tests through the fail path, 0 skipped. Restores byte-identical. A source-reading guard in the repo's established pattern (`core-neutrality.spec.ts`, the `DEF-73` block), not a new cross-runtime guard-chain link
  - 2026-08-10 note (round 8 architect, taste): the deferral rationale above - 'a third cross-runtime guard, and the guard chain is what this campaign stopped adding to' - was OVERRULED by the close-19 ruling, not found inapplicable. The fix IS a guard of that kind, accepted because it is source-reading, fail-loud and mutation-checked
- [x] `DEF-85` **A third copy of the recentLimit bounds lives in the schema generator** - MINOR; `DEF-71` named the bounds in `panel.ts` and imported them into `index.ts`, but `scripts/generate-schema.mjs` states 1 and 100 again as its own literals; `scripts/generate-schema.mjs`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 6, MINOR); CONTESTED - one of the two skeptics refuted it on the grounds that the generator is a build script outside the TypeScript program and cannot import from `src/`
  - 2026-08-10 deferred: the skeptic is right that no import is available, so closing it means a generated constants file, which is machinery for a duplicated pair of integers
  - 2026-08-10 fixed: the single home is `src/core/limits.ts` - import-free so plain Node can load its compiled `lib/core/limits.js`, which `generate-schema.mjs` now imports exactly as it imports the provider barrel, writing DEFAULT/MIN/MAX into the `recentLimit` entry. `panel.ts` and `src/index.ts` import from it; `schema/plugin.json` stayed byte-identical (md5-matched) - the values did not change, their home did. Mutation-checked live: `MAX_RECENT_LIMIT = 50` flowed through rebuild + regenerate to `"maximum": 50` in the schema and back to 100 on a byte-identical restore, proving the generator genuinely reads the module. Residual literal, logged not fixed: the schema description's prose 'displays up to 10 rows' still hard-codes the number
- [x] `DEF-86` **Two of `DEF-71`'s exported constants have no importer** - MINOR; `MIN_RECENT_LIMIT` and `MAX_RECENT_LIMIT` are exported from `src/core/panel.ts` and used only by the clamp in that same file, so the export widens the surface without a consumer; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the slop-hunter adversary (whole-repo round 6, MINOR)
  - 2026-08-10 deferred: un-exporting is a one-word change with no risk, and is held only because it is indistinguishable from the edge fixes this project's ledger shows generating regressions. Resolve with `DEF-85` if the bounds ever get a single home
  - 2026-08-10 fixed: with `DEF-85`'s single home, `MIN_RECENT_LIMIT` and `MAX_RECENT_LIMIT` have real importers outside their defining file - `panel.ts` (the clamp) and `generate-schema.mjs` - and `panel.ts` no longer exports them, so no unimported export remains. The `DEF-85` mutation check is the live proof the generator's import is real rather than decorative. No re-export shim was left behind: the only external importer was `src/index.ts`, repointed
- [x] `DEF-87` **`DEF-63`'s settings-dir plumbing is killed by no test** - MAJOR; `routes.py` passes `_user_settings_dir()` into `migrate.migrate` and `migrate` accepts it as a parameter, but both ends can be severed and `pytest` stays at 163 green - so the fix that made the retirement migration read the CONFIGURED settings directory instead of JupyterLab's default is unguarded, on the one path whose whole job is a one-shot migration of the retired extensions' state; `jupyterlab_ai_code_assistants_extension/core/routes.py`, `core/migrate.py`
  - 2026-08-10 reported: reported by the slop-hunter adversary (whole-repo round 6, MAJOR), which severed both ends and observed the suite stay green
  - 2026-08-10 deferred: this is the strongest of the nine survivors the triage wave dropped, and the reason it was dropped does not hold up - a test here is coverage of an existing fix rather than new machinery. Held only because the migration modality has never been executed end to end (see the round-6 completeness critic), so a unit test asserting the parameter reaches the reader would certify a path nothing has ever run
  - 2026-08-10 fixed, rehearsal first: the migration ran END TO END for the first time, through the HTTP route against a real retired `jupyterlab_claude_code_extension` tree. Observed: the route answered claude with `providers.claude.dangerouslySkipPermissions: true` and the carried favourite `/home/lab/projects/demo`; codex and kimi answered with empty keys; gemini is ABSENT entirely - no LegacySource, skipped rather than recorded; the seeded `recentLimit: 25` was dropped (unmapped); and the value returned was the CONFIGURED folder's `true`, not the default folder's `false` - the plumbing observed live. Committed as `tests/test_migrate_end_to_end.py` (2 tests): the mapped keys land, and severing the `_user_settings_dir()` argument reddens `test_the_route_reads_the_configured_settings_dir` while NO pre-existing test reddens - exactly the gap this entry named. Subtlety recorded for the next reader: both `routes.jupyter_config_dir` and `migrate.jupyter_config_dir` must be patched, because the route always hands its own answer down, making migrate's default unreachable while the plumbing is intact
- [x] `DEF-88` **`DEF-41`'s rendered proof covers the branch badge only** - MINOR; the browser test asserts `.jp-AiAssistantsPanel-branchBadge` sits inside its row, but the defect was written about the branch badge AND the background-agent chip, and `ui-tests/jupyter_server_test_config.py` answers the `agents` route with `[]`, so no bg chip is ever rendered in the suite. The closure note credits the pixel claim as settled for both; `ui-tests/tests/panel-regressions.spec.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 6, MINOR); CONTESTED - one skeptic refuted it, one confirmed the fixture returns an empty agents list
  - 2026-08-10 deferred: seeding a background agent means a fourth fixture shape in a config file that three suites share. The honest cost is that `DEF-41` is closed for one of its two surfaces, which is now recorded here rather than implied by the closure note
  - 2026-08-10 fixed: the fixture's stubbed `claude agents --json` answers exactly one live background agent, owning the WIDE project's newest conversation - deliberately not branchy's, whose click semantics three other specs drive (a bg-owned conversation attaches instead of resuming). The roster entry carries the server's own pid so the liveness filter keeps it. New `panel-regressions` spec asserts the `jp-AiAssistantsPanel-bgBadge` chip inside its row - rendered and green in the Galata 27/27 run at v1.0.11 (`logs/galata-v1.0.11.log`), the first browser rendering of the chip, closing `DEF-41`'s second surface. Caveat: eslint ignores `ui-tests/` so the lint leg was vacuous - the proof is the suite run
- [x] `DEF-89` **`DEF-40`'s rendered proof asserted a floor that could not fail** - MINOR; the browser test read `expect(box.width).toBeGreaterThanOrEqual(14)` under a comment reading "at the size of its siblings, not smaller", while `style/base.css:75` pins `.jp-AiAssistantsPanel-iconButton svg` to exactly `14px` - so the 13x13 glyph the defect was about would render 14x14 and satisfy the assertion. A test that cannot fail is worse than no test: it is a green light nobody can revoke, and the ledger cited it as the browser tier settling the claim; `ui-tests/tests/panel-regressions.spec.ts`
  - 2026-08-10 reported: reported by the ux-designer adversary (whole-repo round 6, MINOR), dropped by the triage wave without an id, then confirmed independently by a methodologist audit of the run and verified first-hand against `style/base.css`
  - 2026-08-10 fixed: the assertion now measures a LIVE sibling - the second icon button in the same header - instead of a constant, so it fails if the warned glyph renders smaller than its neighbours, which is what the defect was about. The misleading comment is replaced by one stating why the constant was wrong. The single proof in the browser tier that was built without a mutation check is the single one that was vacuous

- [x] `DEF-90` **`core/state.py` swallows two `OSError`s with no logger anywhere in the module** - MINOR; `:136` and `:153` are both `except OSError: pass`, and unlike `core/migrate.py`, `core/registry.py`, `core/colour_store.py` and all four providers the module never calls `getLogger`. A failed pin write silently loses the current-conversation marker, so the row snaps back to recency ordering and the user sees a switch that appears to have worked and did not - the same silent-failure shape `DEF-66` closed in `colour_store.py`, and worse, because a lost pin changes what the panel shows; `jupyterlab_ai_code_assistants_extension/core/state.py`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 7, MINOR), which also disproved `DEF-66`'s title in the process - `colour_store.py` was not the one such module
  - 2026-08-10 deferred: the remedy is a logger plus two `warning` calls, keeping the swallow, which is correct - best-effort is documented and intended here. Held under the standing rule that a MINOR is logged rather than fixed, and because `DEF-66`'s own fix is the precedent for what this costs: it was the guard-chain pattern's first link
  - 2026-08-10 fixed: `_log = logging.getLogger(__name__)` declared as `migrate.py` and `colour_store.py` declare theirs, and both swallowed `OSError`s warn naming the provider, the state file path and the error - the swallow KEPT, best-effort being documented and intended, no docstring touched. Two caplog tests in `tests/test_state.py` in the `DEF-66` precedent style; mutation-checked - restoring the bare `except OSError: pass` reddens exactly the two new tests (`assert 'no space' in ''`), the fixed file restored byte-identical (md5-matched)
- [x] `DEF-91` **The saved-settings file is re-parsed four to five times per request** - MINOR; `_user_settings()` (`core/routes.py:204`) globs the user-settings directory and regex-strips the JSONC on every call, and `_enabled_providers()` calls it once per provider through `_provider_enabled`, so `StatusHandler.get` parses it four times and `TerminalHandler.get` five. Twelve lines below, `routes.py:373` does the opposite and says so in a comment - "Read once for the whole listing, not once per row". Multiplied by four panels polling and the 30s colour loop; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 7, MINOR), counted with an instrumented `load_jsonc` rather than estimated
  - 2026-08-10 deferred: the remedy is an optional `settings` parameter on `_provider_enabled` read once in `_enabled_providers()` - no cache, no state, removes three of five reads. Held because it is a performance finding with no reported symptom, on the request path four panels share, in a wave already carrying a packaging fix and a parity assert
  - 2026-08-10 fixed: `_provider_enabled` takes an optional `settings` dict (None reads `_user_settings()` exactly as before), and both `_enabled_providers()` and `StatusHandler.get`'s roster read once and pass down - StatusHandler iterates the registry directly rather than through `_enabled_providers`, so the same optional-parameter mechanism was applied there too. Measured through a real GET /status with counter instrumentation: 4 parses before, 1 after, invariant to provider count; a settings file with `codex.enabled=false` still answers per provider. The instrumentation test was temporary and deleted. Gap logged, not fixed: no permanent test asserts the status route's `enabled` VALUE (mutating it to a literal survives) - proven pre-existing against the pre-wave `routes.py`
- [x] `DEF-92` **`namingStrategy` is a three-valued union of which the core reads two states** - MINOR; `src/core/types.ts:29-35` declares `'launch-flag' | 'server-side' | 'none'` with a documented paragraph each, and the only reader anywhere in the core is `src/core/panel.ts:907` testing `!== 'none'`. Where the branch name actually goes is decided by `forkStrategy` at `panel.ts:930-940`, so declaring `'launch-flag'` on a `server-copy` provider changes nothing and no parity test would catch it; `src/core/types.ts`
  - 2026-08-10 reported: reported by the architect adversary (whole-repo round 7, MINOR) as over-engineering rather than taste - nothing breaks if the union is deleted
  - 2026-08-10 deferred: collapsing it to `promptsForBranchName: boolean` touches the type, its doc block, `NAMING_STRATEGIES` in `src/__tests__/registry.spec.ts:68` and one line in each of four provider modules. Six files to delete a distinction nothing reads is exactly the remedy radius this campaign's ledger shows generating regressions
  - 2026-08-10 fixed: collapsed to `promptsForBranchName: boolean` - claude/gemini/kimi true, codex false - documented as whether the branch flow asks the user to name the new conversation, the destination being `forkStrategy`'s job, which is the confusion the union created. `grep -rn namingStrategy src/` returns nothing; TypeScript-only - no Python descriptor carried the field, parity untouched. Mutation checks reported honestly: flipping codex's VALUE stays green - no test pins per-provider values, a PRE-EXISTING state (the old membership check passed any of the three values too), and no test was invented to manufacture a red; flipping the `panel.spec.ts` testbed descriptor reddens the five `DEF-31` fork tests, proving the reader live. Jest 121/121, `jlpm build:lib` exit 0

- [x] `DEF-93` **Every refusal this server authors reaches the user as the literal string `[object Object]`** - MAJOR; `request.ts` extracted the error message as `data.message || data`, and all 14 refusal sites in `core/routes.py` write `{"error": "<code>"}` with no `message` key - so the parsed object itself became the `Error` message and stringified to `[object Object]` in every toast and banner fed by it. Tornado's own 500s carry `message` and rendered fine, which is why it survived six rounds: the messages that broke were exactly the ones this extension chose to send. `DEF-53` and `DEF-81` are both premised on the message being readable; `src/core/request.ts`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 7, MAJOR), proven against the real `ServerConnection.ResponseError` class under node
  - 2026-08-10 fixed: `data?.message ?? data?.error ?? data` - one clause, fixes all 14 server sites and every frontend surface, touches no route. Verified: the same node reproduction now prints `remove_failed`
- [x] `DEF-94` **The launch pre-flight walks the project's conversations on the IOLoop** - MAJOR; `LaunchHandler.post` called `provider.store.project_session_ids(encoded_path)` inline while wrapping the very next store call in `run_in_executor` - linear in the project's conversation bytes and paid on every row click, measured at a 191ms whole-server freeze against a 20-conversation Gemini store with a canary coroutine on the same loop. Disproves `DEF-82`'s already-corrected text a second time: this was the site its "seven call sites" enumeration missed, and unlike the migration route it is N-scaling; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 7, MAJOR), measured twice - real store and a 1.5s sleep control
  - 2026-08-10 fixed: the pre-flight now awaits the same executor the argv call uses; the `loop` binding is hoisted above it and the duplicate binding below removed. pytest 163 unchanged
- [x] `DEF-95` **A hung server grows the colour queue without bound - the poll enqueues at 1/30s, the queue drains at 1/60s** - MAJOR; `_fetch` had no in-flight guard, so against an unanswering transport the 30s poll enqueued a `load()` behind every previous one while the 60s request ceiling drained half as fast - measured at 1000x compression: 20 polls, 21 requests, and a user's capture waiting 668 virtual seconds. For the backlog's whole length `isPending` stayed true, which withheld the release menu affordance and made `_inheritColour` await the entire queue - a fork's terminal opened and the branch never surfaced. `DEF-30`'s note priced K queued ops as a given; nothing bounded K; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 7, MAJOR), measured against the shipped `lib/core/colour.js`
  - 2026-08-10 fixed: the poll skips its tick while the previous one is unanswered - one `_polling` promise field, cleared in `finally`, read only by the poll so an explicit Refresh still always issues. Caps the backlog at 1 without touching `ColourStore`
- [x] `DEF-96` **Clean Up Parallel Sessions answers "Cleanup failed" when there is nothing left to clean** - MINOR; the menu item is gated on a row up to one poll stale, and `_cleanupParallel` refetched the branch list and sent whatever came back - including `session_ids: []`, which every store refuses, so a second browser tab cleaning the same project got a confirm dialog promising to remove N and a modal ending in a failure. Measured live: `400 {"error": "remove_failed"}`; `src/core/panel.ts`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 7, MINOR), driven against a live server with two clients
  - 2026-08-10 fixed: `_cleanupParallel` returns early on an empty refetched list with the modal reading "Nothing left to clean up.", clearing the branch cache and refreshing so the menu gate catches up
- [x] `DEF-97` **Two of the three per-file metadata caches have no cap; the third does** - MINOR; `claude.py` caps `_tail_cache` at 1024 entries and clears wholesale, while `gemini.py`'s `_meta_cache` and `kimi.py`'s `_message_count_cache` grow one entry per chat file ever statted, including deleted conversations whose key can never hit again. Honest sizing: single-digit MB at ten thousand conversations - the cost is the asymmetry, not the memory; `jupyterlab_ai_code_assistants_extension/providers/gemini.py`, `providers/kimi.py`
  - 2026-08-10 reported: reported by the bug-hunter adversary (whole-repo round 7, MINOR), grep-verified against the capped sibling
  - 2026-08-10 deferred: the remedy is the sibling's own three lines in each file, held under the standing rule that a MINOR with no reported symptom is logged rather than fixed
  - 2026-08-10 fixed: the capped sibling's exact three-line shape applied to both - `_META_CACHE_MAX = 1024` with clear-before-insert in `gemini.py:read_chat_meta` and `_MESSAGE_COUNT_CACHE_MAX = 1024` in `kimi.py:_message_count`, comment sentence copied from `claude.py`. No cache test exists anywhere in the suite - the capped sibling `_tail_cache` is untested too - so verification is code-trace plus a throwaway out-of-tree probe: each cache pre-filled to 1024 dummies cleared to 1 entry on the next insert, counts still correct. Stated rather than faked: no test reddens on removing the cap
- [x] `DEF-98` **The conda-installed copy of the server extension is stale against the tree** - MINOR; `/opt/conda/lib/python3.13/site-packages/jupyterlab_ai_code_assistants_extension` differs from the repo in 11 source files and lacks two test modules, so any measurement or Galata venv built from that copy tests older code - the same false-confirmation shape the campaign hit twice before (`DEF-79`, and the 07:53 venv assets). The round-7 bug-hunter had to force `PYTHONPATH` at the repo for every measurement; environment, not product
  - 2026-08-10 reported: flagged by the bug-hunter adversary (whole-repo round 7) as an environment note; logged because it is the third appearance of this failure shape in one campaign
  - 2026-08-10 deferred: refreshing it is one `pip install` but bumps nothing and fixes nothing shipped; do it whenever the conda env is next used for measurement
  - 2026-08-10 closed in the environment: `make install` at v1.0.11 refreshed the conda site-packages copy, and the Galata venv - found still carrying 1.0.8, this entry's exact failure shape, minutes before the rendered gate - was force-reinstalled from the 1.0.11 wheel. Both copies now match the tree; every campaign measurement ran with `PYTHONPATH` forcing the repo throughout
- [x] `DEF-100` **A parallel lane's mutation-check restore overwrote another lane's landed edits, and the ledger certified the lost fix** - MAJOR; the close-19 workflow partitioned lanes by file, but the guard lane's `DEF-80` mutation check had to plant its violation in the real `src/core/panel.ts` (the guard resolves `src/core` at run time and cannot be pointed at a copy), and its restore raced the TypeScript lane's sequential edits: the backup was taken mid-lane, so restoring it silently removed the two `DEF-76` command-site hunks landed in between - AFTER their own lane had verified them. The guard lane recorded the anomaly ('a later cmp differed at line 1033') without either lane able to see whose bytes were lost; `DEF-76` was then closed on a verification that was true when it ran, and both round-8 adversaries caught the tree/ledger divergence
  - 2026-08-10 fixed: the two hunks re-landed byte-equal to their description and `DEF-76`'s note corrected in place. Process rule recorded for the next campaign: a mutation check that must touch a file OWNED BY ANOTHER LANE cannot run concurrently with it - such checks serialise after the owning lane, or the owning lane re-runs its grep inventory at the barrier. The gates could not catch this because no test asserts the lost strings, which is `DEF-76`'s own no-mutation-check-possible caveat coming home
- [x] `DEF-101` **One pin writer survived on the executor - `ClaudeStore.switch` pinned from the pool, alone among the four providers** - MAJOR; `SwitchHandler.post` runs `store.switch` in `run_in_executor` (`core/routes.py:504`) and claude's switch - uniquely; codex, gemini and kimi never pin in the store - called `state.write_pin` inside it (`providers/claude.py:872`), an unlocked load-modify-save of the same file `toggle_favourite` and the loop-side pin writes rewrite. Both round-9 adversaries found it independently; the bug-hunter reproduced the lost update 200/200 with a two-thread harness. It predates the close-19 wave (present at `ad1402a`), which is why `DEF-99`'s 'serialised by construction' sentence needed the round-9 correction; `jupyterlab_ai_code_assistants_extension/providers/claude.py`
  - 2026-08-10 fixed by REMOVAL, the adjudicated smaller of the reviewers' two remedies: the store-internal write and its cwd-eligibility condition deleted, so claude's switch does what its three siblings' always did - touch the mtime and leave the PIN to the route's loop-thread write at `routes.py:516`. Observable behaviour is byte-identical through the wire: the route has pinned unconditionally right after since the port, so the store's conditional write was a duplicate and its documented protection ('never pin a cwd-foreign branch over a valid pin') was unreachable - the intent is recorded here, and if it is ever wanted it belongs in the route as its own filed decision, not silently resurrected in a store. `test_claude_switch_pins_and_reresolves` realigned to kimi's route-writes-the-pin pattern and made failable (the sibling ages the rival transcript; this now does too). Stated honestly: no test reddens if the store write is reintroduced - the loop-owns-writes invariant is enforced by review and this entry, deliberately not by a new source-reading guard (the `DEF-60`..`DEF-79` chain is what that road builds)
  - 2026-08-10 precision note, self-caught: 'byte-identical through the wire' has one corner - when two transcripts share the same mtime SECOND, the switch response's `current` can name the rival until the route's pin lands one await later, where the old store-side pin forced `requested`. The realigned test flaked on exactly this tie (3-of-5 runs) until it aged the rival like the sibling tests do, which is how the corner was found. It is the behaviour codex, gemini and kimi have always had through this route, the panel refetches after a switch, and a real tie needs two conversations written in the same second - recorded, not defended
  - 2026-08-10 round-10 corrections, both lenses: (1) the fix note's cite is `routes.py:519` - the same wave's comment extension pushed the write three lines down; (2) codex does NOT share the tie corner - `CodexStore.switch` resolves as-if-pinned, so only gemini, kimi and now claude can answer `current` with the rival; (3) the tie window is wider than 'written in the same second' - any rival append between the executor's utime and the resolve (a streaming conversation, the canonical switch-away) also flips `current`, costing one transient panel toast that the post-switch refetch corrects; (4) 'aged the rival like the sibling tests do' was false for claude's OWN delete sibling - `test_claude_delete_never_touches_the_current_conversation` rode the store-side pin and flaked at majority rate once it was gone (architect 12/20, bug-hunter 6/10, adjudicator 2/6) - now aged the same way; (5) the switch-writes-the-pin causal link was covered in NEITHER direction - the realigned test wrote the pin itself, so dropping the route's one pin line left all 169 green; a new route test (`test_a_switch_writes_the_pin_on_the_route_side`) now reddens on exactly that line, mutation-checked
  - 2026-08-10 round-11 corrections: (a) the round-10 correction (3) above is itself WRONG about the mechanism, and the round-11 architect verified it 'true by mechanism' while the bug-hunter disproved it by probe - no rival activity is needed: the stores consult the PIN before recency, the route pinned only after building the response, so every switch in an already-pinned project answered the OLD pin deterministically. Superseded by `DEF-102`, which also repairs this entry's 'byte-identical through the wire' claim for good - the route now re-resolves after pinning. (b) The fix note names `test_claude_switch_pins_and_reresolves`, which this same wave renamed to `test_claude_switch_touches_and_the_route_side_pin_outlives_recency` - recorded so a ledger grep finds the live name. (c) Logged, not fixed (round-11 architect, MINOR): the loop-owns-writes comment stands at five write sites and is absent at four - all six uncommented writes ARE on the loop, so this is signage, and the one load-bearing spot if ever added is the BranchHandler write that sits right after an executor fork. (d) SUSPICION, recorded unresolved: one full-suite run in ~139 answered `1 failed, 169 passed` without the failing test's name being captured, and 136 further runs (including parallel-load and lastfailed-cache runs) never reproduced it - the settling instrument is CI keeping its lastfailed cache until the 1-in-100 fires again and names itself
- [x] `DEF-102` **A pinned project's every next switch answered the OLD pin as `current`, and the panel toasted a failure for a switch that succeeded** - MAJOR; the stores resolve the pin BEFORE recency and `SwitchHandler.post` wrote the new pin only AFTER the store had built its answer, so from the second switch onward the response was `{"requested": B, "current": A}` deterministically and `panel.ts` read `current !== requested` as 'That conversation could not be made the current one.' - a spurious warning on the steady-state path, self-corrected visually by the refetch but toasting every time. Pre-existing for kimi and gemini since the port (their stores never pinned); claude was immune because its store pinned before resolving, and JOINED them for the hours `DEF-101`'s deletion was in the working tree - the wire regression the round-11 bug-hunter caught and proved by probe against both stores, zero rival activity involved. Codex was never affected - its switch resolves as-if-pinned; `jupyterlab_ai_code_assistants_extension/core/routes.py`, `src/core/panel.ts`
  - 2026-08-10 reported: found by the round-11 bug-hunter adversary over the `DEF-101` remediation, against the round-11 architect's verification of the same ledger sentence - the deeper probe outranked the mechanism argument, and the adjudicator reproduced it before accepting it (probe: pin A, switch B, `current == A`)
  - 2026-08-10 fixed at the route, once, for all providers: after `state.write_pin` the handler re-resolves `current` through one pooled READ (`provider.store.resolve_current`), so the response reflects the pin it just wrote while still applying the store's own pin validation - a cwd-foreign pin still resolves away and the toast fires only when it is TRUE. Mutation-checked: severing the re-resolve surfaces the stale pre-pin answer in exactly `test_a_switch_writes_the_pin_on_the_route_side`, which now asserts the response's `current` alongside the pin; restore byte-identical, suite 170 green. Cost: one executor read per user-initiated switch. This closes the pre-existing kimi/gemini toast as well as claude's transient regression - the panel's `current !== requested` warning is truthful again on every path
- [x] `DEF-103` **The `DEF-102` route fix demoted every store's switch-side `current` to dead weight defended by two false contracts** - MAJOR; `SwitchHandler.post` overwrites `result["current"]` unconditionally after writing the pin, yet all four stores still computed it inside `switch()` - claude's by walking the project index on every click - while `core/store.py`'s abstract contract kept promising "`current` re-resolved after the write - the two differ when the requested conversation cannot become current" and `codex.py`'s docstring ordered that "the frontend depends on that disagreement... the pair must not be collapsed". Both claims false on the wire: zero production consumers of the store-level value (the route reads only `error`), exactly two test assertions pinning the dead pair; `jupyterlab_ai_code_assistants_extension/core/store.py`, `providers/claude.py`, `providers/kimi.py`, `providers/gemini.py`, `providers/codex.py`, `core/routes.py`
  - 2026-08-11 reported: round-12 architect (DO-NOT-SHIP over the `DEF-102` remediation, after verifying every `DEF-101`/`DEF-102` ledger claim true against the tree). The round-12 bug-hunter - SHIP with zero findings on the same tree - independently proved the codex half: routing codex through the route's post-pin `resolve_current` yields outcomes identical to its old as-if-pinned answer in every branch (husk target, all-husk project, steady state), so the two lenses corroborate rather than conflict
  - 2026-08-11 fixed by REMOVAL, the architect's own remedy: the `switch` contract slimmed to `{"requested"}` | `{"error": "branch_not_found"}` | None; the four store-side resolutions deleted (claude/kimi/gemini's trailing `resolve_current` call, codex's `_resolve_project_current(..., session_id)` plus its now-callerless `pin` override parameter, collapsed to the unconditional `self._pin`); both false docstrings rewritten truthful, and the `routes.py` comment's codex-contradicted universal ("the store resolved `current` BEFORE this pin existed") rewritten with them - subsuming the architect's comment MINOR. The wire is unchanged: the route still answers `{requested, current}`, `ISwitchResponse` and `panel.ts:1253` untouched, no TS file modified. Mutation-checked: severing codex's `branch_not_found` guard reddens exactly `test_codex_switch_answers_without_writing`, restore byte-identical (`cmp`). Gates: pytest 170 passed four times, the four switch tests 10/10 each in isolation (zero mtime-flake reds), prettier clean. Landed via workflow `wf_238ff696-3ba` (script persisted at `.claude/workflows/round12-remedy-v1.0.14.js`), zero deviations reported
- [x] `DEF-104` **The route switch test proves the `DEF-102` fix with both store halves mocked - no composed real-store wire test** - MINOR; `test_a_switch_writes_the_pin_on_the_route_side` monkeypatches kimi's `switch` AND `resolve_current`, so no test exercises the route-through-real-store composition, and its equality assert holds only under the fixture's fake resolve wired to the real pin - the fixture's contract, not a blanket claim; `jupyterlab_ai_code_assistants_extension/tests/test_routes.py`
  - 2026-08-11 ruled, logged not fixed: taste, per the round-12 architect's own severity. The mutation anchor holds (severing either the pin write or the re-resolve reddens exactly this test), the round-12 bug-hunter probed the real-store composition live in scratch (real `ClaudeStore` and `KimiStore`, route order, old-pin then re-resolved answers both observed), and a composed test would re-enter the mtime-tie flake family the aging discipline just settled - this campaign's ledger shows exactly such edge additions generating the next defect. Declined under the standing rule: MINORs are logged, not fixed
- [x] `DEF-105` **The `DEF-103` edit deleted two false docstrings and wrote a new one into kimi and gemini in the same pass** - MAJOR; the reworded switch docstrings (`providers/kimi.py:466-468`, `providers/gemini.py:668-670`) claimed the mtime touch "keeps the provider's own picker aligned and lets the route's post-pin re-resolution agree with the request instead of answering with the recency winner" - false by `DEF-102`'s own mechanism: both stores match the pin BEFORE recency against the same population their `switch` just validated membership in, so the route-written pin always resolves and the touch cannot influence the post-pin answer. The picker half was false too - gemini's own resume documentation says its picker sorts by start time, which an mtime touch does not move. Honesty trail: the false clause was AUTHORED BY THE ADJUDICATOR as example wording in the round-12 lane spec, and the lane copied it verbatim as instructed - the lane deviated nowhere; `jupyterlab_ai_code_assistants_extension/providers/kimi.py`, `providers/gemini.py`
  - 2026-08-11 reported: round-13 architect (DO-NOT-SHIP over the `DEF-103` remediation), proven by probe with the touch forcibly dead (`os.utime` raising): `current == requested` still held through the composed route order. The round-13 bug-hunter, SHIP with zero findings on the same tree, confirmed by mutation that dropping each touch DOES redden a test - the touch is live code with a then-falsely-stated reason, exactly the deletable-live-write licence `DEF-103` was raised over
  - 2026-08-11 fixed by deletion, adjudicator-verified against `_pick_current` in both files before editing: the false clause cut and replaced with the touch's one true job - "the touch matters only where no pin decides - it feeds the recency fallback for a cleared or dropped pin and the branch list's `file_mtime` ordering". Docstring-only, no code line changed, pytest 170 green
  - 2026-08-11 round-14 corrections, both lenses over this very fix: (a) the replacement clause was AGAIN false - "matters only where no pin decides" denied the touch's third and most visible consumer, the project row's `file_mtime` (sort, relative-time label, staleness classes), which fires WHILE the pin decides; both round-14 lenses proved it by independent probe (row order flipped with the touch live, static with it dead, `current` identical both ways). Superseded by `DEF-109`'s wording. (b) "the route-written pin always resolves" is overstated - an unwritable state directory takes `write_pin`'s logged `OSError` branch and the pin is simply absent, whereupon the touch carries the switch through the recency fallback; the true form is "resolves whenever it was written". (c) "the round-13 bug-hunter... confirmed by mutation that dropping each touch DOES redden a test" is FALSE for kimi and gemini - the adjudicator re-ran the mutation on this tree: severing BOTH touches leaves the suite 170 green, restore byte-identical; only claude's touch is test-pinned (`test_claude_switch_touches_and_the_route_side_pin_outlives_recency`). The round-13 table's "RED (3)" row did not reproduce and is hereby retracted for the kimi/gemini pair. (d) the header's line cites (`kimi.py:466-468`, `gemini.py:668-670`) now hold the replacement text, not the defect - a future grep lands on the fix
  - 2026-08-11 round-15 scoping of note (b): "the pin is simply absent, whereupon the touch carries the switch" holds only for a project with NO prior pin. Probed by the round-15 bug-hunter on real stores: when the project already carries one, the failed write leaves the pins FILE intact - `write_json_atomic` fails at its tempfile and the old file survives - so the STALE pin resolves and wins over the touch. The wild shape is disk-full: the user switches A to B, the panel accepts, and the row keeps showing A with nothing but a server-log warning. Pre-existing best-effort behaviour (`write_pin` logs and swallows, `DEF-90`'s class), recorded here as the disk-full residual rather than filed anew
  - 2026-08-11 round-16 correction, both lenses converging on the one clause: "the panel accepts, and the row keeps showing A with nothing but a server-log warning" was false - it describes the pre-`DEF-102` world. In the stale-pin case the route's post-pin re-resolve answers `current = A` against `requested = B`, and `panel.ts:1253` raises "That conversation could not be made the current one." on every switch path - the user IS told the switch failed; only the REASON (the errno) is confined to the server log. The false clause originated in the round-15 bug-hunter's finding text and survived the adjudicator's transcription; both round-16 lenses disproved it by probe using the campaign's own `DEF-102` contract, and `routes.py`'s own comment states it in the file the note annotates
  - 2026-08-11 round-17 correction, per-provider scopes added in round 18, closing re-homing sentence deleted in round 19 (the matrix as first written universalised kimi-measured cells - both round-18 lenses' four-provider probes supplied the scoping; the deleted sentence re-homed the round-15 clause to a cell where both round-19 lenses probed its "row keeps showing A" element false, the touch making the requested conversation the row's answer): "it describes the pre-`DEF-102` world" is retracted as false. The panel's `current !== requested` warning predates the campaign - present at HEAD `ad1402a` - and at HEAD a prior-pinned KIMI or GEMINI switch answered `current = A` even when the pin write SUCCEEDED - claude was immune (its store pinned before resolving) and codex never affected (as-if-pinned), per `DEF-102`'s own header, three entries above - so that world toasted more, not less, for the two providers this entry is about. Probe matrix - working tree: stale pin + write OK → `current = requested`, accepted (correct, all four); stale pin + write fails → warns (all four); no prior pin + write fails → accepted silently on claude/kimi/gemini, while codex - which touches nothing - warns truthfully, its recency winner never moving (`DEF-106`'s asymmetry through another route). HEAD: stale pin + write OK → warns on kimi/gemini (the `DEF-102` spurious toast), accepted on claude/codex; stale pin + write fails → warns on claude/kimi/gemini, accepted on codex; no prior pin + write fails → accepted silently (all four), there without even the server-log errno - `write_pin` was a bare `except OSError: pass` at HEAD, the logging being `DEF-90`'s working-tree fix
- [x] `DEF-106` **The slimmed abstract contract promises "its one recency-aligning write" while codex writes nothing** - MINOR; `core/store.py:286-287` universalised the touch across four implementors, and `providers/codex.py` says in the same tree "Nothing is written here" - the same false-universal class `DEF-103` fixed, one degree weaker, introduced by the same edit; `jupyterlab_ai_code_assistants_extension/core/store.py`
  - 2026-08-11 reported: round-13 architect (MINOR)
  - 2026-08-11 fixed: "plus, where the provider has one, a recency-aligning touch" - a wording-only edit, no code
- [x] `DEF-107` **`GeminiStore.switch` has zero test callers - one of the four implementations of the changed contract is unpinned** - MINOR; grep over `tests/` finds claude, codex and kimi switch calls but none for gemini, so its `{"requested"}` return and its `os.utime` are asserted nowhere. Distinct from `DEF-104` (route-through-store composition); this is store-level coverage; `jupyterlab_ai_code_assistants_extension/tests/test_provider_stores.py`
  - 2026-08-11 ruled, logged not fixed: per the architect's own recommendation and the standing rule. A gemini mtime test would re-enter the mtime-tie flake family the aging discipline just settled, and the round-13 bug-hunter's real-store probe exercised gemini's composed switch green. Logged because it is why `DEF-105` had teeth - false rationale plus zero coverage is deletable live code
  - 2026-08-11 round-14 note: the coverage picture is starker than filed - kimi's and gemini's touches are under mutation cover NOWHERE (severing both leaves 170 green, adjudicator-verified), not merely uncalled for gemini. The ruling stands unchanged, now on the honest ground: the touches' liveness is the probed row-ordering effect (`DEF-109`), and a new mtime test re-enters the tie-flake family
- [x] `DEF-108` **The switch success payload is a pure echo split across the boundary** - MINOR, taste; all four stores return `{"requested": session_id}` and the route overwrites `current`, so the store contributes a key the route already holds validated, and a hypothetically nonconforming store returning `{}` would ship `requested: undefined` past the error-only check; `jupyterlab_ai_code_assistants_extension/core/routes.py`
  - 2026-08-11 ruled, do nothing - the architect's own recommendation at filing: collapsing it (route builds the whole payload, ignores the store result after the error check) touches six files and two tests to remove four lines and no behaviour - the remedy costs more than the defect. Recorded so the next round does not re-derive it as new
- [x] `DEF-109` **The `DEF-105` replacement wording was false again - "only where no pin decides" denied a live effect, and the summary line asserted the retracted mechanism** - MAJOR; the rewritten kimi/gemini switch docstrings claimed the touch "matters only where no pin decides" while the touch feeds the project row's `file_mtime` - which the panel sorts by, renders as the row's relative time, and derives the recently-active/stale classes from - and that fires with the pin in force on every switch. Both round-14 lenses proved it by independent probe: the switched project's row jumped to the top with the touch live and stayed put with it dead, `current` identical both ways. The architect added that the summary line still read "Make `session_id` current by touching its mtime" - precisely the mechanism the paragraph below it retracts. Second consecutive round in which the MAJOR is a falsity in prose the adjudicator authored the round before - the rewrite-then-flag spiral the review-loop rule names - legitimate here only because both findings were probed facts, not taste; `jupyterlab_ai_code_assistants_extension/providers/kimi.py`, `providers/gemini.py`
  - 2026-08-11 reported: round-14 architect and bug-hunter independently (both DO-NOT-SHIP), converging on the same probe result from separate scratch harnesses
  - 2026-08-11 fixed, wording chosen to claim the minimum: summary lines now "Validate `session_id` and stamp its ... mtime"; the paragraph names the route's pin as what decides `current` and lists the three probed consumers of the touch (project-row `file_mtime` and ordering, branch-list ordering, recency fallback when no pin resolves) with no exhaustiveness claim and no "only". Folded into the same pass, per the architect: the single pin-writer is now named "the route" in all five docstrings that mention it (`codex.py` said "the core", `store.py` said "the ROUTE"), and the `store.py` paragraph the `DEF-106` edit left ragged (a 26-character mid-paragraph line) re-flowed. Docstring-only, zero code lines, pytest 170 green
  - 2026-08-11 round-15 corrections and rulings: (a) the fix note's "all five docstrings that mention it" undercounts - the unified five are the SWITCH docstrings; two `_pick_current` docstrings (`kimi.py:342`, `gemini.py:516`) also mention the pin, as "the core's", and stand untouched deliberately: there the sentence states OWNERSHIP (the pin lives in core state, written on a switch or a fork), not which handler writes it, and `write_pin` does live in `core/`. Both round-15 lenses converged on this same one-word scoping; neither demanded a docstring edit. (b) adjudicator rulings, recorded against re-derivation: the abstract summary "Make `session_id` the project's current conversation" (`core/store.py:284`) stays - the round-14 architect read it as the operation's intent and declined to file it, the round-15 architect filed it as MINOR; the intent reading stands, the store/route split being stated three lines below it. The `except OSError` comment's "only the recency alignment is lost" (`kimi.py`, `gemini.py`) stays - the round-15 architect's own judgement is that it is vague under one vocabulary and true under the natural mtime reading, and it is pre-existing wording outside the campaign's delta
- [x] `DEF-110` **Claude's touch rationale names only an unverified external effect and omits the verified in-repo ones** - MINOR, suspicion; `providers/claude.py:849-850` justifies the switch's mtime touch solely as "so Claude's own `--resume` picker stays roughly aligned" - the same external-picker rationale class `DEF-105` killed in gemini as false - while the touch's three verifiable in-repo consumers (pin-before-recency fallback, project-row sort, branch `file_mtime`) go unstated. Unlike gemini, no in-repo document contradicts the claude claim, and claude's touch IS test-pinned; `jupyterlab_ai_code_assistants_extension/providers/claude.py`
  - 2026-08-11 ruled, logged not fixed: pre-existing wording from the port, outside the round-13/14 delta, and the claim is unverifiable from this repo - the settling instrument is a live probe with the claude CLI (touch one transcript in a scratch store, observe whether `claude --resume`'s list re-orders), recorded here for whoever next has the CLI at hand. Both round-14 lenses raised it; neither demanded an edit

- [x] `DEF-111` **Gemini exposes two approval controls where every other provider has one** - MAJOR, user-reported; the settings page carried both `Gemini: YOLO mode` (boolean, `--yolo`) and `Gemini: Approval mode` (four-rung enum, `--approval-mode default|auto_edit|yolo|plan`), with a precedence rule (enum beats boolean, forced menu action beats both) that existed only to referee between them; `src/providers/gemini.ts`, `jupyterlab_ai_code_assistants_extension/providers/gemini.py`
  - 2026-08-12 reported: Star Colonel - "two instead of one, let's make it easy, just one, like the others"
  - 2026-08-12 fixed (v1.0.22): the enum is deleted, `yoloMode` stays. `src/providers/gemini.ts` drops the `approvalMode` launchMode; `providers/gemini.py` drops `APPROVAL_MODES` and the `--approval-mode` argv branch; with no enum provider left the enum machinery dies with it per house precedent - `ILaunchMode` loses `kind`/`values`/`unsafeValues` (`src/core/types.ts`), the resolver's enum-precedence branch collapses to "force wins, else the first switch that is on" (`src/core/modes.ts`), the schema generator's enum branch goes (`scripts/generate-schema.mjs`), and the descriptor-parity flattening compares bare mode ids. `auto_edit` and `plan` are no longer settable - accepted by the user. A stale `providers.gemini.approvalMode` key in saved settings is ignored by the registry's unknown-key path, and a stale `approvalMode=...` token sent to the server is dropped without reaching the CLI (pinned in `test_provider_stores.py`). Jest 118 (121 - 3 enum tests), pytest 170, Galata 28/28 at v1.0.22
- [x] `DEF-112` **The header new-session button wears the mode's shield instead of +** - MAJOR, user-reported; with a skip-permissions mode armed, the panel's `+` button repainted to the shield (DEF-35's "the mode's glyph wins over the action's own"), so the one always-visible launch surface stopped looking like the add button it is; `src/core/panel.ts`
  - 2026-08-12 reported: Star Colonel - "it must show +, just not menu then"; with the mode off the + drops the normal/variant menu as before
  - 2026-08-12 fixed (v1.0.22): the header button paints `addIcon` unconditionally and the `_repaintNewIcon` hook dies with the conditional (the title still names the armed mode - DEF-36 untouched). The shield keeps its menu-entry home: forced variants, the flattened branch entry, and `Resume (Skip Permissions)` in the row menu, the last now carrying the shield's rendered Galata proof (identified by path data, sized against a live sibling). Unit DEF-35/38 glyph test became DEF-112's always-+ assertion. Jest 118, Galata 28/28 at v1.0.22, rendered proof screenshotted on the 8931 test server

- [x] `DEF-113` **The header button title names the provider its own panel tab already names** - MINOR, user-reported; the + tooltip read "New Claude Code session in the current folder" inside the panel titled "Claude Code Sessions" - the provider word is redundant on the one surface most confined by it; `src/core/panel.ts`
  - 2026-08-13 reported: Star Colonel - "not 'new claude code session' text - just 'new session'"
  - 2026-08-13 fixed (v1.0.24): the title template drops `${this._descriptor.label}` - "New session in <folder>", the armed-mode suffix (DEF-36) kept. The claude-regression Galata suite selected the button by the old exact title and was updated with it. Same pass, user's acc-crit directive: the armed state is "no menu, just + starts new session" - already the behaviour (armed variants self-suppress, the click launches when the visible-variant count is zero, unit-pinned), and the Panel criterion now says it in those words. Jest 118, Galata 28/28 at v1.0.24

- [x] `DEF-114` **The + menu entries still name the provider, and the tooltip interpolates the folder path** - MAJOR, user-reported; DEF-113 stripped the vendor name from the tooltip but left the menu reading "New Claude Code Session" / "New Claude Code Session (Skip Permissions)", and the tooltip still named the live folder, which a deep tree makes unreadable; `src/core/panel.ts`
  - 2026-08-13 reported: Star Colonel - "no vendor name"; "do not show the folder, because folder may be superlong if deep inside tree"
  - 2026-08-13 fixed (v1.0.26): the menu entries read `New session` / `New session (<mode label>)` and the tooltip is the static `New session in current folder` - the folder interpolation deleted outright. The armed-mode tooltip suffix stays, on the user's explicit answer when asked (DEF-36 stands). Unit title tests and the claude-regression selectors (button title, both menu entry texts, the exact-match launch click) followed. Jest 118, Galata 28/28 at v1.0.26, menu rendered and screenshotted on the 8931 test server

- [x] `DEF-115` **The armed header + still drops a menu** - user-reported, NOT REPRODUCED; the claim was that with `providers.claude.dangerouslySkipPermissions` on, the header `+` still opens the two-entry menu instead of launching; `ui-tests/tests/panel-regressions.spec.ts`
  - 2026-08-14 reported: Star Colonel - "currently there is option checked for claude code to skip permissions, and still there is menu under '+'"
  - 2026-08-14 investigated (v1.0.27): the code path is intact - `_visibleVariantCount()` filters every variant whose forced launch equals the plain one, so an armed switch leaves zero variants and the click handler calls `_newSession()` without opening the menu (`src/core/panel.ts`), and the live-settings path reaches it (`settings.changed` -> `reconcile` -> `applySharedSettings` -> `setModes`). On the reporting machine the switch is OFF: `~/.jupyter/lab/user-settings/jupyterlab_ai_code_assistants_extension/plugin.jupyterlab-settings` holds `"providers.claude.dangerouslySkipPermissions": false`, mtime 2026-08-09, and it is the only settings file the running server reads - no `overrides.json` entry, no second copy, and the retired standalone `jupyterlab_claude_code_extension` is not installed, so the panel seen was this one, unarmed. The menu was correct unarmed behaviour
  - 2026-08-14 closed (v1.0.27): the real gap was proof, not behaviour - the acc-crit criterion "Header direct launch when armed" was ticked on unit evidence alone (`_visibleVariantCount() === 0`), and no Galata test had ever clicked the armed `+`. Added `DEF-115 - an armed launch mode leaves the + with no menu at all`: clicks unarmed first so the menu-opens branch is proven live (the assertion can fail), arms the switch through the settings registry, waits for the title to acknowledge it, clicks, then asserts the menu is not in the DOM and a terminal appears. Galata 29/29 at v1.0.27
  - 2026-08-14 confirmed: Star Colonel enabled the switch and the header + launches with no menu - diagnosis (setting off, not behaviour) holds

- [x] `DEF-116` **Panel 404s and a dead Show in File Browser** - user-reported, NOT REPRODUCED; the console showed 404s for `providers/claude/sessions` and `providers/claude/terminal/5`, and `Show in File Browser` appeared to do nothing for `/home/lab/workspace/private/ai-assistants/groundrails`; no code change
  - 2026-08-14 reported: Star Colonel - "'show in file browser' fails to do anything"; "something is wrong with the extension as it returns 404"
  - 2026-08-14 investigated (v1.0.28): the 404s never reached the Jupyter server. `/var/log/jupyterlab.log` holds ZERO 404s for `jupyterlab-ai-code-assistants-extension` or `jupyterlab-share-files-extension` across the whole day, the failing requests' query stamps (`1786736558*`, 21:42:38) appear nowhere in it, and the 21:42:30-21:42:50 window is empty of log lines entirely. The server had not restarted since 08:08, and the same endpoints answered 200 in 1-8 ms before and after. Three corroborating signs of a hop in front of the server: two unrelated extensions failed in the same instant, the browser's own `Notification poll reconnected to server` fired in that window, and the 404 body was plain-text Go-style rather than jupyter_server's HTML 404 (compared directly against a bogus local route). Cause is the reverse proxy / tunnel serving `workbench.lab.stellars-tech.eu`, outside this repository
  - 2026-08-14 investigated (v1.0.28): `Show in File Browser` works. Driven live against the running lab with Playwright (headless chromium, hub token as an auth header, separate `probe` workspace): right-click the groundrails row -> `Show in File Browser` moved the breadcrumb from `/` to `private/ai-assistants/groundrails` and listed its contents, screenshotted. It read as a no-op for the user because their file browser had been sitting in that exact folder since the 08:08 session restore - the contents API shows it polling `api/contents/private/ai-assistants/groundrails` every 10 s since then, so navigating there again changes nothing on screen. The terminal extension's identically-named action shares the destination and therefore the no-op
  - 2026-08-14 logged, not fixed: the action gives no feedback when the file browser is already at the target folder. MINOR, per house rule - a toast for a successful no-op is the kind of edge polish that generates the next defect

- [x] `DEF-117` **The status probe gives up loudly and waits a full minute after a wake** - MINOR, reported by a console triage of the live lab (class C7); the warning claimed `no assistant panels will be docked` when the 60 s timer re-probes and docks them, and nothing re-probed on wake, so a laptop coming back from sleep faced a panel-less sidebar for up to a minute; `src/index.ts`
  - 2026-08-19 reported: triage report `lab-js-errors-summary.md`, class C7 - "the warning text is wrong ... add an immediate re-probe on `online` and on `visibilitychange` to `visible`"
  - 2026-08-19 fixed (v1.0.29 tree): the warning now names the retry - `status probe failed; retrying every 60s and on wake - panels dock again once a probe succeeds` - with the cadence read from `STATUS_INTERVAL_MS` rather than a literal, and the function docstring corrected to match. Listeners on `online` and on `visibilitychange` (guarded to `visible`) re-probe and reconcile immediately. A wake fires both, so a wake costs two probes - deliberately, see the review note below. Blast radius checked first with graphify - `probeStatus` is a closure with two call sites, both inside `activate`, and nothing imports `src/index.ts`. Galata `DEF-117` dispatches the two events separately and measures each against the count taken immediately before its own dispatch, so deleting either listener reddens the suite - verified by deleting each in turn, rebuilding and running (`Expected: > 1, Received: 1` and `Expected: > 0, Received: 0`). Jest 118, pytest 170, Galata 30/30
  - 2026-08-19 adversarial review (architect + bug-hunter, adjudicated): both lenses DO-NOT-SHIP, every finding confirmed, most of them traceable to two additions the brief never asked for. The first fix carried a coalescing wrapper so a wake's two events shared one request; the bug-hunter measured what it cost - a probe wedged mid-cycle made the 60 s interval tick issue NO request at all (gaps of 48.0 s then over 70 s, panels undocked), and a wake arriving while a probe was wedged joined the doomed request instead of issuing a live one, so the sidebar stayed empty at t=17 s. Worst case doubled the dark window to 120 s, which is the defect this entry closes, and made the new "retrying every 60s" text false as it printed. The wrapper is deleted; a wake costs two idempotent GETs and the interval keeps its own tick. The first fix's Galata test was also disproved: dispatching either event alone satisfied both its assertions, so deleting a listener left it green. Rewritten and falsified per listener
  - 2026-08-19 also reverted: the same first fix demoted `src/core/request.ts`'s `Not a JSON response body` from `console.log` to `console.debug`, citing the report's C8. Unrequested scope, and it made a silent path silent - see DEF-118. `src/core/request.ts` is byte-identical to HEAD
  - 2026-08-19 logged, not fixed (round-1 MINORs this entry had not named): the interval body and the wake helper each spell out `void probeStatus().then(reconcile)` rather than sharing one (`src/index.ts`); `visibilitychange` re-probes on every tab focus, not only after a real sleep - measured 3-20 ms server-side, with `setPresentationMode`, `setRecentLimit` and `setModes` all early-returning when unchanged, so a focus burst costs nothing visible; and the listeners register after `await migrate()`, which is now DEF-122. Declined under the standing rule: MINORs are logged, not fixed. The duplication is now a pure deletion rather than a new helper - `window.setInterval(reprobe, ...)` with `reprobe` hoisted above it - and nothing about it is unsafe; it is simply not worth the churn
  - 2026-08-19 round-2 review (architect + bug-hunter, both pinned to the fixes): every closed finding reproduced as closed, and the bug-hunter re-derived the test's power independently - intact GREEN, `online` listener deleted RED, `visibilitychange` deleted RED, the same two failure strings recorded above. Both lenses still returned DO-NOT-SHIP, on four new findings: DEF-121 and DEF-122 below, plus the DEF-118 title and the DEF-120 estimate corrected in place

- [ ] `DEF-118` **A non-JSON body on a 200 loses every panel behind an unlabelled `Not a JSON response body` log** - MAJOR, logged not fixed; `requestAPI` throws only when `!response.ok` (`src/core/request.ts`), so a 200 carrying an SSO or proxy interstitial returns the raw string, `probeStatus` never warns, `status.providers` is `undefined`, every provider reads unavailable and every panel undocks. Measured by the round-1 bug-hunter against a live lab
  - 2026-08-19 logged: the shape that would fix it is a conditional - `debug` when `!response.ok` (proxy noise, DEF-116's case), `warn` when a 200 fails to parse (an anomaly). That is a new branch in a file this brief never named and it needs its own test, so it is its own defect, not a rider on DEF-117. The `console.log` line stays until then, which keeps the path visible at Chrome's default filter level

- [ ] `DEF-119` **A probe that did not answer reads as "nothing is available"** - MAJOR, logged not fixed; `probeStatus`'s catch sets `status = null`, and `reconcile` treats null as every provider unavailable, so it undocks every panel. Pre-existing, but DEF-117's wake listeners newly reach it on an ordinary tab focus or an `online` that fires before a VPN tunnel routes
  - 2026-08-19 scoped (round-3 bug-hunter): this describes a FAST failure - a refused connection, a 403, a 503 - which rejects in milliseconds and is normally the newest probe issued, so DEF-121's guard lets it through and it undocks. A TIMEOUT does not behave this way; see DEF-123
  - 2026-08-19 logged: measured by the round-1 bug-hunter - one `online` with the status route down detaches the open panel within seconds, and after recovery the tab returns but is not visible, so the user's place is lost. The one-line remedy is to keep the previous `status` on failure, so a failed probe is no news rather than a verdict; its stated cost is that a CLI genuinely uninstalled while the server is unreachable stays docked until a probe succeeds. Deferred because it edits panel lifecycle, no Jest test covers `activate`, and the cause stays fully reachable through the pre-existing 60 s tick either way
  - 2026-08-19 blast radius (round-4 architect): the remedy has an in-repo precedent - `src/core/colour.ts` keeps its cache on failure ("Keep the cache; the next poll retries."), and `probeStatus` is the only polled cache in `src/` that discards, which is what DEF-119, DEF-121, DEF-123, DEF-124 and DEF-125 all descend from. Taking it is not one line in isolation: it makes `probeGeneration` and its guard dead code, moots DEF-123 and DEF-124, and REDS `DEF-121 - a newer failure cannot bury an older roster`, which asserts the very undock the remedy removes. Whoever takes it deletes that test deliberately or not at all

- [ ] `DEF-120` **The three probe Galata tests can be satisfied by the 60 s timer's own probe** - MINOR, logged not fixed; each `expect.poll` returns on the first status request it sees, so a timer tick landing inside that few-hundred-millisecond window satisfies an assertion the listener should have satisfied
  - 2026-08-19 logged: the fix is to stamp each counted request and accept only those within ~2 s of the dispatch. Not taken - the test has run green on every full-suite pass since, and edge polish on a passing test is what the house rule exists to stop
  - 2026-08-19 corrected (round-2 bug-hunter): the round-1 estimate of "1-2% of runs" measured the harmless half. On a green build the poll returns on the listener's own request within a few hundred milliseconds, so a stray tick changes nothing. The case that matters is a build with a listener DELETED, where the poll runs its full 5 s and a tick inside that window manufactures a false green - a 5 s window, not a fraction of a second. Both are gated by phase: the measured offset from the activation probe to the spec's first dispatch is ~1.4 s, so a 60 s tick reaches either window only if startup approaches 60 s
  - 2026-08-19 widened (round-4 architect and bug-hunter, independently): the two DEF-121 tests carry the same risk by a DIFFERENT mechanism and a wider window. They do not count requests - they route them, and any third status request falls through to `route.continue()` and re-docks the tab. On the shipping build this is unreachable (measured `seen: 2`, re-dock in 16 ms), but on the mutant each test exists to catch, the poll runs its full 10 s, and a tick inside that window would serve a real roster and manufacture a green FOR THE MUTATION CHECK. Measured startup-to-assertion is ~1.8 s and ~2.7 s, so the 60 s tick is ~40 s clear; the exposure is 10 s wide, not a few hundred milliseconds. If ever fixed, `route.abort('failed')` on `seen >= 3` closes it - not taken, because polishing a passing test is what this rule exists to stop

- [x] `DEF-121` **A stale failed probe discards a newer roster and undocks every panel** - MAJOR, found by the round-2 bug-hunter against the DEF-117 fix; `src/index.ts`
  - 2026-08-19 reported: `probeStatus` wrote the shared `status` whenever its own await settled, so with two probes in flight the LAST TO SETTLE won rather than the last to be issued. DEF-117's wake listeners are what made two probes concurrent - before them the 60 s cadence and the 60 s client deadline (`REQUEST_TIMEOUT_MS`, `src/core/request.ts`) kept probes phase-locked. Measured live: a wake probe wedged at t+30 s, the interval's tick succeeding at t+60.1 s with the correct roster, and the wedged probe's timeout landing at t+75.1 s and wiping it - `t+70s tabs=3`, `t+76s tabs=0`. The sidebar then stays empty until the next tick at t+120 s, and the panel that returns is not the one the user had open (`tabIsCurrent=false`), so their place is lost. Deterministic, not a race: the deadline equals the interval, so exactly one tick always falls inside a hung probe's lifetime, and both wake orderings were measured ending at `tabs=0`
  - 2026-08-19 fixed (v1.0.29 tree): `probeStatus` stamps each probe with a generation counter. A ROSTER is written unconditionally, whichever probe settles last - both are answers from the same server and the loser is at most one client deadline (`REQUEST_TIMEOUT_MS`) stale. That bound covers the roster, not the once-per-id PATH warning it can fire; see DEF-125. A FAILURE is written only while no newer probe has been issued, because a failure is not an answer and must not win on issue order. The `console.warn` sits outside the guard deliberately, so a superseded failure still prints (DEF-124). It suppresses no request, which is what separates it from the coalescing wrapper DEF-117's round-1 review deleted: every tick and every wake still issues its own GET, only a superseded ANSWER is dropped. Blast radius is the function. Galata `DEF-121` holds the first probe open, answers a later one normally, then fails the held one and asserts the panel is still docked; severing the guard reddens it at the tab assertion (verified by mutation, rebuild and run). Jest 118, Galata 31/31
  - 2026-08-19 corrected (round-3, architect and bug-hunter independently): the first version of this fix guarded the SUCCESS path too, which was half a fix too large and reintroduced the very symptom DEF-117 exists to remove. Guarding a success on "no newer probe ISSUED" discards a good roster whenever a newer probe merely exists - and since a failure rejects in milliseconds while a success costs a round trip, the newer probe is systematically the failure. Both lenses measured it from opposite ends: the architect from the wake path (older probe answers in 20 ms, roster discarded, sidebar dark until the slow newer probe settles - up to 60 s where HEAD recovered in ~0.3 s), the bug-hunter from a live rig (`panels down after the fast failure: true` / `panels restored by the older success: false`, against a control with the success guard severed reading `true`). Two lines deleted. Galata `DEF-121 - a newer failure cannot bury an older roster` pins the restored behaviour and reds when the success guard is put back - verified by mutation, rebuild and run, while the older DEF-121 test stayed green, which is exactly the coverage gap the bug-hunter reported. Jest 118, pytest 170, Galata 32/32 on the shipping tree
  - 2026-08-19 note: this does not close DEF-119. A single failed probe with nothing newer behind it still undocks every panel - that one is logged and stays logged. DEF-121 is the narrower claim that a probe already answered cannot be undone by an older one

- [ ] `DEF-122` **A wake during activation is ignored, and the failure warning promised a retry that was not armed yet** - MINOR, found by the round-2 bug-hunter; `src/index.ts`
  - 2026-08-19 reported: the first `probeStatus` is awaited during `activate`, but the interval and both wake listeners register after `await settingRegistry.load` and `await migrate()`. the window is unbounded on a hanging server, not 60 s: only `migrate` goes through `requestAPI` and carries the client deadline, while `await settingRegistry.load` reaches `@jupyterlab/services`' `makeRequest` with no signal and `serverconnection.js` sets no timeout anywhere. So the DEF-117 warning claiming `retrying every 60s and on wake` could be false indefinitely. Measured: warning at t+60.9 s, wakes dispatched at t+30 s and t+90 s producing zero requests, next probe at t+181 s. JupyterLab's shell is blocked for that whole time - the plugin's own activation logged 120494 ms
  - 2026-08-19 fixed by half (v1.0.29 tree): the warning no longer names a cadence. It reads `status probe failed; panels dock again once a later probe succeeds`, which is true at every site it prints from, and a comment at the catch says why the cadence is absent
  - 2026-08-19 logged, not fixed (the other half): the reviewer's remedy is to move the interval and both listeners above `await migrate()` so a wake during activation is heard. Declined - a wake landing before `await settingRegistry.load` reconciles with `settings` still null, where `setting()` returns its `true` fallback, so every panel docks and any the user disabled flashes in before the later reconcile corrects it. (Round-3 bug-hunter correction: the `migrate` half of this reason was wrong and is withdrawn - no provider's `settings_map` carries an `enabled` key, favourites are written server-side by `core/migrate.py` rather than through settings, and `settings.changed` is connected before `await migrate()` anyway, so `reconcile` already runs mid-migration today. The window before `settingRegistry.load` is the whole of the load-bearing case, and it alone carries the decline.) "Open JupyterLab in a background tab, switch to it a second later" fires `visibilitychange` on an ordinary startup, so that flash is a common path while the defect it fixes needs a server hung for a full minute. Trading a rare wrong sentence for a routine wrong sidebar is the wrong way round

- [ ] `DEF-123` **A timed-out probe can never clear the roster, so panels stay docked against a server that stopped answering** - MINOR, found by the round-3 bug-hunter; accepted behaviour, recorded rather than changed; `src/index.ts`
  - 2026-08-19 reported: `STATUS_INTERVAL_MS` (`src/index.ts`) and `REQUEST_TIMEOUT_MS` (`src/core/request.ts`) are both 60 s and independent of each other. A probe issued at tick k therefore aborts at tick k+1 plus a few milliseconds - always AFTER the next tick has bumped the generation - so a timeout's `status = null` is superseded every single time and never lands. Measured: deadline equal to interval, every probe wedged, `probes wedged=4 timeouts logged=3`, tabs still 1 after 16 s; control with the deadline shorter than the interval, tabs 0. The panels stay docked showing stale sessions while `status probe failed` prints once a minute about panels that never went away
  - 2026-08-19 accepted, not changed: undocking on a failure is itself the open defect DEF-119, whose own recorded remedy is to keep the previous roster - so on the timeout path this is the DEF-119 behaviour arriving by accident rather than a new fault. Making the failure verdict independent of issue order is cheap, not costly: `src/core/request.ts` already exports `isRequestTimeout`, already consumed at `src/core/panel.ts` under DEF-81, so `if (!isRequestTimeout(err) && mine === probeGeneration)` does it with no new state and both DEF-121 tests stay green. It is declined on the merits above rather than on cost - an earlier version of this entry claimed the remedy needed probe bookkeeping, which was wrong. What it costs: against a server hung rather than refusing, a stale roster is shown instead of an empty sidebar, and the once-a-minute warning is misleading about panels. Round-4 bug-hunter, in the user's favour: the surviving panel raises its own inline banner naming the cause (`The server is not answering - retrying`), where undocking leaves an empty sidebar with no explanation at all, and the skip-tick guard in `src/core/panel.ts` held it to 8 outstanding requests over 25 s, so nothing piles up
  - 2026-08-19 note: the equality of the two constants is what makes this total rather than occasional. Nothing enforces it - retune either and this entry's mechanism changes silently, which is why the comment in `probeStatus` states the ordering rule rather than the two literals

- [ ] `DEF-124` **The failed-probe warning prints for verdicts that were discarded** - MINOR, logged not fixed, found by the round-3 architect and bug-hunter independently; `src/index.ts`
  - 2026-08-19 logged: the `console.warn` sits outside DEF-121's generation guard, so a superseded failure warns even though it changed no state and undocked nothing. Measured at three lines per wedged cycle with zero state change. DEF-117 itself came out of a console triage of the live lab, so this is per-minute noise aimed back at the surface that found the original defect. Declined under the standing rule: MINORs are logged, not fixed. Moving the warn inside the guard also silences the settle signal the DEF-121 test synchronises on - reproduced by the round-4 bug-hunter as `warnings=0` with the test reddening at its poll rather than its assertion, `tabs=1` throughout - so the edit costs a test change as well

- [ ] `DEF-125` **An older successful probe can overwrite a newer one, and the false PATH warning it fires can never be re-issued** - MINOR, logged not fixed, found by the round-4 bug-hunter; `src/index.ts`
  - 2026-08-19 reported: the failure write is ordered by ISSUE (DEF-121's guard), the success write by SETTLE, so an older probe answering second overwrites a newer roster. Measured live: a user with `providers.claude.enabled` explicitly set upgrades the claude CLI, which unlinks the binary for a moment; a slow wake probe issued inside that window answers after the tick that already saw the relinked binary, and its obsolete `available: false` undocks the panel 6 ms later. The panel set self-corrects on the next tick - up to 60 s, deselected - but the WARNING set does not: `warnedUnavailable` is once-per-id, so the false `"claude" is enabled but its binary was not found` toast permanently suppresses the real one. Measured `infos=1` after the false alarm, still `infos=1` when the binary genuinely went missing afterwards
  - 2026-08-19 logged: the write itself is not a regression - `git show HEAD:src/index.ts` also writes the roster unconditionally on success, and the round-3 deletion restored exactly those semantics. What is new is the concurrency, which the DEF-117 wake listeners introduced in this same change, so this is DEF-119's situation again: a pre-existing behaviour that the listeners newly reach
  - 2026-08-19 logged, not fixed: declined under the standing rule, MINORs are logged, not fixed. The remedy the reviewer tested is two lines - keep a `lastAnswered` generation and write the roster only when `mine > lastAnswered` - which suppresses no request and leaves both DEF-121 tests green (verified by in-flight bundle mutation, `clobbered: false`). It is declined because it adds a second piece of probe state to the same function DEF-123 just declined to grow, and the trigger needs a CLI upgrade to race a slow wake probe. The residue worth watching is the warning, not the panel
