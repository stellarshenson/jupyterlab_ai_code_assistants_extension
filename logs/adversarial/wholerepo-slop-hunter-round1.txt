I'll start by reading the adversary definition file.

VERDICT: DO-NOT-SHIP (14 findings) - ~330 removable lines, almost all of it documentation and test scaffolding rather than shipped code; the code is lean, but two docs carry claims that no longer check out (a README setting that does not exist, an evidence table whose citations have all rotted).

## Findings

- **[MAJOR] 124 byte-identical log lines in the acceptance-criteria doc** - `/home/lab/workspace/private/jupyterlab/jupyterlab_ai_code_assistants_extension/docs/acc-crit-jupyterlab-ai-code-assistants.md`, axis 5. Of 285 `- log:` lines, 241 are two strings repeated verbatim: `- log: 2026-08-07 criterion added (v0.1.0)` ×124 and the `wf_a4376a8b-03e ... 125 pytest + 59 Jest + 16/16 Galata green at v0.1.7` closure ×117. Cut: delete the 124 "criterion added" lines, state the fact once under the H1 ("added 2026-08-07 at v0.1.0 unless a log says otherwise"). Delete-test: applied to a scratch copy, `acc-crit.py check --strict` stays at **0 errors, 0 warnings** (file 530 → 405 lines) because every affected criterion keeps its closure log line. Cutting *both* strings instead removes 241 lines but trips 111 `no log: line under criterion` warnings under `--strict` - do not do that one. Load-bearing check: cleared (checker verified, no other file parses this doc).

- **[MAJOR] `docs/feature-parity.md` - 73 lines whose file:line evidence no longer resolves** - axis 5. Written at commit `8e62249` when `panel.ts` was 2020 lines; it is now 2198 and the doc has never been touched since. Spot-checked 5 of ~60 citations, all 5 wrong now (`panel.ts:71-75` claimed "section labels" is `BRANCH_WATCH_MAX_ATTEMPTS`; `panel.ts:1491-1501` claimed "favourite command" is tooltip assembly; `panel.ts:1155-1167` claimed "favourites filter" is `_basename`). The audit's actual product - "42 present, 2 dropped, 0 missing" - is already recorded in acc-crit:293. Cut: `git rm docs/feature-parity.md`, inline the two dropped-feature reasons into acc-crit:293's log line, drop the filename from that line. Load-bearing check: cleared - the only inbound reference is acc-crit:293 and defects.md DEF-9's provenance note; git keeps the table. Not fabrication: the citations were correct at authoring time (verified with `git show 8e62249:src/core/panel.ts`), they rotted.

- **[MAJOR] `docs/research-source-extensions.md` - 72 lines of pre-implementation research for a finished port** - axis 5, verify-then-delete. Reads "Reference for implementing the provider core", "to implement, Kimi strategy", "Coupling points to burn out of the core" - all ten executed and closed. Its file:line pointers are into three retired external repos. Cut: after the feature-parity cut removes its only two inbound references (`:63` ×2), `git rm` it; the one fact not recorded elsewhere is the webpack/chalk pin rationale, which the README already carries verbatim at `README.md:68`. Load-bearing check: cleared by grep - no script, workflow or test reads it.

- **[MAJOR] `src/__tests__/registry.spec.ts:132-178` - "descriptor filtering" tests a predicate that lives in the test file** - axis 3, 47 lines, 3 tests. `enabledAndAvailable` is defined at `:136-143` inside the spec and has 6 occurrences, all in that file; it is a reimplementation of `src/index.ts`'s reconcile rule, not an import of it. Delete-test: `npx jest --coverage` reports `index.ts | 0 | 0 | 0 | 0 | 1-356` - **no Jest test imports `src/index.ts` at all**, so no mutation of the real predicate can turn this block red. Two of its three assertions are self-referential (`expect(predicate(registry.descriptors, {}, all)).toEqual(registry.ids)`). The behaviour is genuinely covered one tier up, at the only tier that can observe it: `ui-tests/tests/panels.spec.ts:94-123`. Cut: delete the whole `describe`; change the Testing matrix cell at acc-crit:423 (`Enable/disable | ... | descriptor filtering |`) to `-`, as the row below it already does for CLI-absent. Load-bearing check: cleared.

- **[MAJOR] `src/__tests__/colour.spec.ts:402-423` is a byte-identical copy of `:313-334`** - axis 3, 23 lines. `diff` of the two bodies with comments stripped differs on the title line only ("does not let an answer older than a write speak for it" vs "...than a CONFIRMED write..."). Delete-test, run: deleted the second, Jest 81 → 80 pass; then mutated `colour.ts:210` (`if (seq <= asked)` → `if (seq > asked)`) and the **surviving** twin fails - `● ColourStore origins › does not let an answer older than a write speak for it`, 79 passed / 1 failed. Zero mutation coverage is lost. Load-bearing check: cleared.

- **[MAJOR] `logs/adversarial/HARVEST.md` - 21-line resume runbook for a campaign that finished** - axis 1/5. "Harvest instructions (written before session limit)", tells a future session to re-spawn round 1 from `/tmp/claude-1000/.../scratchpad/adv/` (a path that no longer exists) and records "Tree state at handoff: v0.1.9". The gate it describes is closed at acc-crit:476-480. Cut: `git rm logs/adversarial/HARVEST.md`. Load-bearing check: cleared - grep for `HARVEST` across the tree hits only the file itself and the graphify cache; `logs/README.md` names `*-prompt.txt` and `*-roundN.txt`, not this file.

- **[MAJOR] One domain gotcha restated nine times across four files** - axis 4. "painting a tab makes the companion extension release its own record of the choice" appears at `src/core/colour.ts:192`, `colour.ts:272`, `src/core/terminals.ts:347-353`, `src/__tests__/colour.spec.ts:138,269,316,362,405`, and `jupyterlab_ai_code_assistants_extension/tests/test_colour_store.py:327` - already drifting in wording (release / releases / drop / dropped). Separately, the `isOverride`-is-stale-under-a-pending-write paragraph exists three times: `colour.ts:149-156`, `panel.ts:2034-2038`, `colour.spec.ts:426-428`. Cut: keep one canonical statement of each at the decision point (`terminals.ts:347-353` for the paint hazard, `panel.ts:2034-2038` for the pending gate) and reduce the rest to their case-specific clause - roughly 25 comment lines. Context: `colour.ts` is 33% comment and `terminals.ts` 27%, against 15% for `panel.ts` and 18% for `popup.ts`. Defend, do not cut: the single canonical statement of each - both explain a non-obvious cross-extension side effect the code cannot say itself.

- **[MAJOR] `README.md:43` advertises a presentation mode that does not exist** - axis 8. "Presentation modes - label rows by session name, folder name, or path relative to the JupyterLab root". `schema/plugin.json` `presentationMode.enum` is `["name", "path"]`; the folder name is the fallback *inside* `name` (`panel.ts:1148-1159`), which `docs/feature-parity.md:24` states explicitly. A user reading this looks for a third setting value. Cut: two words, `folder name, `. Checked against the generated schema, not inferred.

- **[MINOR] `ui-tests/tests/jupyterlab_ai_code_assistants_extension.spec.ts:29-47` asserts the panel count through a console string** - axis 3, 19 lines plus one Galata run. It greps the log for `${AVAILABLE.length} of `, which is printed from `live.size` at `index.ts:350-352` - the same value `panels.spec.ts:42` asserts against the DOM (`toHaveCount(AVAILABLE.length)`). Any mutation that changes the docked count fails the DOM test first. Load-bearing check: cleared - the *first* test in this file (the verbatim activation message) is named by acc-crit:452 and by the copier template; this second one is named by no criterion. Keep the first.

- **[MINOR] Four `ISession` fields on the wire that nothing reads** - axis 1. `src/core/types.ts:114-117` (`summary`, `first_prompt`, `created`, `modified`) have zero readers in `src/`; server-side they are emitted at `providers/gemini.py:594-597` and `providers/claude.py:729-733`. Cut: the four type members and the four row-dict emissions, plus the mention in `core/store.py:229`. **Do not touch the computation**: `gemini.py:630-631` uses `meta.get("first_prompt")` as the branch-menu label fallback and `:572` uses `summary` to derive the row name - both load-bearing, only the row-level *emission* is dead.

- **[MINOR] `core/colour_store.py:85-87` `load_overrides` has zero production callers** - axis 1. Grep across the package excluding `tests/`: nothing. The route reads both halves in one pass via `load_store` (`routes.py:473`), deliberately - `load_store`'s own docstring says two reads "can straddle another process's write and answer halves that never coexisted", which is precisely the hazard this leftover single-half reader reintroduces for the next caller. Kept alive by 7 test call sites; `load_overrides(p)` is exactly `load_store(p)[1]`, so the substitution is mechanical, not a refactor. Load-bearing check: cleared - not an entry point, not string-dispatched.

- **[MINOR] `package.json:88` `"mkdirp": "^1.0.3"` is a scaffold leftover** - axis 6. Zero references in `src/`, `scripts/`, `ui-tests/`, all 27 npm scripts, the `Makefile`, `pyproject.toml` and every `.github/workflows/*.yml`. DEF-22 swept `@types/react` and `yjs` from the same scaffold and missed this one. Load-bearing check: cleared (not a binary any script invokes; `rimraf` next to it *is* used by the clean targets - keep that). Checked and **rejected** as a finding: `@types/json-schema` looks equally unused but is load-bearing - `@jupyterlab/settingregistry/lib/tokens.d.ts:8` imports `@rjsf/utils`, whose `types.d.ts` imports from `json-schema`, so `tsc --noEmit` needs it.

- **[MINOR] `src/core/colour.ts:409` comment is wrong** - axis 4. `/** Unanswered writes per id. Read by the release affordance only. */` - `_pending` is also read on the deletion path: `forget()` filters on `this.isPending(id)` at `colour.ts:348`, and `forget` is called from `_cleanupParallel` (`panel.ts:686`) and `_deleteBranches` (`panel.ts:1113`), not just `_resetColours`. One-line correction, not a deletion.

- **[MINOR] (taste) Three exports with no external consumer** - axis 2. `providerPath` (`request.ts:58`), `API_NAMESPACE` (`request.ts:6`) and `showLaunchSpinner` (`terminals.ts:34`) are each referenced twice, both times inside their own file, and are not re-exported through `lib/index.js`. Dropping the `export` keyword narrows the published `.d.ts` surface. Note graphify was unreliable here - it reported "no affected nodes" for `showLaunchSpinner` despite the call at `terminals.ts:159`, so every claim above is grep-verified, not tool-verified.

**Total proposed deletion: ~330 lines** (124 acc-crit + 73 feature-parity + 72 research doc + 47 registry.spec + 23 colour.spec + 21 HARVEST + 19 Galata + ~25 comments + ~10 wire fields + 3 `load_overrides` + 1 devDependency), of which 217 are documentation, 89 tests and ~40 source.

## What earns its place

- **The `ColourStore` write-ordering machinery** (`colour.ts:398-414`) - I hunted it and stood down: `docs/defects.md` DEF-30 already proposes exactly this cut and the owner deferred it with a priced caveat (`requestProvider` sets no timeout, so serialising writes behind a hung GET stops colour capture for the session). That is a settled decision with a reason, not dead weight.
- **`src/__tests__/colour.spec.ts` minus the one duplicate** - 93.98% statement coverage of `colour.ts` and the mutant I planted was killed by the surviving test. These are behaviour tests over out-of-order answers, not coverage padding.
- **`assistantsIcon` (`icons.ts:19-22`)** - zero TypeScript references, and it must stay: `schema/plugin.json:5` reaches it by the string key `jupyterlab_ai_code_assistants_extension:assistants`, which exists only because the `LabIcon` constructor self-registers at module load. Textbook string-keyed lookup; a naive delete would silently drop the settings-page icon.
- **`core-neutrality.spec.ts`** - the architecture's only enforcement. It reads the source of `src/core/` rather than asserting behaviour, which is the only way an `if (id === 'claude')` could ever fail a test; the three `stripComments` cases below it guard the scan against passing vacuously, so they pay rent despite testing a test helper.
- **`test_store_isolation.py`** - path traversal, symlink escape and cross-provider key confusion, driven against four real stores on disk. Nothing here is a mock asserting itself.