`[2026-08-09 Sun 14:22]`

## Verdict
`VERDICT: DO-NOT-SHIP (8 findings)` - the ladder change itself is correct and both tiers now agree, but the write-back keys on a guessed conversation, a failed POST silently destroys the colour the change exists to protect, and three stale native-is-read-only artefacts survived. Recommendations leave the system net **SMALLER** (-93-line spec, -1 dead guard, -1 wrapper, -1 param, -1 import; +1 bool return, +1 local).

Verified mechanism first: `node_modules/jupyterlab_colourful_tab_extension/lib/index.js` - `api.setColour` calls `releaseMenuColour(widget.id)` on any truthy colour, deleting the `jupyterlab-colourful-tab-colours` entry and saving; `lib/colours.js:setWidgetTabColour` rides `title.className`. So every paint over a user-coloured tab is destructive, and read-before-paint is load-bearing. Ran `pytest test_colour_store.py test_routes.py` - 48 passed.

## Inconsistencies / defects

- **[MAJOR] The write-back persists the user's colour against a conversation it only guessed** - `src/core/terminals.ts:303-318`: `sessionId` falls back to `sessionForCwds(...)`, which `src/core/colour.ts:95-99` explicitly documents as a per-PROJECT representative ("resolving colour by row tints every terminal of a project with the representative's colour"). Both the new write-back and the new `userSet && !sessionId` bail (`:324`) use that fallback, so a colour hand-set on a terminal whose id is unreadable (`ITerminalProbeResponse.session_id` is nullable by design, `src/core/types.ts:248`) is stored under a DIFFERENT conversation, served to its rows, and inherited by its forks. Before this change native providers skipped the write-back entirely, so this is newly opened for claude. Fix: `const owner = info.session_id ?? null;` - use `owner` for the write-back condition and for the bail, keep the cwd fallback only for the paint. Adds one local.
- **[MAJOR] A failed store write destroys the colour it was meant to capture** - `src/core/colour.ts:174-186` swallows the POST failure, `src/core/terminals.ts:334` then paints, and the paint releases the localStorage menu entry. The write only fires when `userSet !== cache` (`terminals.ts:316`) and the cache is updated before the request (`colour.ts:169-173`), so there is never a retry: one failed POST = colour gone on the next reload, silently. Fails requirement 2. Fix: `set()` returns `false` from its catch and `true` otherwise; skip `_applyColour` for that widget when it returns false - the menu entry survives, the tab looks identical, the next 30 s pass retries. Adds a bool return and one branch.
- **[MAJOR] `BranchHandler` still carries a dead native guard and a rationale that is now false** - `jupyterlab_ai_code_assistants_extension/core/routes.py:549` (`colour_source != "native"`) is unreachable: the route serves only `SERVER_MINTED_FORKS = {"server-copy"}` (`core/registry.py:51`, gate at `routes.py:537-539`) and the sole `native` provider is `fork_strategy: native-flag` (`providers/claude.py:1056`), so it 400s `fork_unsupported` first. Its docstring `routes.py:513-515` still asserts the colour store "refuses every write" - deleted by this diff. Fix: delete the guard (call `inherit_colour` unconditionally) and delete the "except against a ``native`` provider…" clause. Nothing today reaches either. -5 lines.
- **[MAJOR] `colour_store.py`'s module docstring still states the pre-change rule** - `core/colour_store.py:6-8`: "the write-back beats the default everywhere except a ``native`` provider, where the assistant owns its own colour (acc-crit "Override precedence")" - contradicts `routes.py:193-199` and the already-updated criterion (`docs/acc-crit-jupyterlab-ai-code-assistants.md:244,247`). This is the file a maintainer reads to learn the rule. Fix: one-line edit to match `_effective_colour`.
- **[MAJOR] The new galata spec exercises no UI and duplicates pytest** - `ui-tests/tests/colour-override.spec.ts:1-93` boots JupyterLab only to issue three `page.request` calls; all three assertions already exist server-side (`tests/test_colour_store.py:209` parametrised over `native`, release at `:150-160`, `tests/test_routes.py:307-321`). The logic this change actually introduced - capture-before-paint ordering and the `userSet && !sessionId` bail (`terminals.ts:309-326`) and the native inherit (`panel.ts:925-935`) - has no test at any tier (`src/__tests__/colour.spec.ts` covers only the pure ladder). Its own comment is false: "the way the panel's write-back does" (`spec:34-35`) - the panel calls `set` from exactly one site, `terminals.ts:317`, always with a truthy colour, never null. Fix: delete the file; if one thing deserves a new test it is the ordering, as a jest test over `reconcileColours` with a stubbed `IColourfulTabs`. Net -93 lines.
- **[MINOR] `_inheritColour`'s third parameter is redundant** - `src/core/panel.ts:925-935`; all three call sites (`:836`, `:890-894`, `:992-996`) pass `session.session_id`, which is the same session `parentColour` was computed from at `:784` (`colourFor(session)` defaults to `session.session_id`, `terminals.ts:353-354`). Decide the ladder once at `:784` - `const parentColour = this._descriptor.colourSource === 'native' ? this._colours.get(session.session_id) : this._terminals.colourFor(session);` - and `_inheritColour` collapses to a pure forward; delete it and call `this._colours.inherit(parentColour, id)` directly. -1 method, -1 branch, -3 arguments; the explanatory comment moves to `:784`.
- **[MINOR] Orphaned import created by this diff** - `tests/test_colour_store.py:14` `import tornado`; its only use was the deleted `pytest.raises(tornado.httpclient.HTTPClientError)`. Delete the line.
- **[MINOR] Release is advertised but has no UI path** - `docs/acc-crit-…:244` ("Releasing the override hands the conversation back to its default") and the spec/test names are reachable only by HTTP: the first paint releases the menu entry, colourful-tab's `colourful-tab:clear` command mutates the DOM classList only (`lib/index.js`, clear command) while the API colour lives on `title.className` and is repainted from the store every 30 s. For a native provider this is new - once the user touches a claude tab, `/color` is shadowed permanently. That permanence IS the stated requirement, so **no affordance should be built**; flagging so the operator prices it, and so the "not a one-way door" phrasing in `tests/test_colour_store.py:150` and `spec:90-91` is understood as server-side only.

## Convention census - who steers off `colourSource`

| Site | Role | Status |
|---|---|---|
| `src/core/colour.ts:77-93` `effectiveColour` | TS ladder | canonical |
| `core/routes.py:185-199` `_effective_colour` | Py ladder | canonical, now identical to TS |
| `src/core/terminals.ts:338,357` | feeds the flag into the ladder | correct |
| `src/core/panel.ts:931` | native fork carve-out | correct behaviour, wrong place (see MINOR) |
| `core/routes.py:549` | native fork carve-out | dead, contradicts the TS carve-out |
| `core/store.py:379-386` `default_colour` | per-provider default only | correct |

No provider is named in core on either tier - requirement 4 passes.

## What is already consistent
- The two ladders now resolve identically (`colour.ts:77-93` vs `routes.py:199`), and the Python side got there by deleting a branch rather than adding one.
- Read-before-paint ordering in `reconcileColours` is right against the real `releaseMenuColour` mechanism, and the `userSet && !sessionId` bail is the correct instinct - it just uses the wrong id (finding 1).
- The store stays a dumb per-provider map (`colour_store.py`) with the policy in exactly one place per tier; no new route, flag, setting or public method was added for this change.
- `_MAX_COLOUR_LEN`, `TAB_COLOUR_IDS`, `COLOUR_INTERVAL_MS` all remain named constants in their tier's own module - no dial got inlined.
