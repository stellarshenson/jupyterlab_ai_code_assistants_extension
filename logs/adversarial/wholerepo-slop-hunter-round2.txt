I'll start by reading the adversary file and my round-1 report.

VERDICT: DO-NOT-SHIP (12 findings) - all fourteen round-1 cuts landed and verified clean, but the fix round left one lint error that reddens `jlpm lint:check`, two zero-reference interfaces, a mutation-dominated new test, and the comment duplication you asked me to press on is now twelve restatements of five rationales; ~57 lines removable.

## Round-1 re-confirmation (all fourteen)

Verified against the live tree, not taken on report: acc-crit is 407 lines with 2 remaining `criterion added` occurrences and the fact stated once at `docs/acc-crit-jupyterlab-ai-code-assistants.md:6`; `feature-parity.md`, `research-source-extensions.md`, `HARVEST.md` are `D` in the index; `descriptor filtering` and `enabledAndAvailable` return zero hits tree-wide and the Testing matrix cell at `:327` is `-`; `colour.spec.ts` now holds one `does not let an answer older than a CONFIRMED write speak for it`; the Galata file holds only the verbatim activation test; `summary`/`first_prompt`/`created`/`modified` are gone from `ISession` with `gemini.py:589`/`:643-644` intact; `load_overrides` returns zero hits; `bgChip`, `branchBadge` and `IHookContext` are gone, `_bgBadgeFor` has one caller and the CSS class is still live at `style/base.css:381`. The `_pending` comment (`colour.ts:409-411`) now names `forget`, and `README.md:43` reads "session name or by path". Baseline reproduced in a scratch copy: Jest 81/81, pytest 147+2 skipped, `tsc --noEmit` exit 0. Three cuts I re-tested rather than trusted: the two dead interfaces below, the mkdirp decline, and the `resolvedLaunchModeEntry` cases.

## Findings

- **[CRITICAL] `DEFAULT_SIDEBAR` is substituted at two sites where the literal meant "right", and the fix breaks the lint gate** - `/home/lab/workspace/private/jupyterlab/jupyterlab_ai_code_assistants_extension/src/index.ts:338` and `:351`, axis 2. `npx eslint . --no-cache` exits non-zero: `351:65 error Replace ·?·'left' with ⏎... prettier/prettier`, caused precisely by the new constant pushing that ternary past the print width. `jlpm lint:check` runs `eslint . --cache`; the "eslint 0 errors" in your brief does not hold for this tree - I reran it three ways. Beyond the formatting, the substitution is semantically wrong at both sites: `:338` reads `next === 'left' || next === DEFAULT_SIDEBAR`, which is a *membership* test over the `Sidebar` union, and `:351` reads `... === 'left' ? 'left' : DEFAULT_SIDEBAR`, which is the *other* branch. Set `DEFAULT_SIDEBAR = 'left'` and `:338` becomes `next === 'left' || next === 'left'` (the right sidebar stops being accepted) and `:351` returns `'left'` unconditionally. The knob is only correct for the one value it holds, which is the definition of a dial that hides a bug. Cut: keep `DEFAULT_SIDEBAR` at `:115`, `:336` and the `setting<string>('sidebar', DEFAULT_SIDEBAR)` argument at `:351`; restore the literal `'right'` at the two comparison/ternary positions. Load-bearing check: cleared - a local `const`, no export, no schema key. Behaviour today is unchanged; the lint error is not.

- **[MAJOR] Twelve restatements of five rationales across six files - ~31 lines, and they have already drifted** - axis 4/5. This is the finding you asked me to name exactly. Five canonical sites, twelve copies:

  *Paint hazard* - canonical `src/core/terminals.ts:351-357` (the only place the code decides **not** to paint) and `jupyterlab_ai_code_assistants_extension/tests/test_colour_store.py:325-330` (the Python canonical). Cut: `src/core/colour.ts:191-192` the trailing clause "and the pass that picked it goes on to paint the tab from this cache, which is what makes the companion extension release its own record" (-2); `src/core/colour.ts:272-274`, keeping "Answers whether the store now HOLDS the colour. The caller needs that:" plus a pointer to `reconcileColours` (-3); and in `src/__tests__/colour.spec.ts` the trailing hazard clause at `:160-161` (-1), `:242` (-1), `:268-269` (-1), `:338-339` (-1), `:381-382` (-2). The spec already states it in full at `:136-139`; the four cuts in `describe('ColourStore origins')` lose their in-scope anchor, so add one sentence to the file header at `:1-8`, where the paint hazard is the whole file's premise. Drift is already live: "release its own record" / "drop the last record" / "drop its own record" / "dropped its own record" are four wordings of one fact.

  *Hand-set vs inherited* - canonical `jupyterlab_ai_code_assistants_extension/core/colour_store.py:10-16` (Python), `src/core/colour.ts:142-144` (TS), `src/__tests__/colour.spec.ts:208-210` (spec). Cut `colour_store.py:71-72` "The release affordance is offered for exactly the second: an inherited tint is the branch's own identity, not a preference to take back" (-2, keep `:72-74`); `colour_store.py:164-165` (-3); `src/core/colour.ts:388-390` (-3). The last two are near-byte-identical across runtimes: "Inherited, never hand-set: the user chose this colour for the parent's tab, not for a conversation that did not exist yet" against "Inherited, not hand-set: the user picked this colour for the parent's tab, not for a conversation that did not exist yet". Keep the `hand_set` / `handSet` **parameter** contracts at `colour_store.py:119-121` and `colour.ts:266-269` - those are the signature, not the rationale.

  *Pending gate* - canonical `src/core/panel.ts:2119-2123`, the only site that applies the rule. Cut `src/core/colour.ts:151-154` (-4, keep `:155-156` "Not consulted by the capture gate...", which is a different fact about the other reader) and `src/__tests__/colour.spec.ts:403-405` (-2).

  *Legacy store carries no origins* - canonical `colour_store.py:41-50`, which owns the asymmetry argument. Cut `src/core/colour.ts:195-198` (-4, keep "A store with no hand-set ids named predates the distinction. Nothing in it counts as hand-set.") and `src/__tests__/colour.spec.ts:227-230` (-2).

  *One read for both halves* - canonical `colour_store.py:72-74`. Cut `jupyterlab_ai_code_assistants_extension/core/routes.py:496-497`, which restates the callee's own contract at the call site where the caller has no choice to make (-2).

  Load-bearing check: cleared - comments only; every WHY survives once, at the site that acts on it. This is where I disagree with a blanket "leave it": `colour.ts` is 143 comment lines of 417 (34%) against `panel.ts`'s 385 of 2282 (17%), and four of the twelve copies are already worded differently from each other.

- **[MAJOR] `IFavouriteRequest` and `IForkRequest` have zero references anywhere** - `src/core/types.ts:159-162` and `:204-208`, axis 1, 10 lines with blanks. Delete-test run in a scratch copy: both removed, `tsc --noEmit` exit 0, Jest 81/81. Load-bearing check: cleared three ways - `src/index.ts` exports only `default plugin`, so `lib/index.d.ts` never re-exports them (not published API); TypeScript interfaces erase at compile, so no reflection path exists; `graphify affected` over `tmp/graphify-out/graph.json` returns "No affected nodes found" for both, and grep across `src`, `ui-tests/tests`, `schema`, `docs` and the Python package hits only the two definitions. Note the neighbours are *not* dead - `IFavouriteResponse`, `IForkResponse`, `IDisposalReport` all have live readers; these two request shapes are the ones the panel builds inline.

- **[MAJOR] One of the four new `resolvedLaunchModeEntry` cases kills no mutant its siblings do not** - `src/__tests__/launch-mode.spec.ts:71-78` (`names the mode that actually wins, not both`), axis 3, 8 lines. I ran six mutants against `src/core/modes.ts` in the scratch copy: dropping the `split('=')` , removing the `!token` guard, changing the `?? null` fallback, making `find` skip boolean modes, flipping the precedence loops, and dropping the `=${value}` from the enum arm. `:71-78` failed on exactly two of them, and both were also failed by another test - the split mutant by `names the enum too` (`:67-70`), the precedence mutant by `resolveLaunchMode`'s own `lets a non-default enum beat a boolean that is on` (`:32-34`). It re-asserts precedence through a wrapper that does not implement precedence. The other three earn their place and I checked each: `:61-65` uniquely killed the boolean-skipping `find` mutant, `:80-85` uniquely killed the null-guard mutant, `:67-70` uniquely killed the `find`-on-raw-token mutant. Load-bearing check: cleared - deleting it leaves Jest at 80 with no mutant escaping.

- **[MINOR] Four dead keys survive in Claude's fallback session dict** - `jupyterlab_ai_code_assistants_extension/providers/claude.py:648`, `:649`, `:651`, `:652` (`"summary"`, `"firstPrompt"`, `"created"`, `"modified"`), axis 1, 4 lines. The row emission that read them was cut this round; grep over the whole file shows `latest` is now read only for `sessionId`, `messageCount`, `fileMtime`, `gitBranch`, `projectPath`, `customTitle`, `agentColor`. Load-bearing check: cleared - the dict is a local default, never serialised whole, never `getattr`-reached. Leave the real index entries alone; only the hand-built fallback carries them now.

- **[MINOR] Gemini computes two timestamps nobody reads** - `jupyterlab_ai_code_assistants_extension/providers/gemini.py:305-306` (`"created": meta.get("startTime")`, `"modified": meta.get("lastUpdated")`), axis 1, 2 lines, plus `"startTime"` and `"lastUpdated"` in `_META_KEYS` at `:110` which exist only to feed them. Zero readers of either key after the wire-field cut. Load-bearing check: cleared, and note the near-miss - `:376-379` also names `startTime`/`lastUpdated`, but on the raw file record inside `stamp()`, a different dict; that path stays. `"summary"`, `"kind"` and `"sessionId"` in `_META_KEYS` are all read (`:589`, `:489`, `:298`) and stay.

- **[MINOR] The mkdirp decline does not survive the test you asked me to run** - `package.json:88`, axis 6, 2 lines. The premise is that dropping it desyncs the lock and only a version-bumping Makefile target can resync. Both halves fail. `mkdirp@1.0.4` is also a dependency of `jest-junit`, so `node_modules/mkdirp` stays in the tree either way; removing the root devDependency is a matching two-line edit, `package.json:88` and `package-lock.json:39`. I ran it in the scratch copy and then `npm install --package-lock-only --offline --ignore-scripts` to see whether npm agreed the lock was in sync: it regenerated with **no network**, and the only differences from the pre-edit lock were the removed line and `"version"` at the top level. No Makefile target, no version increment. Load-bearing check: cleared - zero references in `src/`, `scripts/`, `ui-tests/`, all 27 npm scripts, the `Makefile`, `pyproject.toml` and every workflow; not a binary any script invokes.

- **[MINOR] The lockfile already carries the drift the decline was protecting** - `package-lock.json:3`, axis 6. `package.json` says `1.0.0`, `packages[""].version` says `1.0.0`, top-level `"version"` still says `0.6.17`. Whatever bumped the version patched one of the two fields. Self-healing at the next `make build` (`jlpm install` after `increment_version`), but the tree as it stands is not the clean state the mkdirp decline assumed - and the offline `--package-lock-only` regeneration above fixes it as a side effect.

- **[MINOR] Three untracked `graphify-out` trees, ~10 MB, and no `.gitignore` rule for any of them** - `.gitignore`, axis 6. `git check-ignore` returns nothing for `graphify-out/` (1.6 MB, a bare `cache/` with no graph in it), `tmp/` (8.3 MB, holding the real `tmp/graphify-out/graph.json`) or `ui-tests/tmp/`. `git status` lists all three as `??`, so one `git add -A` commits them. One line - `graphify-out/` plus `tmp/` - removes the risk; the root and `ui-tests/` copies are duplicate caches of the graph that already lives at the sanctioned path.

- **[MINOR] A test docstring cites a file this round deleted** - `jupyterlab_ai_code_assistants_extension/tests/test_provider_stores.py:5`: "The trees below mirror the layouts recorded in ``docs/research-source-extensions.md``". That file is `D` in the index as of this round. Axis 8 - a reader following the pointer finds nothing. One-line reword ("mirror the layouts of the retired standalone extensions"), not a deletion.

- **[MINOR] A test docstring claims coverage the test does not provide** - `jupyterlab_ai_code_assistants_extension/tests/test_provider_stores.py:74-75`: "``updatedAt`` is compared and subtracted, and ``fileMtime`` is coerced". The test plants a bad `updatedAt` only. I removed the `fileMtime` guard at `providers/claude.py:658-665` in the scratch copy and pytest stayed at 147 passed - nothing reddens. Either the docstring names one field, or the test plants a string `fileMtime` too. I am **not** asking for the guard to be cut: it prevents a real `ValueError` out of `int()` and the reproduced `updatedAt` twin next door proves the class is live. It is the only new branch in this round that no test reaches, which is worth knowing before it is trusted.

- **[MINOR] (taste) Three exports still with no cross-file consumer** - `src/core/request.ts:8` `API_NAMESPACE`, `:60` `providerPath`, `src/core/terminals.ts:34` `showLaunchSpinner`. Re-confirmed by an export scan over `src/` plus `ui-tests/tests/`: each has exactly two in-file references and zero elsewhere. `src/index.ts` exports only `default plugin`, so they are not published API; dropping the `export` keyword narrows the emitted `lib/core/*.d.ts` and nothing else. Standing declined is defensible - I record it as re-confirmed fact, not a demand. Same for `graphify` as a tool: it agreed with grep on the two dead interfaces this time, but I still grep-verified every claim above.

**Considered and cleared** (so you know they were checked, not missed): the 117 identical closure `log:` lines in acc-crit stay - they are the checker's per-criterion requirement now that the `criterion added` twin is gone, and cutting them trips 111 warnings for no structural gain; `ForkStrategy: 'none'` has no TS producer but is the live `Capabilities` default at `core/registry.py:71` and is compared by `test_descriptor_parity.py:87`; every `.jp-AiAssistantsPanel-*` class in `style/base.css` has a TS reference (scanned all 400+ selectors, zero orphans); `pyflakes` over the whole Python package is silent.

**Total: ~57 removable lines** - 31 comment, 10 type declarations, 8 test, 6 dead dict/type keys, 2 dependency - plus one lint error to fix, one `.gitignore` line to add, and two docstring rewords.

## What earns its place

- **`write_json_atomic`'s 15-line docstring and its threaded test** (`core/store.py:72-100`, `tests/test_store_isolation.py:183-223`). I reverted `mkstemp` to the shared `<name>.tmp` in the scratch copy and pytest went red on exactly that test, with the `FileNotFoundError` the docstring predicts. It runs in 0.07 s - the slowest call in the suite and still cheap. The docstring carries a measured number ("a third of writes lost with three writers") rather than a restatement of the code.
- **`terminals.ts:340-346` and `:351-357`** - two *different* facts at the paint site (Lumino rebuilding the tab element's classes from `title.className`, and the companion releasing its menu entry on any applied colour). Neither is inferable from the code, and I am proposing every other copy be folded back into these.
- **The pty-dimensions and shallowest-wins tests** (`test_routes.py:356-378`, `:507-533`). Mutating `dimensions=(1, 1)` to terminado's own `(24, 80)` reddens the first; both docstrings explain a mechanism - SIGWINCH not firing on a no-op resize, a claude terminal that spawned `codex` as a tool - that the assertion alone cannot convey.
- **`core-neutrality.spec.ts` and `test_descriptor_parity.py`** - the architecture's only two enforcement points, and the parity test grew a genuinely load-bearing assertion this round: `legacyPluginId` carries suppression in TypeScript and the migration folder name in Python off one string, and drift between them is silent in both directions.