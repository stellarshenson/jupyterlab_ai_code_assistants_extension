# Claude Code Journal

This journal tracks substantive work on documents, diagrams, and documentation content.

---

1. **Task - Project initialisation** (v0.1.0): Created `jupyterlab_ai_code_assistants_extension` as a new JupyterLab 4 extension and stood up its Claude Code configuration<br>
   **Result**: Scaffolded from the `jupyterlab/extension-template` copier template `v4.6.4` in `frontend-and-server` kind with settings and tests enabled - TypeScript frontend in `src/`, `jupyter_server` extension in `routes.py`, settings schema in `schema/plugin.json`, Jest and Playwright tests. The extension consolidates `jupyterlab_claude_code_extension`, `jupyterlab_codex_extension` and `jupyterlab_kimi_code_extension` into one package with a joint settings page, a per-assistant right-toolbar panel and per-assistant toggles defaulting to all-on. Wrote `.claude/CLAUDE.md` as a thin overlay importing the user- and workspace-level configs without duplicating them, adding Makefile-only lifecycle rules, the Makefile-currency check against `@utils/jupyterlab-extensions/Makefile`, the paired `package.json` / `package-lock.json` commit rule, and the required skills `jupyterlab-extension` and `my-browser`. Local `Makefile` verified byte-identical to canonical version 1.36. Rewrote `README.md` with the full badge set and a six-point feature summary.

2. **Task [Extended] - Acceptance criteria and architecture decisions** (v0.1.0): Settled the provider-registry architecture and wrote `docs/acc-crit-jupyterlab-ai-code-assistants.md` before any implementation<br>
   **Result**: Surveyed the three assistant extensions to ground the base choice - `jupyterlab_claude_code_extension` v1.2.73 (3041 TS / 2124 py lines) leads `codex` v0.6.12 and `kimi` v0.7.8, and all three carry an identical module shape (`icons`/`index`/`request`/`types`/`widget` over `routes.py`/`sessions.py`) within 40 TS lines of each other, confirming they are forks of one design rather than independent builds. Claude adopted as base. Three decisions taken with the Star Colonel: one consolidated acc-crit doc over per-feature files, since a shared core makes the shared-vs-variant split only legible in one read; live enable/disable of assistants with no JupyterLab reload, matching how the base already applies `presentationMode` live and costing widget teardown, poll-stop, command-removal and tint-clearing logic; and provider modules over a shared Python core (`core/registry.py` dispatching `/providers/<id>/...`) rather than three near-identical route trees. Purpose clarified mid-session as retirement of the standalone extensions, not coexistence, which promoted feature parity, settings and favourites migration, and duplicate-panel conflict detection to first-class criteria. Doc carries 106 criteria across 11 sections including a Galata tier grounded in the `jupyterlab-extension` skill's testing reference - port knob via `JLAB_TEST_PORT`, unconditional `reuseExistingServer: false` (the base uses `!process.env.CI`, unsafe for a suite that drives terminals), serialised workers, redirect-not-`tee` so a dead server reports failure, and a mutation check proving each tier can still go red. Validates clean under `acc-crit.py check`.

3. **Task [Extended] - Bearings research, Gemini provider, colour rules** (v0.1.0): Deep-mapped all three source extensions and the Gemini CLI, then extended the acceptance criteria with a Colour section and a Gemini Provider section<br>
   **Result**: Three parallel Explore agents mapped the ground. The base extension's mechanism: status probe hard-gates activation on `shutil.which("claude")`, one 2571-line widget class over a private `CommandRegistry` (invisible to the palette), 30s row polling coupled by comment contract to a 35s server-side bg-agents cache, terminal reuse keyed on conversation id resolved from `/proc` tree walks plus `~/.claude/sessions/<pid>.json`, and a `.jl-current` pin subsystem compensating for Claude's lossy path encoding. Ten coupling clusters resist generalisation, the deepest being background-agent handling threaded through every layer and the argv grammar in both construction and `/proc` cmdline re-parsing. The derivatives diverge more than expected: Codex reads a read-only SQLite thread index (`state_5.sqlite`) keyed by cwd with no encoded-path concept and shells destructive ops to `codex archive/delete`; Kimi is a directory tree under `~/.kimi-code` with `wd_id` registry, server-side `copytree` forks and FNV-1a derived colours. Gemini CLI 0.54.4 installed via `lab-utils install-ai-assistant/google-gemini-cli`; bundle-source inspection proved the store (`~/.gemini/projects.json` registry mapping project root to short id, chats at `tmp/<shortId>/chats/*.json`) and flags (`--session-id`, `--session-file`, `--yolo`, `--approval-mode`, no fork). Two new Star Colonel colour rules captured as a Colour section: tab colour changes on colour-less assistants write back to an extension-owned store as if the assistant changed colour, and branches always inherit the parent's effective colour. Doc now 120 criteria across 13 sections, clean under `acc-crit.py check`; Gemini added to `.claude/CLAUDE.md` context and README.

4. **Task - Implementation workflow designed and launched** (v0.1.0): Authored the resumable multi-agent implementation workflow and launched it with Opus executors<br>
   **Result**: Research condensed into `docs/research-source-extensions.md` as the durable priming artifact (capability matrix, base mechanism, derivative deltas, Gemini store, coupling points, build gotchas) since conversation-held maps die with the container. Workflow script at `.claude/workflows/implement-ai-code-assistants.js`: Core (TS + Python parallel), Providers (four parallel, two files each), Integrate (`make install` fix loop, max 4 rounds), Tests (pytest/Jest then Galata in a dedicated `ui-tests/.venv` - live system untouched, standalone extensions absent by construction, plus a Claude regression spec ported from the original 1088-line suite), Review (read-only acc-crit audit). Every agent is idempotent against on-disk state, so container death is survived by re-running the same script - run id `wf_a4376a8b-03e`, resume via `Workflow({scriptPath, resumeFromRunId})` in-session or plain re-run cross-session. Added criteria: per-provider unsafe mode under each assistant's own name, Claude regression, venv isolation, adversarial review gate (architect + bug hunter, to run post-workflow). Seeded `docs/defects.md` per the defects-tracking skill. Both validators clean.

5. **Task [Extended] - Implementation campaign complete, defects fixed, criteria closed** (v0.1.7): Ran workflow `wf_a4376a8b-03e` through two passes to a green, audited implementation<br>
   **Result**: First pass (10 Opus agents, 83 min, 2.07M tokens, 0 errors): TS core decomposed the base's 2571-line widget into panel/popup/terminals plus colour-store client with zero assistant names in core; Python core with pkgutil registry, store-adapter ABC and full route tree; four provider pairs in parallel; `make install` green on integration round 1 of 4; 111 pytest + 59 Jest with a real mutation check (five guards broken in turn, each test went red, restoration diff-verified); Galata 16/16 in a dedicated `ui-tests/.venv` with the three standalone extensions verifiably absent and the original Claude suite's 7 regression tests ported intact. Test tiers and the pre-fix review surfaced eight defects (DEF-1..8) - the worst being frontend/server route-shape mismatches, `provider_disabled` gating dead on a nested-vs-flat settings key read, plugin load failing outright without `jupyterlab_colourful_tab_extension`, migration never invoked, and Gemini resuming by positional `--resume`. Stopped the resumed run mid-flight to widen fix orders from four defects to seven - cached phases made the restart free. Fix pass closed all eight; re-verify green at 0.1.6 (125 pytest, 16/16 Galata against the force-reinstalled wheel); post-fix review: 8 MET / 4 PARTIAL / 0 UNMET. Main-session follow-ups at 0.1.7: schema generator fixed for Node ESM (globs `lib/providers/*.js`, prettier-normalised, wired into both build chains), CLI-absent console warning, `tests/test_descriptor_parity.py` binding the TS and Python descriptor declarations (2 passed), package.json URLs and check-links badges corrected. Closed 120 of 124 acceptance criteria with evidence; open: feature parity (audit agent running), adversarial gate (next), metapackage repoint and README marking (deferred to release, reasons logged).

6. **Task [Extended] - Adversarial review gate closed** (v0.1.15): Ran the devils-advocate loop (architect, bug-hunter, ux-designer) through rounds 1-4 with four fix batches between, closing the acc-crit adversarial gate<br>
   **Result**: Round 1 whole-repo sweeps returned DO-NOT-SHIP from all three (19+7+15 findings), triaged into batches. UX batch: keyboard access on session rows, one-shot error copy split (`_showActionError`), absolute-time tooltips, reduced-motion CSS plus code-side spin-floor skip, aria labels, warning glyph on unsafe launch variants, two-step arming delete when trash off, 24px hit targets, and launch-mode precedence extracted to `src/core/modes.ts` (off-default enum beats boolean) - a separate module because `panel.ts` drags untransformed ESM Jest can't import. Batch 2 (26 rulings): `owns_pid` store hook killing gemini's node-comm collision, pin-clear on plain new-session launch, `removed_ids` wire honesty, migrate skip on null settings, trash-failure raise plus `send2trash` dep, stat-race guards; deletions dominated - fiction wire fields, `scope:"parallel"`, zero-implementor seams, the top-level `routes.py` shim - net -103 lines. Grep-before-delete kept `ClaudeStore.default_colour` (real `TerminalHandler` user) and declined the `CLAUDE_CONFIG_DIR` half (legacy never read it). The `jupyterlab-extension` skill checklist caught the Node-26 duplicate-chalk landmine: `chalk 4.1.2` pin plus a real npm `overrides` block, isolated `python -m build` now green. Pinned re-confirm rounds: architect SHIP round 2; bug-hunter SHIP round 3 after `remove()` returns actually-disposed ids and gemini matches argv basenames not substrings; ux SHIP round 4 after the switch affordance moved off the row onto the label span (nested-interactive fix) with a `deleting` guard. DEF-10..29 registered and closed; evidence in `logs/adversarial/`. Versions 0.1.10→0.1.15; Jest 61, pytest 132, Galata 16/16 in `ui-tests/.venv`, eslint 0 errors.

7. **Task - Release 0.6.15 and retirement** (v0.6.15): Shipped the extension to npm and PyPI at the Star Colonel's version 0.6.15 and executed the retirement of the standalone extensions<br>
   **Result**: Set `package.json` to 0.6.14 so `make publish`'s own increment landed exactly on 0.6.15 - no hand-edited release version. Committed the full working tree (98 files, lockfiles paired) as the consolidation feat commit, created `github.com/stellarshenson/jupyterlab_ai_code_assistants_extension` via the GitHub API (vault cold, PAT taken from its documented `~/.git-credentials` store without touching the transcript), wired `origin`, pushed. `make publish` green: npm `jupyterlab_ai_code_assistants_extension@0.6.15`, PyPI `jupyterlab-ai-code-assistants-extension 0.6.15`, post-publish metadata commit pushed. Retirement: `stellars_jupyterlab_extensions` 1.1.5 published with claude/codex deps replaced by the consolidated package (kimi was never listed) and a README bullet naming it as superseding the standalones; superseded notices committed and pushed atop the claude/codex/kimi READMEs. Closed the final acc-crit criteria - Metapackage updated, Standalone repos marked - leaving 0 open of 124. Remaining known unknown: live `claude --fork-session` agent-colour inheritance still needs an interactive session to verify.

8. **Task - Official brand icons and README** (v0.6.17): Replaced the Kimi crescent and hand-drawn Gemini spark stand-ins with the official marks and rewrote the README in the claude extension's style<br>
   **Result**: Kimi now wears the official "K" glyph with its accent square from Moonshot's Branding-Guide repo (the `k-only` asset - their full icon is a 1MB starfield texture, unusable at 16px); Gemini wears the silhouette path of Google's 2025 icon with the gradient fill dropped. Both monochrome `jp-icon3` like every sidebar icon, in `src/providers/kimi.ts` and `gemini.ts`. Verified in a live JupyterLab from `ui-tests/.venv` after grepping the wheel for the new path data (hatch had skipped a stale rebuild); Jest 61, pytest 132, Galata 16/16. README rewritten from the stale "early development" stub to the claude extension's shape - why-section, full feature list, migration section, webpack/chalk warning - with a panel screenshot at `.resources/screenshot.png` captured from a demo-seeded lab so no real project names leak.

9. **Task - Hand-set tab colour beats `/color`** (v1.0.0): Made a colour the user sets on a terminal tab override a native-colour assistant's own, and set the version to 1.0.0<br>
   **Result**: Claude's colours were read-only: the route refused a write with 400 `colour_owned_by_assistant`, `_effective_colour` ignored the store for `native`, and `reconcileColours` skipped the write-back - so a hand-set tab colour was repainted within one 30s poll. Now the store overrides for EVERY provider in both tiers, and the write-back captures the menu colour BEFORE applying anything, which matters because `jupyterlab_colourful_tab_extension`'s `setColour` calls `releaseMenuColour` and deletes the localStorage entry the moment it is handed a colour; a hand-set colour that cannot be attributed to a conversation is left alone rather than destroyed uncaptured. `_inheritColour` inherits only a genuine override for native providers - copying the parent's assistant-chosen tint would pin a fork and shadow its own `/color`. Version hand-set to 1.0.0 at the Star Colonel's order, so `make build` is off-limits until publish. pytest 134, Jest 61, Galata 19/19 with the three new specs proven to go red against the restored refusal.

10. **Task [Extended] - Colour override survives three adversary rounds** (v1.0.0): Ran the devils-advocate panel (architect, bug-hunter, ux-designer) over the hand-set-colour change; rounds 1-3 all returned DO-NOT-SHIP from all three, converging each round on one defect<br>
    **Result**: Round 1 added a release affordance - a hand-set colour was a one-way door because `setColour` consumes the companion extension's own record, so `/color` would never show again - plus `ColourStore.set` returning a boolean with cache rollback, since a paint after a failed write destroys the colour. Round 2: the release keyed on the ROW's current conversation while the capture keyed on the terminal's OBSERVED one, so a colour set on a terminal from `Open Branched Conversation` was unreachable; the menu now settles its target set at open time over the row plus its branches. Round 3 broke two locked criteria - the store conflated "hand-set" with "inherited at fork time", so one click un-inherited every branch of a `derived` provider and scattered it to `fnv1aColour(ownId)`; on the Star Colonel's ruling the state file gained `overrides` beside `colours` (`hand_set` on the POST, absent key means legacy-hand-set) and the release filters on it. Bug-hunter also reproduced a `_save` swallowing `OSError` behind a 200, defeating the whole confirmed-write gate: the route now answers 500 `colour_store_unwritable` and the client verifies the returned map rather than trusting "did not throw". Release moved to the bulk DELETE so it cannot half-apply, menu open raced against a 250ms budget, label carries its count. pytest 141, Jest 71, Galata 21/21, the inheritance and branch-release specs both mutation-checked red.

11. **Task [Extended] - Round 4 of the colour adversary loop** (v1.0.0): Third fix batch over the hand-set-colour change; all three adversaries returned DO-NOT-SHIP again, this time on the migration rule and a destructive menu race<br>
    **Result**: The round-3 origin marker shipped with a false premise - `_read` treated an `overrides`-less file as all-hand-set "because the tab write-back was its only writer", but `inherit_colour` has written fork tints through the same file since the shipped `8e62249`, so an upgrader's `Reset Tab Colours` would un-inherit exactly as before and the first write would cement it. Architect and bug-hunter both reproduced it; a legacy file now holds nothing hand-set, since offering an inherited tint destroys a branch's colour with no undo while withholding a release costs one re-pick - and the capture gained `&& isOverride(...)` so re-picking the same colour is no longer swallowed as a no-op. Bug-hunter's other MAJOR: `Menu.clearItems` CLOSES an attached menu, so a slow `_openContextMenu` losing the race to a second right-click shut the visible menu, reopened its own elsewhere with no row highlighted, and pointed `Remove` at the abandoned project - now gated by a `_menuGeneration` token, with the branch fetch moved inside the same 250ms race that already covered the colour work. Also: `_absorb` no longer takes a POST/DELETE answer whole (two writes in flight could wipe each other's cache entry), `reconcileColours` catches internally instead of four rejection disciplines across five call sites, the sessions listing reads the colour file once rather than once per row, and a malformed colour answers 400 `colour_invalid` rather than the 500 that means the disk is unwritable. pytest 143, Jest 72, Galata 21/21.

12. **Task [Extended] - Round 5 of the colour adversary loop** (v1.0.0): Fourth fix batch; all three adversaries converged on the round-4 generation token being stamped after the state it protects<br>
    **Result**: `_openContextMenu` guarded the rebuild but not the branch continuation, which wrote `_lastBranches` and both submenus unconditionally - so a slow row losing the race to a fast one refilled the visible menu with the other project's conversations, and `_resettableColours` reading those instance fields could drop project A's colours from a menu opened on B (`drop_colours` is keyed by provider, not project). Fixed by fetching into a local and moving every shared write past the guard, into a new `_fillBranchSubmenus`. Architect and ux both killed `branchesKnown`: hiding the reset when the branch list had not arrived removed the only exit from a hand-set colour, and ux timed `claude agents --json` at 318-334ms against the 250ms budget - Claude alone pays that spawn, and its 35s cache expires between right-clicks. Scope now narrows to the row's own conversation, which the count states exactly, and `MENU_COLOUR_BUDGET_MS` became `MENU_STATE_BUDGET_MS = 1500` to bound a hang rather than clip honest work. Bug-hunter's three repros drove the cache fixes: `_snapshot` is per-key (a failing write was rolling back a concurrent successful one whose tab had already been painted, releasing the companion's record), and `_absorb` now skips ids with a write in flight. Five artefacts saying a native fork inherits "only a HAND-SET override" were false - the code inherits any stored colour, which `Edge: branch of a branch` requires - and the README gained the override, the reset item and both caveats, its only user-facing surface. pytest 143, Jest 74, Galata 21/21, both new race tests mutation-checked.

13. **Task [Extended] - Round 6 of the colour adversary loop** (v1.0.0): Fifth fix batch; four MAJORs, all in the cache-coherency model the round-5 fixes introduced, all reproduced by the adversaries against compiled copies of `colour.ts`<br>
    **Result**: Round 5's `_inFlight` set was keyed on "a request is on the wire" when the invariant is "this answer predates a write to that id" - so a GET issued before a write and answered after it still wiped the colour, a second write to one id cleared the first's guard, and an older write's rollback undid a newer successful one. Replaced with a monotonic `_writeSeq`, a per-id `_writeStart` map and a per-id `_pending` COUNT: `load` stamps the counter when it asks, `_absorb` keeps the cache's own value for any id written since (or still unanswered), `_snapshot` skips ids an overtaken write no longer owns, and the success path re-asserts nothing once overtaken. Architect found the same fix had opened a worse hole in the other direction - preserving in-flight ids made an UNCONFIRMED write visible to the `held` gate, so an overlapping reconcile pass (five triggers, no mutual exclusion) could paint on it and release the companion's record before the POST failed; the gate now also requires `!isPending`, deliberately not folded into `isOverride`, which drives the release affordance. Ux measured the right-click silence at up to 1.5s with a retry that supersedes and restarts it: budget cut to 800ms and the losing branch fetch kept per project, so a slow listing costs one degraded menu rather than the submenus for good - a deliberate deviation from architect's "cut the comment, do not retain", framed as a project-keyed cache rather than `_lastBranches`. README's colour bullet gained the Codex/Kimi unobservable-id caveat and the per-project scope. pytest 143, Jest 77, Galata 21/21.

14. **Task [Extended] - Round 7 of the colour adversary loop** (v1.0.0): Sixth fix batch, and the first where the winning fix was a deletion - the optimistic cache write that rounds 5 and 6 had been building machinery to police<br>
    **Result**: Architect reproduced a phantom: two overlapping writes that both FAIL leave the newer one restoring the older one's unconfirmed value, because its restore point was captured after the older had already written the cache - and the capture gate then reads that phantom as held, paints, and the companion extension drops the last record of the colour. Rather than another ordering rule, `ColourStore` stopped writing the cache before the server confirms; nothing reads it before the promise settles, so the optimism bought nothing. That deleted `_snapshot`, all four rollbacks, the `_pending` count and `isPending` (with its clause in `terminals.ts`), and collapsed bug-hunter's NEW-2 - a reset clicked inside the capture window, undone by its own refresh - since the item is no longer offered for an unconfirmed write. Bug-hunter's NEW-1 needed the one addition: two reloads in flight can be served in either order, so `load` carries a read sequence and an answer older than one already absorbed is discarded. Removing the `_writeStart` prune fell out of that - it was deleting the entry a still-flying write needs to know it is current. All three adversaries traced the same branch-cache defect: `branches()` answers null both for "no other conversations" and "fetch did not arrive", so a cleaned-up project served its deleted listing on every open for good; the fallback is now gated on the row having branches, and the cache is cleared when a conversation is deleted. Ux found the round-6 README caveat was itself false - reopening a bare Codex/Kimi conversation opens a NEW terminal, so the colour is not recovered - and the bullet went from 150 words to 97 with the pre-1.0.0 caveat moved into the migration section. `docs/defects.md` gained DEF-30 for architect's serialisation proposal, deferred with the caveat it did not price: no request timeout means a chain behind a hung GET stops capture entirely. pytest 143, Jest 78, Galata 21/21.

15. **Task [Extended] - Colour loop stopped, graph-led instead** (v1.0.0): Star Colonel called out the circling - each round's fix seeding the next round's defect - and directed graphify impact analysis before further changes<br>
    **Result**: The diagnosis is specific: rounds 5-8 were almost entirely self-inflicted, all inside `ColourStore`'s cache semantics, which was rewritten four times (optimistic write + rollback, then per-key rollback, then in-flight set, then write sequences) without a model of who reads that cache and when. Round 8's batch landed first - bug-hunter refuted the round-7 collapse (`isOverride` answers from the last CONFIRMED colour, so a capture REPLACING one leaves it true and the release is offered under an unanswered write), so the in-flight count returned as a release-side filter only; `forget` now includes ids whose only write is on the wire, so a conversation deleted mid-capture cannot keep an orphan colour; `_writeStart` is re-stamped at confirmation, closing architect's write-vs-read case; `_branchCache` is cleared on cleanup and fork; and the capture strips our own tint BEFORE the write, narrowing ux's Lumino class-rebuild flip. Then built the graph: 1324 nodes, 2440 edges, AST-only, no LLM. `graphify affected "ColourStore"` names the blast radius as exactly `colour.ts`, `terminals.ts`, `panel.ts` plus tests - every edit made was inside it - and a grep over that budget enumerates 17 consumer sites of the cache contract. Audited all 17: 15 satisfy "the cache holds only confirmed values", the remaining 2 are one narrow gap (a fork reading the parent's colour inside the capture window), registered as `DEF-31` and deliberately NOT fixed, since closing it means more machinery in the object that caused the churn. Adversarial loop stopped at round 8 rather than spawning round 9. pytest 143, Jest 81, Galata 21/21, tsc clean.

16. **Task [Extended] - Whole-repo adversarial sweep, graph-led** (v1.0.0): Four lenses over the whole project rather than another round on the colour change; 54 findings, fixed in graph-bounded batches with the graph refreshed after each<br>
    **Result**: Bug-hunter's worst was live: Gemini's `comm_name = "node-MainThread"` matches only Node 26 - `/proc/<pid>/comm` reads `MainThread` up to Node 24, and `@google/gemini-cli` declares `engines: >=20` - so on every Node the CLI actually supports its terminals were unidentifiable, spawning duplicates and never tinting, silently; the test had monkeypatched the literal, baking in the assumption. Now a set, parametrized over all three spellings. Also runtime-proven and fixed: both destructive routes ran the store inline on the IOLoop (`codex archive` measured 0.71s each, so a 20-conversation cleanup froze the whole server ~14s); one ISO string in Claude's own `sessions/*.json` raised out of the executor and 500'd the entire listing forever; and `write_json_atomic` shared one temp filename per state file, losing a third of writes with three writers - each fix mutation-checked against the exact exception the adversary reproduced. Ux's CRITICAL was a safety cue inverted: the warning glyph keyed off the menu's `force` argument, so with skip-permissions ON the plain `Resume` lost its glyph while the redundant marked duplicate kept one - now driven by `resolvedLaunchModeEntry` in `modes.ts`, tested there because `panel.ts` is not Jest-importable. Architect verified the registry claim by ADDING a fifth provider (compiled, generated settings, no core edit) and found the FNV-1a hash duplicated across runtimes with only the TS side pinned to literals; the Python side now asserts the same three. Slop-hunter removed ~290 lines - 124 duplicate acc-crit log lines, `feature-parity.md` (every file:line citation rotted), `research-source-extensions.md`, `HARVEST.md`, a Jest block testing a predicate defined inside the test file (`index.ts` has 0% Jest coverage, so no mutation could redden it), and a byte-identical duplicate test whose twin still kills the mutant. Declined with reasons: deleting `mkdirp` (would desync package-lock, and the only Makefile target that regenerates it increments the version off 1.0.0) and deleting the template's activation log (Galata asserts it verbatim). pytest 148, Jest 81, Galata 20/20, eslint 0 errors.

17. **Task - Recovered routes.py from an over-broad checkout** (v1.0.0): A `git checkout` used to undo a one-line mutation reverted the whole file to HEAD, taking every uncommitted change with it<br>
    **Result**: The mutation check on `_tree_assistant`'s shallowest-wins invariant worked - the test went red as intended - but `git checkout jupyterlab_ai_code_assistants_extension/core/routes.py` restored HEAD rather than the pre-mutation state, silently discarding this session's colour-route contract (`overrides` in the payload, `hand_set`, 400 `colour_invalid`, 500 `colour_store_unwritable`, `load_store`), `_effective_colour`'s override-for-every-provider rule and its preloaded map, both destructive routes moved off the IOLoop, the pty spawned at 1x1, and the native branch-inheritance expression. Caught by pytest dropping to 139 passed / 10 failed. All reconstructed and re-verified: 149 pytest including the 29 colour-store cases that pin the route contract, `routes.py` at +106/-37 against HEAD. Lesson recorded here rather than repeated: a mutation check restores from a scratch COPY of the file, never from git, whenever the tree carries uncommitted work.

18. **Task [Extended] - Round 2 of the whole-repo sweep** (v1.0.0): All four lenses returned DO-NOT-SHIP again; the fixes themselves were the bulk of the findings, and two were inert or inverted<br>
    **Result**: Architect and bug-hunter INDEPENDENTLY disproved the pty fix at runtime - `dimensions=(1, 1)` is accepted by `terminal_manager.create` and dropped before the pty, so `TIOCGWINSZ` still read 24x80 and the 5.15s stall persisted, while my test certified it green against a fake whose signature I had widened to take the kwarg the real library discards. Replaced with `get_terminal(name).ptyproc.setwinsize(1, 1)`, the fake now models the real manager, and the assertion is the winsize. Bug-hunter also caught that my Codex toast fix INVERTED the defect - the new copy names Codex's reason and is wrong for Claude in the identical scenario - so it now names neither. Ux and bug-hunter both found the glyph model too crude: it fired on Gemini's `plan`, which is read-only, under a label naming the control; `ILaunchMode` gained a provider-declared `unsafeValues`, the suffix names the VALUE, and duplicate suppression compares the resolved TOKEN so `approvalMode=yolo` no longer sits beside `yoloMode`. Slop-hunter found a CRITICAL in my own `DEFAULT_SIDEBAR` constant - substituted at a membership test and at the other branch of a ternary, where it means neither - and DISPROVED the `mkdirp` decline by running `npm install --package-lock-only --offline`: no network, no version bump, no Makefile. Also: the CI gate was red on prettier because `tmp/` and `graphify-out/` were in neither ignore file, drowning three real failures in 120; both ignore files now cover them. `yarn.lock` had been rewritten as a build side effect and was restored from git, after which `jlpm lint:check` passes 0 errors. pytest 149, Jest 82, Galata 20/20.

19. **Task [Extended] - Rounds 3-5, the fix wave, and the colour store's fifth rewrite** (v1.0.7): Three adversarial rounds and two workflows closed 44 defects; the colour store's two deferred entries finally became tractable once their prerequisite was identified<br>
    **Result**: `DEF-30`'s own deferral note named the blocker - a serialisation chain needs a bounded request first - and the round-5 wave confirmed it independently: `requestAPI` set no timeout and `ServerConnection.makeRequest` beneath it none either, which is also what made `DEF-55`'s cleanup modal a genuine trap. So `request.ts` gained `REQUEST_TIMEOUT_MS = 60_000` (a full CLI timeout above `codex.py`'s `_CLI_TIMEOUT_S = 30`, hand-rolled `AbortController` because `AbortSignal.timeout` is absent under this jest env), and only then did `ColourStore` get its `_run(op)` chain - five lines and one field that DELETED `_writeSeq`, `_writeStart`, `_readSeq`, `_readAbsorbed`, `_current` and `_absorb`, 22 references to 0. Every deleted interleaving test carried a proof its state can no longer be constructed. The new cross-runtime guard immediately earned itself: `DEF-52`, the two FNV-1a implementations disagreeing above the BMP because JS walks UTF-16 code units and Python code points - `"𝕏id"` tinted rose in the browser and peach on the server - now both hash UTF-8 bytes, bound by a 508-id differential corpus. Two more defects were the fixes' own residue: `DEF-73`, a deadlock invariant unenforceable at runtime and so enforced by a source-reading test, and `DEF-75`, a freshness guard whose false positive no rebuild clears. Six panel defects moved from "argued under jsdom" to measured in a browser via a new `ui-tests/tests/panel-regressions.spec.ts`; Galata 26/26. Version tracking repaired at Star Colonel's direction - 1.0.0 to 1.0.7, one bump per landed unit, and `package-lock.json` found drifted at 0.6.17.

20. **Task [Extended] - Round 6 stopped by ruling, not by a clean round** (v1.0.8): Star Colonel diagnosed the runaway loop as "unnecessary edge fixes and vanity"; the ledger was measured against that claim and it held<br>
    **Result**: Round 6 returned 1 SHIP and 3 DO-NOT-SHIP with 18 findings surviving two skeptics each. Rather than a sixth fix wave, scanned all 75 entries for one naming a prior fix as its cause: 11, and six of the nine causing fixes were MINORs - `DEF-26`, "popup rows are semantically anonymous", spawned a CRITICAL and two MAJORs on its own. Fix-induced rate 15% campaign-wide, 50% in the last wave. Three Explore agents then scoped every MAJOR against a rebuilt graph (1,861 nodes, 3,102 edges) and downgraded or refuted all of them, the reviewer wrong about the mechanism each time: the N-scaling DELETE ceiling needs 86 conversations in one project to breach and costs a toast rather than data; the tint-teardown reviewer blamed an unwired `clearColours` that is wired, the real gate being a 404 from the just-disabled provider. So the wave is two lines - `panel.ts:995` and `:1044` to `_showActionError`, byte-identical to the working claude sibling at `:968`, closing the half of `DEF-53` its note was silent about - plus three comments that had asserted the opposite of `DEF-72` since it landed, and seven log-only entries `DEF-76`..`DEF-82`. `DEF-79` was fixed at the CAUSE rather than the detector: three plan files instructed `npx tsc --noEmit`, which refreshes the buildinfo without emitting and so disarms the lib-freshness guard, and they now say `jlpm build:lib`. Measured live an hour later - `lib/` three hours stale, guard green. No round 7. pytest 163, Jest 119, Galata not re-run, eslint 0 errors.

21. **Task [Extended] - Round 7 on the committed tree, both lenses SHIP** (v1.0.9): Architect and bug-hunter only, at Star Colonel's direction, over the committed `212aeb8` - the first round to examine surfaces the six prior rounds never reached<br>
    **Result**: Both lenses returned SHIP, the campaign's first, yet the confirmed MAJORs were real precisely because they sat outside every earlier round's scope: packaging, the release lane, the wire error contract and IOLoop discipline. Bug-hunter's best finding: every refusal this server authors rendered as the literal `[object Object]`, because `requestAPI` fell through `data.message || data` while all 14 refusal sites in `routes.py` write `{"error": "<code>"}` and none writes `message` - six rounds argued error COPY while the strings never displayed at all; fixed with `data?.message ?? data?.error ?? data`. Second, the launch pre-flight ran `project_session_ids` inline on the IOLoop (191ms whole-server freeze measured per row click) while the call beside it already used the executor - hoisted into the same executor. Third, the colour poll enqueued at one per 30s against a `_run` chain draining one per 60s under a hung server, an unbounded backlog a fork then awaits in full - now an in-flight guard on the poll only, Refresh untouched. Fourth, cleanup of an already-clean project answered "Cleanup failed" - early return saying "Nothing left to clean up." instead. Architect's packaging MAJOR: the shipped sdists carried `.claude/`, `logs/` and `docs/defects.md` (32 files in 1.0.0); `pyproject.toml` exclude extended, a scratch sdist build proving 0 leaked files. Descriptor parity gained the `label` binding, mutation-checked - kimi's Python label mutated reddens exactly the parity test. Deferred with ids rather than fixed: DEF-97 (two of three metadata caches uncapped), DEF-98. Ledger 98 entries, 79 closed / 19 open, contiguous. pytest 163, Jest 119, tsc 0, eslint 0 errors, Galata 26/26 at v1.0.8.

22. **Task [Short] - Close-19 campaign launched** (v1.0.10): Dynamic workflow `wf_7abc4538-7da` closing all 19 open ledger defects<br>
    **Result**: Plan at `.claude/plans/breezy-wandering-bubble.md`, script at `.claude/workflows/close-19-v1.0.10.js` - 14 fixes in five file-partitioned Opus lanes, 4 rulings, 1 environment. Resume: `Workflow({scriptPath: ".claude/workflows/close-19-v1.0.10.js", resumeFromRunId: "wf_7abc4538-7da"})`; lanes idempotent against on-disk state.

23. **Task [Extended] - Close-19 executed, campaign closed on a clean pair at round 20** (v1.0.16): Five Opus lanes landed the 14 fixes and 5 closures, then nine adversarial rounds burned the remedy chain down to zero findings<br>
    **Result**: Workflow `wf_7abc4538-7da` closed all 19 open entries; rounds 8-11 then caught what the wave itself had seeded - `DEF-99` (the `DEF-82` executor conversion de-serialised the loop-owned state writers, lost update 200/200, fixed by reverting four wraps), `DEF-100` (a cross-lane mutation-check restore silently clobbered `DEF-76`'s landed hunks), `DEF-101` (claude's store-internal executor pin, fixed by deletion) and `DEF-102` (every switch in a pinned project answered the OLD pin and the panel toasted a spurious failure - fixed once at the route with a post-pin `resolve_current`). Round 12 demanded the dead store-side `current` go too: workflow `wf_238ff696-3ba` slimmed the switch contract to `{"requested"}` across all four providers (`DEF-103`), mutation-checked, Galata 27/27 at v1.0.15. Rounds 13-19 were a prose-truth burn-down the ledger records candidly as its own spiral (`DEF-109`): each round's MAJOR was a falsity in wording the adjudicator authored the round before - the false touch rationale (`DEF-105`), a false contract universal (`DEF-106`), the "only" clause, then ledger-note corrections that ended as a per-provider probe matrix plus a sentence deletion. Two reviewer claims were themselves retracted after adjudicator re-probes (round-13's "RED (3)" mutation row; round-15's "panel accepts silently"). Round 20: SHIP + SHIP, zero findings, both lenses probing rather than trusting. Ledger 110 entries, 0 open. `DEF-104`/`DEF-107`/`DEF-108`/`DEF-110` ruled logged-not-fixed with named settling instruments. pytest 170 green throughout, prettier clean, graph refreshed (2,099 nodes), one Makefile bump per landed unit v1.0.11 through v1.0.16.

24. **Task [Short] - Release of the campaign tree** (v1.0.17): Shipped the close-19 campaign work to npm and PyPI via the release lane<br>
    **Result**: First release since v1.0.10 - carries the 19 ledger closures, the switch-path contract truth work (`DEF-99`..`DEF-110`) and the round-20 clean adversarial pair. Makefile refreshed 1.36 → 1.37 (nodeenv-pinned global installs) from the canonical copy in the same pass.

25. **Task - Shield glyph on unsafe launch variants** (v1.0.18): User-directed swap of the caution triangle for the reference implementation's shield on `Resume (Skip Permissions)` and the other danger-marked menu entries<br>
    **Result**: User overruled the port-time rationale ("a shield reads as protected") - the shield is the established marker for an action touching the permission system (UAC pattern), and the reference `jupyterlab_claude_code_extension` used it. `warningIcon` renamed `shieldIcon` in `src/core/icons.ts` with the reference's shield path; DEF-40's conspicuousness fix deliberately kept (16x16, `jp-icon-warn0` orange), so the reference's 13x13 grey was not copied verbatim. All seven `panel.ts` call sites follow, including the DEF-45 destructive-pair marker on `Clean Up Parallel Sessions` - one danger glyph for the menu, not two. Jest 121 green, Galata 27/27 at v1.0.18 (includes DEF-40's computed-fill probe), shield rendered in the context menu on the 8931 test server and screenshotted. Ledger note appended under DEF-40.

26. **Task [Short] - Release of the shield glyph** (v1.0.19): Shipped entry 25's glyph swap to npm and PyPI via the release lane<br>
    **Result**: Single-change release on top of v1.0.17 - the unsafe-launch marker is now the shield, DEF-40's sizing and colour kept. Makefile already current at 1.37.

27. **Task - Neutral shield and sweep-trash cleanup glyph** (v1.0.20): Two user rulings on v1.0.19's icons - the shield goes standard colour, and `Clean Up Parallel Sessions` gets its own trash-variant glyph<br>
    **Result**: The shield now paints in the theme's neutral `jp-icon3` grey like every sibling - the user ruled the shape alone marks a permission-touching action, reversing DEF-40's orange half while keeping its 16x16 size half. `Clean Up Parallel Sessions` no longer shares the unsafe-launch marker: new `cleanupIcon` in `src/core/icons.ts` (Material `delete_sweep` - trash with sweep strokes, "with something extra" per the user), same family as `Remove from ...`'s plain trash but visibly the bulk delete. Unit colour test flipped to assert `jp-icon3` parity; the Galata probe now finds the glyph by its path data since colour no longer distinguishes it, fill-differs assertion deleted. Jest 121, Galata 27/27 at v1.0.20, rendered proof screenshotted on the 8931 server. Ledger notes under DEF-40 and DEF-45.

28. **Task [Short] - Release of the neutral shield and cleanup glyph** (v1.0.21): Shipped entry 27's two icon rulings to npm and PyPI via the release lane<br>
    **Result**: Single-scope release on top of v1.0.19 - the shield in standard `jp-icon3` grey on the permission-skipping entries, the `delete_sweep` trash on `Clean Up Parallel Sessions`. Makefile current at 1.37.

29. **Task - DEF-111 and DEF-112: one approval switch, neutral header button** (v1.0.22): Two user-reported defects fixed together - Gemini's double approval setting collapsed to `yoloMode` only, and the header new-session button wears + in every mode state<br>
    **Result**: DEF-111 deleted Gemini's `approvalMode` enum (`src/providers/gemini.ts`, `providers/gemini.py` argv branch); with no enum provider left, the enum machinery died with it per house precedent - `ILaunchMode` lost `kind`/`values`/`unsafeValues`, the resolver collapsed to force-wins-else-first-true-boolean, the schema generator and descriptor-parity flattening followed. `auto_edit`/`plan` no longer settable, accepted by the user; stale keys and tokens are ignored on both runtimes (pinned in `test_provider_stores.py`). DEF-112 made the header button paint `addIcon` unconditionally, deleting the `_repaintNewIcon` hook - the shield keeps its menu-entry home and the armed mode is still named in the button's title (DEF-36). Galata shield proof moved to `Resume (Skip Permissions)`, identified by path data. Caught mid-gate: `registry.spec.ts` silently dead in a "green" run (104 vs 121 exposed it) - fixed and re-verified. Jest 118, pytest 170, Galata 28/28, rendered proof screenshotted on 8931. Ledger DEF-111/DEF-112, eight acc-crit criteria closed.

30. **Task [Short] - Release of DEF-111 and DEF-112** (v1.0.23): Shipped the single-switch Gemini and the always-+ header button to npm and PyPI via the release lane<br>
    **Result**: Two-defect release on top of v1.0.21 - one approval control per provider, shield confined to menu entries. Makefile current at 1.37.

31. **Task - Header title drops the provider name** (v1.0.24): DEF-113 - the + tooltip reads "New session in <folder>", the redundant provider word gone<br>
    **Result**: Title template in `src/core/panel.ts` slimmed per user direction; claude-regression Galata selected the button by the old exact title and followed. Same pass, the user's acc-crit wording for the armed state ("no menu, just + starts new session") entered the Panel criteria - behaviour already held and unit-pinned, text now carries it. Jest 118, Galata 28/28.

32. **Task [Short] - Release of DEF-113** (v1.0.25): Shipped the "New session" header title and the acc-crit wording to npm and PyPI via the release lane<br>
    **Result**: Single-change release on top of v1.0.23. Makefile current at 1.37.

33. **Task [Short] - Vendor-less menu labels and static tooltip** (v1.0.26): DEF-114 - the + menu reads "New session" / "New session (Skip Permissions)", the tooltip no longer interpolates the folder path<br>
    **Result**: DEF-113's scope corrected by the user: menu entries kept the vendor name and the tooltip named a live folder that deep trees make unreadable. Both fixed; the armed-mode tooltip suffix kept per the user's explicit answer. Jest 118, Galata 28/28, menu screenshot-verified.

34. **Task [Short] - Release of DEF-114** (v1.0.27): Shipped the vendor-less menu labels and static tooltip to npm and PyPI via the release lane<br>
    **Result**: Single-change release on top of v1.0.25. Makefile current at 1.37.

35. **Task - DEF-115 armed + menu, not reproduced** (v1.0.27): User reported the header + still dropping its two-entry menu with `dangerouslySkipPermissions` on; traced the path, found the code correct and the reporting machine's switch off<br>
    **Result**: `_visibleVariantCount()` filters every variant whose forced launch equals the plain one, so an armed switch leaves zero and the click handler calls `_newSession()` directly (`src/core/panel.ts`); the live path `settings.changed` -> `reconcile` -> `applySharedSettings` -> `setModes` reaches it. Evidence for the non-reproduction: `~/.jupyter/lab/user-settings/.../plugin.jupyterlab-settings` holds `false`, mtime 2026-08-09, sole settings source (no `overrides.json`, no second copy, standalone claude extension not installed). Real gap was proof - the acc-crit criterion was ticked on unit evidence alone and no Galata test had ever clicked the armed +. Added one that clicks unarmed first (so the assertion can fail), arms via the settings registry, waits for the title, then asserts no menu in the DOM and a terminal. Galata 29/29.

36. **Task [Short] - DEF-115 confirmed by the user** (v1.0.27): Star Colonel enabled `dangerouslySkipPermissions` and the header + launched with no menu<br>
    **Result**: Diagnosis holds - the setting was off, not the behaviour. DEF-115 closed with the confirmation logged.

37. **Task [Short] - Release of the DEF-115 proof** (v1.0.28): Shipped the armed-+ Galata coverage to npm and PyPI via the release lane<br>
    **Result**: Test-and-docs release - no runtime change since v1.0.27. Makefile current at 1.37.

38. **Task - DEF-116 404s and the dead file-browser action** (v1.0.28): Two user-reported symptoms traced to causes outside the extension - a proxy blip and a file browser already at the target folder<br>
    **Result**: The 404s never reached the Jupyter server: `/var/log/jupyterlab.log` holds zero 404s for either failing extension all day, the failing requests' query stamps appear nowhere in it, and the 21:42:30-21:42:50 window has no log lines at all, while the server had run unrestarted since 08:08 and answered the same endpoints in 1-8 ms either side. Two unrelated extensions failed in the same instant, the browser's own notification poll reconnected in that window, and the 404 body was plain-text Go-style rather than jupyter_server's HTML - so the reverse proxy in front of `workbench.lab.stellars-tech.eu` owns it. `Show in File Browser` proved working live via Playwright against the running lab (hub token as auth header, separate workspace): the breadcrumb moved to `private/ai-assistants/groundrails`, screenshotted. It looked dead because the file browser had polled that exact folder every 10 s since the 08:08 restore. No-feedback-on-no-op logged as MINOR, not fixed.

39. **Task [Short] - Release of the DEF-116 record** (v1.0.29): Shipped the ledger and journal record of the not-reproduced 404 and file-browser reports<br>
    **Result**: Docs-only release - no runtime change since v1.0.28. Makefile current at 1.37.

40. **Task - C7 status probe, wake re-probe** (v1.0.29): Console triage of the live lab assigned one of nine error classes to this extension; fixed it and the one console-noise line that was also ours<br>
    **Result**: C7 - the probe's failure warning claimed `no assistant panels will be docked` while the 60 s `STATUS_INTERVAL_MS` timer re-probes and docks them, and nothing re-probed on wake, so a slept laptop faced an empty sidebar for up to a minute. Graphify first: `probeStatus` is a closure with two call sites, both inside `activate`, nothing imports `src/index.ts` - budget was that file. Warning now names the retry cadence from the constant; `online` and `visibilitychange`-to-`visible` listeners re-probe at once; `probeStatus` became a coalescing wrapper over the original body so the pair of wake events costs one request. Galata `DEF-117` dispatches both in one synchronous task, asserts a probe inside 5 s and exactly one request, and was falsified against the pre-fix wheel. Also C8: `request.ts` logged a non-JSON body at `log` on every request - demoted to `debug`. Jest 118, pytest 170, Galata 30/30. Ran as workflow `wf_289a404e-784`.

41. **Task - DEF-117 survives adversarial review by subtraction** (v1.0.29): Architect and bug-hunter both returned DO-NOT-SHIP on entry 40's fix; the adjudicated remedy deleted most of it<br>
    **Result**: 14 findings, all confirmed, 10 traceable to two additions the C7 brief never asked for. The coalescing wrapper was the worst: measured live, a probe wedged mid-cycle made the 60 s interval tick issue no request at all, and a wake arriving during a wedged probe joined the doomed request rather than issuing a live one - doubling the dark window to 120 s, the very defect being closed. Deleted; a wake now costs two idempotent GETs. The `request.ts` log demotion was reverted whole - unrequested, and it hid the only signal on a path where a 200 with a non-JSON body silently undocks every panel. Entry 40's Galata test was disproved (either event alone satisfied both assertions); rewritten to measure each dispatch against the count before it, and falsified by deleting each listener in turn and rebuilding. Deferred as logged-not-fixed: DEF-118, DEF-119, DEF-120. Jest 118, pytest 170, Galata 30/30.

42. **Task - DEF-121 guard, then half of it deleted** (v1.0.29): Rounds 3 and 4 of the DEF-117 review found two more real defects in the probe, and the shipping fix is smaller than the one entering round 3<br>
    **Result**: Round 2 found `probeStatus` writing `status` on settle order with two probes concurrent - a 45 s-old timeout discarding a just-confirmed roster and undocking every panel while the server was healthy. Its generation stamp guarded both writes - half a fix too large. Round 3's lenses converged from opposite ends: guarding the SUCCESS write on "no newer probe issued" discards a good roster whenever a newer one merely exists, and since failures reject in milliseconds while successes cost a round trip, that newer probe is systematically a failure - reopening DEF-117's dark window. Two lines deleted: a roster always wins, a failure only while unsuperseded. A second Galata test pins the restored ordering, each falsified by its own mutation. Round 4: bug-hunter SHIP, architect no code change. Logged not fixed: DEF-123 (deadline equals interval, so a timeout never clears the roster), DEF-124 and DEF-125. Jest 118, pytest 170, Galata 32/32.

43. **Task [Short] - Release of the probe ordering fix** (v1.0.30): Shipped entry 42's wake re-probe and probe-ordering guard to npm and PyPI<br>
    **Result**: First runtime change since v1.0.28. Makefile current at 1.37.

44. **Task - bg-record sweep refuted and reverted** (v1.0.30): The auto-sweep of stale `kind: "bg"` Claude session records was dropped after adversarial review disproved the reason it existed<br>
    **Result**: Architect and bug-hunter both returned DO-NOT-SHIP and both doubted the causal premise, so it was tested rather than argued. Two arms against the real config dir - a scratch project holding one transcript, differing only by a dead `kind: "bg"` record naming it (synthetic, then a real 15-field record copied from the live store) - and `claude -c` continued the conversation every time. A dead bg record does not gate `-c`, so the sweep deleted another program's files for a fault it does not fix; reverted whole, patch kept in scratch. This also voids the earlier diagnosis of the `-c` failure in `jupyterlab_export_markdown_extension`, which is unexplained again. The review's one surviving find predates the change: `DEF-126` - `os.kill` answers an out-of-range int with `OverflowError`, not an `OSError`, so it escaped `pid_alive`'s own handler and 500'd the sessions poll for every project. Fixed at the origin in `core/store.py`, mutation-verified, pytest 171.

45. **Task - `claude -c` follows the panel's primary** (v1.0.30): Diagnosed the "No conversation found to continue" report and made the panel decide which conversation the terminal resumes<br>
    **Result**: Not stale records and not a holding process: `-c` refuses a chain rooted at a `system`/`compact_boundary` - the shape every conversation takes after compaction - then falls back to the next transcript, so a project whose only conversation was compacted refuses while the panel resumes it by id. Measured in both directions on isolated clones, with a known-good transcript as the control that rules out the method. `-c` orders by mtime and does fall back; Claude's own fork inherits the boundary, so rewriting is the only lever. `ensure_continuable` adds a synthetic `isMeta` root and re-points the boundary, removing nothing; `make_continuable` gates on liveness plus an mtime and size re-check, transcripts being append-only. Wired into `switch` before its mtime touch, and into `launch_argv`. Seven tests, seven mutants each reddening exactly one. pytest 178, DEF-127.
