VERDICT: DO-NOT-SHIP (8 findings, 2 MAJOR / 6 MINOR)

Honest headline first: I found NO dead code, NO duplicated block, NO unreachable
branch and NO stale citation of any consequence in the source tree. Every one of
the ten closures listed in my brief reproduces in the live tree, and two of them
kill their mutants under measurement. Structurally this is the leanest the tree
has been across five rounds - the whole-repo scans that produced findings in
rounds 1-4 (unused exports, dead CSS, unused Python imports, duplicated 8-line
blocks, over-long lines, comment identifiers with no referent) come back empty
or near-empty now.

What is left is a different species: the tree has no slop, it has no NET. Six
defects were closed this session in `panel.ts` and `icons.ts`, and not one of
them can be reddened by any suite I am permitted to run. Two of those six
(DEF-39, DEF-46) were themselves regressions introduced by the fix for DEF-35 /
DEF-37 earlier in the same session, which is the class recurring under
observation. That is the case for DO-NOT-SHIP, and it is the only serious one I
have. The remaining six findings are lint-config and documentation hygiene.

GATE - three uncached legs, run separately, never the cached script:

  python -m pytest -q                       150 passed          (baseline: 150) OK
  npx jest                                   82 passed          (baseline:  82) OK
  npx tsc --noEmit                           exit 0             (baseline:   0) OK
  npx eslint . --no-cache                    1 ERROR, 54 warn   (baseline: 0/54) FAIL
  npx prettier "**/*{.ts,.tsx,.js,.jsx,.css,.json,.md}" --check
                                             1 file unformatted (baseline: clean) FAIL
  npx stylelint --no-cache "style/**/*.css"  exit 0             (baseline:   0) OK
  Galata                                     NOT RUN - out of scope for this agent

  Both lint failures are new since the baseline was taken and both are
  self-inflicted by this review campaign, not by the fix batch. Details in
  findings 3 and 5. `npx eslint . --no-cache --ignore-pattern 'tmp/**'` returns
  the baseline 0 errors / 54 warnings exactly, which isolates the cause.

CLOSURES - all ten hold. How each was checked:

  DEF-32  `routes.py:822` reads `terminal_manager.terminals.get(terminal_name)`;
          `get_terminal` appears only in the 16-line comment above it explaining
          why it is not used. MUTATION-CHECKED in a scratch copy of the package:
          restoring `terminal_manager.get_terminal(terminal_name)` reddens
          `test_probing_an_unknown_terminal_creates_nothing` with
          "DID NOT RAISE HTTPClientError" (1 failed, 30 passed). The test kills
          its mutant.

  DEF-33  `_INIT_WAITER` at `routes.py:53-62` opens with
          `stty rows 1 cols 1 2>/dev/null;` and polls `stty size` until it is no
          longer 1x1. `grep -n "SIGWINCH\|setwinsize" routes.py` returns one hit,
          in the header comment, describing the deleted design. No `trap`, no
          baseline read, no server-side resize. The launch site at `:746-750`
          now explains the absence by the LIVE mechanism (a resize here is
          indistinguishable from the client attach), which is what round 4 asked
          for. `test_a_launch_sizes_the_pty_from_inside_the_child` asserts both
          halves - the mark is present and "WINCH" is absent.

  DEF-34  Closed with DEF-33's deletion, as claimed. The `model.get("name") if
          isinstance(model, dict) else getattr(model, "name", None)` resolution
          at `:753-757` is now the only reader of the model, and nothing
          subscripts it above.

  DEF-35  `panel.ts:2005-2008`: label is
          `_visibleVariantCount() === 0 ? 'Branch Session' + _variantSuffix()
          : _variantSuffix() ? 'Default' + suffix : 'Normal'`. All three arms
  DEF-39  are reachable - the `Default (...)` arm needs an enum resolving while a
          boolean variant stays visible, which is exactly Gemini with
          `approvalMode: yolo` and `yoloMode: false`. Icon at `:2016-2017` is
          `_variantIcon() ?? (_visibleVariantCount() === 0 ? branchIcon
          : undefined)` - the mode's glyph wins, `branchIcon` is the fallback.

  DEF-36  `panel.ts:318` appends `${this._variantSuffix()}` to the `+` title;
  DEF-38  `nameNewButton` is stored on `this._renameNewButton` at `:328` and
          re-run from `setModes` at `:266`, with the reason stated at `:264-265`.

  DEF-37  `_resolvedVariant()` at `:1744-1750` consults settings only. The
          background-agent test moved to `_resumeVariant()` at `:1764-1768`
          (`hasBgAgents && _activeSession?.bg_id ? null : _resolvedVariant()`),
          and `grep` confirms `_resumeVariant` has exactly two readers, both on
          the `resume` command (`:1816` label, `:1820` icon). The four
          non-attaching surfaces read `_resolvedVariant` directly.

  DEF-40  `icons.ts:60-62`: `width="16" height="16"` and
          `class="jp-icon-warn0"` on the triangle path.

  yarn.lock  `git diff yarn.lock` is 1 insertion / 2 deletions: `mkdirp: ^1.0.3`
          gone from the workspace block at `:7623`, and the descriptor key
          narrowed from `"mkdirp@npm:^1.0.3, mkdirp@npm:^1.0.4"` to
          `"mkdirp@npm:^1.0.4"`. Surgical and correct. `package-lock.json`
          agrees: root `devDependencies` no longer lists it, transitive
          `node_modules/mkdirp` retained. `package.json` and `package-lock.json`
          root `dependencies` (14) and `devDependencies` (21) match exactly.

  'Empty.' / 'No favorites yet.'  Both gone. `panel.ts:1411` has only
          `empty.textContent = 'No matches.';` left.

  acc-crit  117 log lines shortened to the 47-character form as claimed. I am
          NOT re-raising the residual duplication - round 4 proved the lines are
          load-bearing for `acc-crit.py check --strict`, and pressing a shortened
          fix in the next round is exactly the loop antipattern.

  ISession.colour  `types.ts:129-134` now says "The override half is resolved
          for every provider, so this is not a native-only field." Corrected.

NOTHING THE CLOSURES BROKE. I looked specifically for orphans left by the three
deletions:
  - Unused Python imports after the setwinsize block went: AST scan over the
    whole package returns zero.
  - Unused TS exports after the panel rework: `assistantsIcon` looks dead by
    grep (zero importers) but is NOT - it self-registers with the LabIcon
    registry under `jupyterlab_ai_code_assistants_extension:assistants`, which
    `schema/plugin.json:5` resolves by name for the settings entry, and
    `icons.ts` is loaded because `panel.ts:27` imports `branchIcon` from it.
    Refuted, not reported.
  - Dead CSS after the icon change: all 62 `jp-*` selectors in `style/base.css`
    have a referent in `src/` or `ui-tests/tests/`.
  - Duplicated blocks: no 8-line non-comment sequence repeats anywhere in `src/`.
  - Comment identifiers with no referent: one hit tree-wide (finding 8).
  - Uncalled private members in `panel.ts` / `terminals.ts` / `popup.ts` /
    `index.ts` / `registry.ts`: none.
  - Settings keys nothing reads: none (the seven that fail a naive grep are read
    generically as `providers.${id}.${modeId}`).

## Findings

- **[MAJOR] Six defects were closed in `panel.ts` / `icons.ts` this session and
  no suite can redden any of them.** Axis: tests that kill no mutant. `npx jest
  --coverage` over the 82-test suite reports `panel.ts` at **0% statements, 0%
  branches, 0% functions, lines 2-2396 uncovered**, alongside `terminals.ts` 0%,
  `popup.ts` 0%, `index.ts` 0%, `request.ts` 0%, `icons.ts` 0% - 11.2% overall
  for the frontend. Every surface DEF-35 through DEF-40 touched lives in those
  files: `_visibleVariantCount`, `_variantSuffix`, `_variantIcon`,
  `_resumeVariant`, `_buildsSameLaunch`, `nameNewButton`, and the `warningIcon`
  SVG. `tsc --noEmit` cannot see a label string or an icon choice. That leaves
  Galata, and `grep -n "Branch Session\|Skip Permissions\|Bypass Approvals\|warn"
  ui-tests/tests/*.spec.ts` returns exactly ONE assertion across all 20 tests -
  `claude-regression.spec.ts:73`, `hasText: 'New Claude Code Session (Skip
  Permissions)'`, which is the `+` DROPDOWN item, not the `+` button title
  (DEF-36/38), not the branch item's label or glyph (DEF-35/39), and not the
  resume label on a background-agent row (DEF-37). Why it matters rather than
  being a generic coverage complaint: this exact class has already recurred
  twice inside this session under adversarial observation - `docs/defects.md:133`
  records DEF-39 as "DEF-35's fix put `branchIcon` on the flattened item
  unconditionally", and `:151` records DEF-46 as "a fix-introduced regression
  from DEF-37, landed mid-review". Both were caught by a human reading the diff,
  which is not a control. `modes.ts` at 95% is the proof the extraction works -
  `resolvedLaunchModeEntry` was pulled out of `panel.ts` precisely so the warning
  rule could be asserted, and its five cases are the only DEF-37-adjacent logic
  with a net. The same treatment applied to `_visibleVariantCount` /
  `_variantSuffix` / `_buildsSameLaunch` - three pure functions of
  `(launchModes, modeValues)` - would put the branch-item label and the `+` title
  under Jest. Load-bearing check: I did not attempt to import `panel.ts` under
  Jest (it pulls the JupyterLab widget surface, and creating a probe spec would
  mean editing the tree, which I am barred from); the 0% figure is measured, not
  inferred, and stands on its own. FACT.

- **[MAJOR] The terminal probe route's success path has no test at all, and the
  fake built for it is provably dead.** Axis: tests that kill no mutant / dead
  weight. `grep -n '"terminal"' jupyterlab_ai_code_assistants_extension/tests/*.py`
  returns exactly one hit - `test_routes.py:414`, `jp_fetch(URL, "providers",
  "claude", "terminal", "never-seen")` - which is the 404 path. Nothing in 150
  tests ever probes a terminal the manager HOLDS, so the entire body of
  `TerminalHandler.get` below `routes.py:826` is unexecuted: the `ptyproc`
  extraction, `_tree_assistant(ptyproc.pid)`, the provider-scoping rule
  `running = found is not None and found.id == provider.id`,
  `_terminal_cwds(ptyproc.pid)` (which has no test of its own anywhere -
  `_tree_assistant` does, at `test_routes.py:569`), and the `_effective_colour`
  composition at `:844-850` that resolves this conversation's own default
  against the stored override. DELETE-TEST, run in a scratch copy of the package
  at `.../scratchpad/mut1` (baseline there: 148 passed, 2 skipped -
  `test_descriptor_parity.py` skips twice without `lib/providers`): stripping the
  body of `_FakePty` at `test_routes.py:365-368` (`self.ptyproc = self`,
  `self.pid = 1`) leaves `test_routes.py` at **31 passed, unchanged** - the class
  is a corpse. Round 4 reported the same corpse as a CRITICAL and it was not
  taken; I am re-raising it because round 5 changed what it means. The fake was
  defensible while it was merely unused scaffolding; after DEF-32 rewrote this
  exact handler it is the visible symptom of a rewritten CRITICAL route whose
  only test asserts what it does NOT do. Concretely: one test that registers a
  `_FakePty` under a name, monkeypatches `_tree_assistant` to answer a provider
  and a session id, and asserts the four response fields would give `_FakePty` a
  job and put the colour composition - the module header of `colour.ts` calls it
  "the part of this panel that regressed twice behind a green suite that never
  ran the logic" - under pytest. Load-bearing check: cleared -
  `grep -rn "_FakePty\|_FakeTerminalManager"` returns hits in `test_routes.py`
  only; no conftest export, no fixture. FACT.

- **[MINOR] `eslint.config.mjs` never learned the ignore that `.prettierignore`
  and `.gitignore` both gained this session, and the lint gate is red right
  now.** Axis: config that does nothing. This session's diff added `tmp` and
  `graphify-out` to `.prettierignore:9-10` and `tmp/` + `graphify-out/` to
  `.gitignore:141-142`, with a comment explaining that both patterns are
  unanchored on purpose. `eslint.config.mjs:10-19` was not touched and its
  `ignores` array lists `node_modules, dist, coverage, **/*.js, **/*.d.ts,
  .venv, tests, **/__tests__, ui-tests` - no `tmp`, no `graphify-out`. Because
  the flat config sets `parserOptions.project: 'tsconfig.json'` and `tsconfig`
  includes `src/**/*` only, ANY `.ts` file under the mandated scratch directory
  is linted, fails to resolve to a project, and raises a severity-2 parsing
  error. Measured: `npx eslint . --no-cache` returns **1 error, 54 warnings**;
  `npx eslint . --no-cache --ignore-pattern 'tmp/**'` returns **0 errors, 54
  warnings**, the exact baseline. Honest caveat, stated plainly: the file
  currently triggering it is `tmp/adv/panel-probe.spec.ts`, timestamped 09:32
  today, which a concurrent agent in THIS review round dropped there - it is not
  a repo artefact and it will vanish. The config gap is the repo's, is permanent,
  and turns the project's own sanctioned scratch location into a lint-gate
  landmine; `.prettierignore` was updated for exactly this reason and eslint was
  left behind. Fix is two strings in one array. FACT.

- **[MINOR] `@typescript-eslint/no-unused-vars` is configured against the
  codebase's own `_`-prefix convention, and 9 of the 54 standing warnings are the
  result.** Axis: config that does nothing. `eslint.config.mjs:47` reads
  `'@typescript-eslint/no-unused-vars': ['warn', { args: 'none' }]`. `args:
  'none'` exempts function parameters and says nothing about catch bindings,
  whose default is `caughtErrors: 'all'` in typescript-eslint v8 - so every
  deliberate `} catch (_err) {` is flagged. Breakdown of the 54 by rule, from
  `eslint -f json`: `jupyter/no-untranslated-string` 26,
  `jupyter/command-described-by` 18, `@typescript-eslint/no-unused-vars` 10, of
  which **9 are the identical message `'_err' is defined but never used`**
  (`colour.ts:69`, `:169`, `:307`, `:337`, and five more). The underscore is
  the project's stated convention and `tsconfig.json` honours it - the
  `noUnusedParameters: true` added this session passes because TypeScript exempts
  `_`-prefixed names, so the two checkers disagree about the same convention in
  the same tree. Delete-test, run without touching a file: `npx eslint .
  --no-cache --rule '{"@typescript-eslint/no-unused-vars":["warn",{"args":"none",
  "caughtErrorsIgnorePattern":"^_"}]}'` returns **45 warnings** against 54 - nine
  gone, nothing else moved. Nine permanent warnings is nine places a real one can
  hide. FACT.

- **[MINOR] The prettier leg fails on `.claude/workflows/confirming-round-v1.0.0.js`.**
  Axis: gate integrity. `npx prettier "**/*{.ts,.tsx,.js,.jsx,.css,.json,.md}"
  --check` reports `[warn] .claude/workflows/confirming-round-v1.0.0.js` and
  "Code style issues found". The file is untracked today, but its sibling
  `.claude/workflows/implement-ai-code-assistants.js` IS tracked and IS prettier-
  clean, so the convention in this repo is that workflow scripts get committed
  and get formatted; committing this one as-is turns `jlpm lint:check` red for
  everyone. Caveat, stated: this is the harness driving the round I am part of,
  not the fix batch. Either format it before it is committed, or add
  `.claude/workflows` to `.prettierignore` and make the exemption deliberate.
  FACT.

- **[MINOR] `package-lock.json` is a 317 KB tracked lockfile that no build path
  reads, `make publish` commits it on every release, and its root record has lost
  the pins the README calls load-bearing.** Axis: config that does nothing.
  Nothing installs from it: `Makefile:77` and `:145` run `jlpm install`,
  `.github/workflows/build.yml:30` and `:131` run `jlpm`, and `jlpm` here is yarn
  berry (`yarn.lock` `__metadata: version: 6`). `npm` appears in the Makefile
  twice - `npm publish` (reads `package.json`, not the lock) and
  `npm install -g yarn rimraf`. Yet `Makefile:125` stages exactly
  `git add package.json package-lock.json` - the dead file - and never
  `yarn.lock`, the live one. Worse, the two have drifted in a way that matters:
  `package.json` carries `resolutions: {lib0, webpack: 5.106.0, chalk: 4.1.2}`
  and `overrides: {webpack: 5.106.0, chalk: 4.1.2}`, and
  `package-lock.json`'s `packages[""]` record carries **neither key** - it has no
  `overrides` at all, which means it was last regenerated before the pins were
  added. README's `[!WARNING]` block states that without those pins "`make
  publish` and CI fail on `python -m build`". So the one lockfile the release
  target commits is the one that would reproduce the documented failure if anyone
  ever ran `npm ci` from it. Load-bearing check: NOT a delete demand - I did not
  run `jlpm install` or `npm install` (Makefile-only rule, and either would
  rewrite a lockfile). Either regenerate it as part of the build, or drop it and
  fix `Makefile:125` to stage `yarn.lock`. FACT.

- **[MINOR] `logs/README.md` documents two log families out of ten, and one of
  the two cannot exist.** Axis: stale prose. The file is 4 lines and names
  `make-*.log` and `adversarial/`. `logs/` actually holds `batch2-*.log` (6),
  `batch3-*.log` (3), `batch4-*.log` (3), `galata-round{3,4}.log`,
  `publish-0.6.{15,17}.log`, `ui-tests-*.log` (5), `waiter-race-sweep.log`,
  `icon-verify-jlab.log`, `readme-shot-jlab.log`, `make-install-*.log` (6) and
  `make-test-*.log` (4) - 34 files in ten families. The user-level rule is
  explicit: "The `logs/` directory MUST always contain a `README.md`" that
  "should briefly explain what each log file tracks". Round 4 raised the second
  half of this - `.gitignore:4` is `*.log`, so `git ls-files logs/` returns
  `README.md` and the `adversarial/*.txt` set and nothing else, meaning
  `make-*.log` names a class the repository has never contained and cannot. The
  README was edited this session and still names it. FACT.

- **[MINOR] `codex.py:80` documents `_THREAD_COLUMNS` by naming a function that
  does not exist.** Axis: stale prose. The comment reads "Columns
  ``_list_threads`` needs; anything missing in an older/newer schema is selected
  as NULL". There is no `_list_threads` anywhere in the tree - the reader is
  `_threads_from_db` at `:183`, and the public entry point is `list_threads` at
  `:315`. Found by scanning every backticked identifier in every comment and
  docstring across `src/` and the Python package against the set of defined
  names; this is the ONLY hit in 14,469 lines, which is why it is worth the line
  it takes to fix. FACT.

## What earns its place

- **`test_concurrent_writers_never_lose_a_write_or_publish_a_torn_file`**
  (`test_store_isolation.py:183-223`). It kills its mutant hard. Replacing
  `tempfile.mkstemp` in `write_json_atomic` with a shared `path + ".tmp"` name
  in a scratch copy produces `AssertionError: assert [FileNotFound...ectory'),
  ...] == []` with **73 lost writes** out of 160 across four threads - the
  measured failure the docstring describes, reproduced by the test that guards
  it. This is what a regression test is supposed to look like.

- **`test_probing_an_unknown_terminal_creates_nothing`**. Reddens with
  "DID NOT RAISE HTTPClientError" the moment `get_terminal` comes back. The
  16-line comment above `routes.py:822` earns its length: it explains why the
  attribute is read with no `getattr` default, which is the half no test states.

- **`resolvedLaunchModeEntry` and its five cases** (`modes.ts` at 95%
  statements / 88% branches). The one piece of `panel.ts` logic that was
  extracted so it could be asserted, and it is the one piece of DEF-37's
  neighbourhood with a net. It is the template for finding 1's fix, not a
  criticism.

- **`colour_store._read`'s asymmetry docstring** (`:41-50`) and the
  `_INIT_WAITER` header (`routes.py:33-52`). Both explain a one-way-door choice
  the code cannot state - why a legacy file counts as holding nothing hand-set,
  and why marking-and-polling in one process removes an interleaving rather than
  narrowing it. Round 4 asked for the launch-site comment to stop citing the
  deleted baseline read; it now cites the live reason, and the header carries the
  post-mortem once rather than twice.

- **The structural cleanliness.** Zero dead CSS in 62 selectors, zero unused
  Python imports, zero uncalled private members, zero duplicated 8-line blocks,
  zero unused TS exports that survive scrutiny, zero over-100-character Python
  lines, one stale comment identifier tree-wide. Four rounds of deletion did
  their job.

## Declined, with reasons

- **The 117 identical acc-crit `log:` lines.** Round 4 proved they are required
  by `acc-crit.py check --strict` and had them shortened. Pressing the residue is
  the loop antipattern - a round flagging the previous round's rewrite.
- **`.claude/plans/close-adversarial-defects-v1.0.0.md`.** Round 4 raised it and
  it was not taken; that is a judgement call about what belongs in `.claude/`,
  already made. Noting only that it is untracked and unignored, so `git add -A`
  would commit it with its rotted cite table.
- **The five `whole-repo round 3` citations in `docs/defects.md` pointing at a
  report series that does not exist in `logs/adversarial/`.** Same - round 4's,
  not taken, and it is bookkeeping, not correctness.
- **The `force`-vs-resolved rationale written three times** (`modes.ts:56-62`,
  `panel.ts:1738-1743`, `launch-mode.spec.ts:60-64`). Round 4's MINOR, declined.
  Taste. Not re-pressed.
- **`_resolvedVariant()`'s pointless local** (`panel.ts:1745-1749` assigns
  `resolved` and returns it on the next line). Taste. One line, no defect.
- **`IRemoveResponse` / `ICleanupResponse` / `IDeleteSessionsResponse` as three
  aliases of one `IDisposalReport`, each used exactly once.** Deliberate naming
  at the call site. Taste.
- **`assistantsIcon` looking dead.** Refuted before reporting - it registers
  itself with the LabIcon registry for `schema/plugin.json:5`. Recorded here so
  the next reviewer does not spend the same twenty minutes.
