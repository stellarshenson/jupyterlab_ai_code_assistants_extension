I'll start by reading the adversary definition and my round-1 report.

Now let me examine the current state of the tree.

VERDICT: DO-NOT-SHIP (10 new findings, 4 major) - the glyph fix reads correctly for every boolean mode, but on Gemini's enum it warns on `plan` (the safest setting) under a suffix that names the control instead of the value, and on a Claude background row it now labels an attach `(Skip Permissions)` when the server provably drops that flag.

## (a) Round-1 findings, re-confirmed

1. **CRITICAL glyph inversion - CLEARED for booleans, NOT-CLEARED for the enum and for the primary gesture.** `modes.ts:57-67` + `panel.ts:1693-1701, 1725, 1740, 1903, 1921`. I simulated the real descriptors: mode off → `Resume` unglyphed + `/!\ Resume (Skip Permissions)`; mode on → a single `/!\ Resume (Skip Permissions)`, duplicate suppressed. Correct in both states for Claude, Codex, Kimi and Gemini's `yoloMode`. Residual below as NEW-1, NEW-2, NEW-5, NEW-11.
2. **MAJOR contrast - CLEARED.** `base.css:188-193` empty states at `--jp-ui-font-color2` (4.58:1 light, computed); `base.css:902-904` stale dim moved to `.jp-AiAssistantsPanel-name` only (5.04:1 light / 6.52:1 dark at 0.65); `base.css:329-337` dot at `--jp-success-color0` full opacity; `base.css:394-404` star 0.8. Token ramp reverses in the dark theme so `color0` is correct in both.
3. **MAJOR veil on every show - CLEARED.** `panel.ts:213-221` gates `_setLoading(true)` on `this._sessions === null`; the poll never calls `refresh()` (`panel.ts:2206-2212` → `_fetch`).
4. **MAJOR Cancel that does not cancel - CLEARED.** `terminals.ts:47` `Dialog.okButton({ label: 'Hide' })`, rationale at `:44-46`.
5. **MAJOR poll destroys focus - PARTIAL.** `panel.ts:1258-1265, 1314-1322` restore a focused row by `data-encoded-path`. Section headers carry no key and are destroyed by their own click handler - NEW-3.
6. **MAJOR missing CLI silent - CLEARED, overcorrected.** `index.ts:259-262` `Notification.warning` naming the binary. See NEW-4.
7. **MAJOR four nouns - NOT-CLEARED** (acknowledged). `panel.ts:1942, 1949, 1884`, `popup.ts:186-190`. Ruling below.
8. **MAJOR separator before the destructive pair - CLEARED.** `panel.ts:2018-2021`.
9. **MINOR roving tabindex - NOT-CLEARED** (acknowledged). `panel.ts:1387`.
10. **MINOR three counts - NOT-CLEARED** (acknowledged). Badge `extra_sessions + 1` (`panel.ts:1429`), submenu `branches.length` (`panel.ts:2139, 2151`), `Manage Sessions... (${this._lastBranches.length})` (`panel.ts:1884`), popup `items.length + 1` (`popup.ts:186`).
11. **MINOR star reads as a control - CLEARED.** `panel.ts:1449-1459` `aria-label` + `role="img"`, no `title`.
12. **MINOR favourites vanish under a filter - NOT-CLEARED** (acknowledged). `panel.ts:1295-1297` still gates on `favourites.length > 0`, so `'No favorites yet.'` at `panel.ts:1363-1365` remains unreachable while its own comment at `:1360-1361` describes it as a live state.
13. **MINOR duplicated empty-state lines - NOT-CLEARED** (acknowledged). `panel.ts:1269-1278`; per-section `No matches.` at `:1362`.
14. **MINOR time column overflow - CLEARED.** `base.css:872-886` `4.5em` + `tabular-nums`.
15. **MINOR + button never names the folder - CLEARED (pointer path).** `panel.ts:306-312`. See NEW-10.
16. **MINOR popup Cancel - CLEARED; the sub-point is not.** `popup.ts:138` says `Close`. The cleanup dialog still demands a dismissal click on success - `panel.ts:683-689` only restores the footer in `finally` (`:731-734`); auto-dispose on the success branch was not taken.
17. **MINOR README phantom mode - CLEARED.** `README.md:43`.
18. **MINOR Gemini `yoloMode` beside `approvalMode` - NOT-CLEARED** (acknowledged) and now materially worse - NEW-1/NEW-5.
19. **MINOR (taste) indicator contrast + `favourite` spelling - CLEARED.** `base.css:329-337, 394-404`; `panel.ts:602` "favorite".

## (b) New friction

- **[MAJOR] NEW-1 The warning glyph now fires on Gemini's safest setting, under a label that names the control instead of the value.** `panel.ts:1698-1701` builds the suffix from `mode.menuLabel ?? mode.title`, and `approvalMode` has no `menuLabel` (`providers/gemini.ts:73-81`). Simulated against the real descriptor: `approvalMode: plan` → `/!\ Resume (Approval mode)`, `/!\ New Gemini Session (Approval mode)`, `/!\ Normal (Approval mode)`. `plan` is read-only - the most cautious value Gemini has - and it now wears the same triangle as `--yolo`. `auto_edit` and `yolo` render the identical string, so three different launches are indistinguishable in the menu. Fix: give each enum VALUE its own menu label and glyph it only for the values that widen approval (`yolo`, `auto_edit`); `plan` gets the suffix and no icon, `default` gets neither.
- **[MAJOR] NEW-2 An attach is labelled with a mode the server drops.** `panel.ts:1723-1725`: the plain resume item is `${resumeLabel(session)}${_variantSuffix()}` with the glyph, so a Claude background-agent row with `dangerouslySkipPermissions` on reads `/!\ Attach to Background Agent (Skip Permissions)`. `providers/claude.py:985-990` returns the argv before the flag is appended, with the comment "passing either would print that warning into the user's terminal and promise something it never did". The forced variant right below already guards this case (`panel.ts:1744` `isEnabled: () => !(hasBgAgents && bg_id)`); the plain item asserts it instead. Fix: suppress the suffix and the icon under the same predicate.
- **[MAJOR] NEW-3 Collapsing a section with the keyboard throws focus to `<body>`.** `panel.ts:1347-1351` re-renders on the header's own click; `panel.ts:1258-1265` keys focus restore on `dataset.encodedPath`, which the header button does not have. A keyboard user pressing Enter on `Recent (10)` loses their place at the moment of their own deliberate action - sharper than the 30 s poll case the fix was written for. Fix: put `data-section` on the header and restore by either key.
- **[MAJOR] NEW-4 A default install fires three warning toasts on every page load.** All four providers default `enabled: true` (`schema/plugin.json`), and `index.ts:259-262` warns once per id per load for each enabled-but-absent binary. A user who installed only Claude - the common case, since this extension supersedes the Claude one - gets three 8-second warnings about assistants they never asked for, and the only way to silence them is to find and untick three settings. Fix: warn only where the user explicitly set `enabled` (`settings.get(key).user !== undefined`), and aggregate the rest into one line; keep `console.info` for the default-on case.
- **[MINOR] NEW-5 The duplicate-suppression is keyed on mode id, so Gemini `approvalMode: yolo` still shows two glyphed entries.** `panel.ts:1740` compares `_resolvedVariant()?.id !== mode.id`, and `approvalMode` ≠ `yoloMode`, so `/!\ Resume (Approval mode)` and `/!\ Resume (YOLO)` sit adjacent, both auto-approving every tool call. This is the round-1 shape surviving on one path. Fix without touching the schema: suppress the boolean variant when the resolved TOKEN is `approvalMode=yolo` - a value comparison, no settings migration.
- **[MINOR] NEW-6 The focus restore undoes the scroll restore.** `panel.ts:1321` calls `.focus()` after `list.scrollTop` is written (`:1310`). A user who tabbed to a row and then wheel-scrolled it out of view gets scrolled back to it on the next poll. Fix: `focus({ preventScroll: true })`.
- **[MINOR] NEW-7 Two menus collapse to a single item once a mode is on.** `panel.ts:1932-1937` and `:1955-1962` build the `+` dropdown and the `Branch Session` submenu as plain-plus-variant; with the variant hidden each has one visible child. The `+` button now costs two clicks for its only outcome, and `Branch Session` is a hover-submenu holding one entry. Fix: when exactly one item is visible, execute it directly from `+` and flatten the submenu to a top-level `Branch Session (Skip Permissions)`.
- **[MINOR] NEW-8 `Normal (Skip Permissions)` contradicts itself.** `panel.ts:1902` labels the plain branch item `Normal` + suffix. Fix: `Default${suffix}`, or drop the word once a suffix is present.
- **[MINOR] NEW-9 Under reduced motion an explicit Refresh gives no feedback at all.** `panel.ts:225-230` waives the 500 ms floor and `base.css:441-448` replaces the spin with `opacity: 0.5`; with the veil now correctly gone on a warm refresh (`panel.ts:218`), a click on Refresh produces a few-millisecond dip on a 16px icon. Fix: keep the floor under reduced motion and hold a static dimmed/active state for its duration - a held state is not motion.
- **[MINOR] NEW-10 The `+` button's accessible name is still the generic one.** `panel.ts:306-312` refreshes `title` on `pointerenter` only, and the icon-only button has no other name, so a screen-reader or keyboard user hears "in the current folder" while a mouse user sees the folder. Fix: bind `focus` as well as `pointerenter`.
- **[MAJOR] NEW-11 (unclosed CRITICAL residual) The gesture that carries the mode is the one gesture that never mentions it.** A plain left-click on a row launches with the resolved mode (`panel.ts:751-755` → `_launchMode()`), and nothing outside the context menu says so: `_buildRowTooltip` (`panel.ts:1536-1570`) has no mode line, and the panel header has no indicator. The fix marked the menu, which is the minority path. Fix: one persistent chip in the panel header while a mode is in force - it is a panel-wide state, not a per-row one - plus a `Launch mode: <label>` line in the row tooltip.

## Ruling on the six not-done items

None of the six blocks on its own; one of them is now entangled with a blocker. **Gemini's `yoloMode`** does not need deleting to ship - NEW-1 and NEW-5 are both fixable in TypeScript with no schema change and no user-settings migration, so the owner's call can wait. **The four nouns** is the largest real cost to comprehension but costs no data and makes no false claim; its blast radius is six Playwright locators in `ui-tests/tests/claude-regression.spec.ts:124-216` plus `colour-override.spec.ts:58`, all mechanical - defer it to its own change. **Roving tabindex**, **favourites-under-filter**, **duplicated empty lines** and **the three counts** are all MINOR and stay MINOR. The one I would still fix before a release, because it is two lines: `panel.ts:1295` gating the Favorites section, which leaves the `'No favorites yet.'` branch dead while `panel.ts:1360-1361` documents it as reachable.

## Journeys I could not verify without a running JupyterLab

Whether Lumino's `lm-mod-hidden` leaves no visual gap where the suppressed variant sat (read from the renderer contract, not observed); the perceived stacking and dismissal of three simultaneous startup notifications; whether `.focus()` after `scrollTop` actually scrolls in JupyterLab's nested scroll containers; the section-header focus loss under a real screen reader; contrast under third-party themes - my figures are computed against the stock JupyterLab light/dark token values; and long-press as the touch route to the context menu.