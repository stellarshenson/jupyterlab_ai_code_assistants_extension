I have the lens and my round-1 report. Now let me survey the current tree.

This is a strong lead. Let me empirically verify the `dimensions` kwarg against the real terminado stack.

VERDICT: DO-NOT-SHIP (7 findings) - twelve of thirteen round-1 findings clear cleanly, but a fix added this session does nothing at runtime and is certified green by a mock, and the repo's own lint gate is red. Recommendations leave the system net SMALLER (~30 lines out, 4 config lines in).

Independently verified: 149 pytest, 81 Jest, `tsc --noEmit` clean, pyflakes clean. `jlpm lint:check` (the CI gate, `.github/workflows/build.yml:31`) FAILS.

## (a) Round-1 findings

| # | Round-1 finding | Status | Evidence |
|---|---|---|---|
| 1 | Derived-colour hash pinned on one side only | CLEARED | `tests/test_provider_stores.py:512-514` pins `peach`/`peach`/`lavender`, the same triple as `src/__tests__/colour.spec.ts:68-76` |
| 2 | Four dead wire fields | CLEARED on the wire | gone from `src/core/types.ts`, from `core/store.py` `list_sessions`, and from all three row dicts (`claude.py:743-750`, `codex.py:472-480`, `gemini.py:600-610`). `model`/`tokens_used`/`live` correctly kept - readers at `providers/codex.ts:77-81`, `panel.ts:1522,1560`. Residue inland → new finding 6 |
| 3 | `bg` chip built three times, one unreachable, seam with one implementor | CLEARED | `bgChip` gone from `providers/claude.ts`; `IProviderHooks` (`types.ts:287-297`) now holds three hooks, all called (`panel.ts:1566,1644,1724`); `panel.ts:2176` calls `_bgBadgeFor` unconditionally and it gains `title` at `:2196`. The surviving second builder at `panel.ts:1438-1443` differs deliberately (no `title`, reason at `:1436-1437`) |
| 4 | Migration targets unchecked, docstring names a dead key | CLEARED | `core/registry.py:83` names the real key; `tests/test_registry.py:206` asserts `new in schema["properties"]`. I re-read the schema - all 13 property keys are the dotted flat form, so the assertion is meaningful, not vacuous |
| 5 | `legacyPluginId` outside the parity binding | CLEARED | `tests/test_descriptor_parity.py:42,94-96` |
| 6 | LOG_PREFIX broken in 2 of 12 sites | CLEARED | `request.ts:47` prefixed; `registry.ts:3-7` names the one exception. Full census re-run: 13 prefixed sites, 1 unprefixed (`index.ts:69`), which `ui-tests/.../jupyterlab_ai_code_assistants_extension.spec.ts:24` and `docs/acc-crit-...:348` both pin verbatim. Your decline is correct; I withdraw it |
| 7 | README advertises a third presentation mode | CLEARED | `README.md:43` |
| 8 | `fork_strategy: "none"` vocabulary split | CLEARED as an inconsistency, NOT as a whole | `src/core/types.ts:21-22` now admits `'none'`; one vocabulary, and your objection to churning 19 fixtures is sound - I concede the smaller fix. But the doc line you added with it is false → new finding 4 |
| 9 | `colour_store.load_overrides` zero production callers | CLEARED | symbol absent repo-wide |
| 10 | `IHookContext` never read; `menuLabel` guard | CLEARED | `types.ts:292` `tooltipLines?: (session: ISession) => string[]`, `providers/codex.ts:75` matches; `IHookContext` gone. `menuLabel` left optional as round 1 permitted |
| 11 | Server-copy fork writes the colour twice | CLEARED | `panel.ts:935-939` keeps only `_branchCache = null`, with the reason recorded. Left a dead parameter → new finding 3 |
| 12 | `_tree_assistant` shallowest-wins unstated | CLEARED | `core/routes.py:128-133` states it; `tests/test_routes.py:507-533` pins it with claude at depth 0 and codex at depth 1 |
| 13a | `REFRESH_SPIN_FLOOR_MS` inline | CLEARED | `panel.ts:73`, used `:231` |
| 13b | `'right'` spelled four times | CLEARED | `index.ts:30`, used `:115,338,339,351` |
| 13c | `codex.py launch_argv` parameter order | NOT CLEARED | `codex.py:670-672` still `mode, name, fork_from` vs `store.py:331-333` `fork_from, mode, name`. Keyword-only, so zero functional risk - **I withdraw it**, it was taste and does not earn an edit |
| 13d | `assistantsIcon` exported with no importers | NOT CLEARED | `src/core/icons.ts:19`; name restated at `scripts/generate-schema.mjs:116`. **I withdraw it** for the same reason |

`mkdirp` decline accepted - a lockfile desync costs more than a phantom dependency. It is a standing one-line debt for whichever change next regenerates the lock legitimately.

**Reconstructed `core/routes.py` - verified structurally.** 12 handler classes, 10 route tuples, all handlers reachable from `setup_route_handlers:854-891`; `asyncio` imported at `:11`; every `colour_store` symbol the file calls exists with the matching arity (`load_store`, `load_colours`, `is_colour`, `set_colour(...,hand_set)`, `drop_colours`→bool, `inherit_colour`); both `_effective_colour` call sites (`:328` with `stored`, `:824` without) resolve identically because `get_colour` is `load_colours(...).get(...)`. No handler, no route and no docstring is missing against HEAD; the only diff hunks are this session's own work. 149 pytest pass. The file is sound.

## (b) New defects

- **[CRITICAL] The pty size fix does nothing, and its test certifies it through a mock** - `core/routes.py:747` passes `dimensions=(1, 1)` to `terminal_manager.create`, with six lines of comment at `:740-746` explaining that this dodges terminado's 24x80 default so the init waiter always sees a real SIGWINCH. It does not. `jupyter_server_terminals.TerminalManager` (0.5.4) defines neither `new_terminal` nor `new_named_terminal`, so the kwarg reaches `terminado.TermManagerBase.new_terminal`, which uses `options["shell_command"]`, `make_term_env(**options)` and `options.get("cwd")` and constructs `PtyWithClients(argv, env, cwd)` - `dimensions` is swallowed by `make_term_env`'s `**kwargs` and never reaches `PtyProcessUnicode.spawn`, whose own default is `(24, 80)`. I ran it: `SingleTermManager(...).new_terminal(dimensions=(1, 1))` then `TIOCGWINSZ` on `term.ptyproc.fd` reports **24 x 80**. `tests/test_routes.py:378` asserts `manager.created[0]["dimensions"] == (1, 1)` against `_FakeTerminalManager` (`:339-354`), a mock whose `create` accepts and records a kwarg the real library discards - so the suite is green on a behaviour that does not exist, and the test docstring at `:358-364` promises the user-facing five-second stall is gone. Fix, smallest first: either delete the kwarg, the comment and the test (−19 lines, and re-open the stall as a defect entry rather than leave code that lies), or keep them and make the resize real with the mechanism already in this file - `TerminalHandler:807` already reaches `terminal.ptyproc`, so `terminal_manager.get_terminal(terminal_name).ptyproc.setwinsize(1, 1)` after the `create` is one line, and the test must then assert the winsize, not the mock's argument list. Do not leave it as is: an inert fix with a confident comment is worse than the open defect.

- **[MAJOR] The CI lint gate is red on three tracked files, all modified this session** - `.github/workflows/build.yml:31` runs `jlpm run lint:check`, which includes `prettier:check` over `**/*{.ts,.tsx,.js,.jsx,.css,.json,.md}`. Three tracked files fail: `src/index.ts:351` (the `DEFAULT_SIDEBAR` ternary is 90 columns and must break across three lines), `src/__tests__/colour.spec.ts:251,252,277,299` (four `mockReturnValueOnce(new Promise(...) as any)` calls over width), `docs/acc-crit-jupyterlab-ai-code-assistants.md:324-330` (the Jest column is padded two characters wider than its widest cell). Fix: `jlpm prettier`. Adds ~4 lines of wrapping, removes nothing else.

- **[MAJOR] Graph artefacts are neither gitignored nor prettierignored, which is why the gate above went unnoticed** - `graphify-out/` (1.6M, 33 files), `tmp/` (8.3M, 81 files) and `ui-tests/tmp/` (180K) are untracked and matched by no rule in `.gitignore` or `.prettierignore` (which covers only `node_modules`, `lib`, `package.json`, `eslint.config.mjs`, `.venv`). Two consequences, both live: `git add -A` before a release commits 10M of AST cache and `GRAPH_REPORT.md`, and locally `prettier:check` reports **120** failing files instead of 3 - noise that hides the real ones, which is the plausible reason "eslint 0 errors" was checked and prettier was not. `uv.lock` is in the same state. Fix: four lines in `.gitignore` (`tmp/`, `graphify-out/`, `ui-tests/tmp/`, `uv.lock`) and two in `.prettierignore` (`**/tmp`, `graphify-out`). Adds 6 config lines; the only recommendation here that grows anything, and it buys back a usable lint gate.

- **[MINOR] The round-1 fork-colour fix left a dead parameter, and the compiler flag that would have caught it is off** - `panel.ts:919` declares `parentColour: string | null` on `_branchByServerCopy`; deleting the `_inheritColour` call at what is now `:935-939` left it with no reader in the body (`:921-960`), and `panel.ts:845` still computes and passes it. `tsconfig.json:14` sets `noUnusedLocals: true` but not `noUnusedParameters`, which is why `tsc` stayed clean. I ran `tsc --noEmit --noUnusedParameters`: exactly one error repo-wide, this one. Fix: drop the parameter and the argument (−2 lines; keep `const parentColour` at `:829`, the other two strategies still use it), and add `noUnusedParameters: true` beside its sibling (+1 line, clean today, closes the class).

- **[MINOR] The `'none'` fork strategy documents a behaviour no code implements** - `src/core/types.ts:16` says "`none` - the assistant cannot branch; the menu offers no fork". Nothing reads the value: `panel.ts:844,848` branch on `server-copy` and `native-command` and fall through to `_branchByNativeFlag`, and the `branch-session` command (`panel.ts:1901-1905`) carries no `isEnabled`/`isVisible`. A descriptor declaring `'none'` would silently perform a native-flag fork. Do not add the gate - zero descriptors need it, and a guard for an impossible state is the defect I am paid to remove. Fix the fiction instead: reword the bullet to what is true - `'none'` is the Python `Capabilities` default, spelled here so a descriptor that omits the flag is legal in both runtimes; no provider uses it and the panel does not special-case it. (−1 line of claim, +0 code.)

- **[MINOR] Dead-field residue the wire cleanup did not follow inland** - two producers still compute fields with no reader anywhere. `providers/claude.py:648-652` - the `latest` fallback dict supplies `summary`, `firstPrompt`, `created`, `modified`; every read of `latest` is a keyed `.get` (`:658,666,668-670,713,726,738,745-747,755,807,848`) and none names those four. `providers/gemini.py:305-306` - `_parse_chat` returns `created`/`modified`, grepped repo-wide for readers and there are none; `summary` and `first_prompt` genuinely have them (`:589,643-644`), as you said. Dropping the two also frees `"startTime"` and `"lastUpdated"` from `_META_KEYS` (`:110`) - the fork's own timestamp rewrite at `:376-379` reads `payload`, not `meta`. No test asserts any of the six. Fix: delete. (−8 lines.)

- **[MINOR] (taste) Two residual seams, one edit each** - `core/routes.py:191-195` gives `_effective_colour` an optional `stored` map and two lookup paths for one question; `TerminalHandler:824` is the only caller taking the `None` path, and `get_colour` performs the identical `load_colours` read internally, so passing `colour_store.load_colours(provider.id)` there makes `stored` required and deletes a branch at no cost (−3 lines; `get_colour` keeps its `inherit_colour` caller). `core/popup.ts:31,293` keeps `branchBadge?` optional with `?.` when its single call site (`panel.ts:2176`) always supplies it - drop the two tokens, or leave it and say so; either is fine, the guard costs nothing to keep.

## Convention census (deltas since round 1)

| Mechanism | Sites | Canonical |
|---|---|---|
| TS console prefix | 13 prefixed, 1 documented exception (`index.ts:69`) | `LOG_PREFIX` - now consistent |
| Colour vocabulary | `colour.ts:22`, `kimi.py:78` | both pinned to the same three literals |
| Fork strategy tokens | `types.ts:21-22`, `registry.py:71`, `test_registry.py:30` | one four-value vocabulary across both runtimes |
| Lint gates | eslint ✅, stylelint ✅, tsc ✅, pytest ✅, jest ✅, **prettier ❌** | all six are `lint:check` + CI; prettier is the outlier |
| Ignore files | `.gitignore` / `.prettierignore` both miss `tmp/`, `graphify-out/`, `ui-tests/tmp/` | one shared exclusion set |

## What is already consistent

- **The parity binding is now complete for every cross-runtime field** - id set, `cliBinary`, `forkStrategy`, `colourSource`, launch-mode tokens and `legacyPluginId`; `test_descriptor_parity.py` skips loudly rather than passing vacuously when `lib/` is absent.
- **Migration is bound end to end** - the target key must exist in the shipped schema (`test_registry.py:206`), and `index.ts:314-327` now fails one key at a time instead of losing the rest of the batch.
- **The shallowest-wins invariant is stated and tested**, which is what makes the "just ask the calling provider" simplification permanently unavailable to the next reader rather than merely unattempted.
- **`pyflakes` is clean across the package** and `IProviderHooks` is down to three hooks with three live call sites - no seam without an implementor remains on the provider contract.